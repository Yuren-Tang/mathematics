# AffineCDC — Independent B2/B8 source-fidelity re-audit

## 1. Control, fixed object, and write boundary

Role: `AffineCDC — Independent B2/B8 Source-Fidelity Auditor` (`AC-AUDIT-SF-01`).

Controlling authority:

- `Yuren-Tang/research-workbench#46`;
- exact candidate: `curation/affine-cdc-programme-a-b1-b8-source-fidelity-v1@f4d6f801fac69746ca0b2ce9351735a43c79b482`;
- immutable predecessor: `curation/affine-cdc-programme-a-b1-b8-unified-v1@ec765cd03271abd3588ec36faec3d53d0f8aa03b`;
- exact authorial repair source: `proof-development/affine-cdc-rigour-v1@9ce8de5ca5b7b41e139be4c94572de7725446046`;
- prior independent assurance source: `audit/affine-cdc-five-support-corpus-v1@110f014c551d4ce0f109ca5559d234ddb124d8f1`;
- controlling correction and return records: issue #24 comments `5029523835` and `5042745814`;
- prior Audit B control and defect record: issue #36.

Review branch:

`audit/affine-cdc-b2-b8-source-fidelity-reaudit-v1`, created exactly at candidate SHA `f4d6f801fac69746ca0b2ce9351735a43c79b482`.

This audit writes only the three files under

`audit/affine-cdc-b2-b8-source-fidelity-reaudit-v1/`.

## 2. Independence protocol and method

The review was performed in two ordered passes.

### Pass I — independent reconstruction

Before accepting any Curator conclusion, the audit reconstructed the disputed source, proposition, packet, migration, and assurance classifications directly from:

1. the exact predecessor and its active B2/B8 statements;
2. the exact historical packet body at
   `dad218dd18ed05d1b7cb730c2dc2431b4db5ec9c:projects/affine-cdc/research/FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md`, blob `2043ada9d28789ecc5f4f0028e62133f37835bc1`;
3. the prior Audit B report, claim/source matrix, and defect/supersession/frontier ledger at `110f014c...`;
4. the four authorial B2/B8 repair dossiers at `9ce8de5...`;
5. the complete packet list and the exact B2 mathematical argument.

This pass independently established:

- the historical packet is a valid fixed-dimensional theorem packet;
- the arbitrary-rank proposition is not present in that packet and has no earlier recovered committed theorem source within the bounded repository search;
- the two objects must be classified separately;
- the B2 lower-bound and deleted-module mathematics is valid independently of that source attribution;
- the historical packet population remains seventy-eight and the named packet belongs to the theorem/mechanism class;
- only deductions that actually use the arbitrary-rank proposition may be marked affected.

### Pass II — candidate comparison

Only after Pass I was complete did the audit compare that reconstruction with the candidate’s manifests, active chapters, source ledgers, assurance ledgers, migration maps, supersession maps, dependency maps, status files, and final Curator audit.

No Curator or PDL statement was treated as independent assurance merely because it appeared in the candidate.

## 3. Final classification

`SOURCE-FIDELITY REPAIR VERIFIED / READY FOR AC-DIR CANONICAL DISPOSITION`.

This classification means that the bounded B2/B8 source-fidelity replacement has passed independent re-audit and may return to `AffineCDC — Director` for canonical disposition.

It does **not** itself:

- move `main`;
- create canonical status;
- approve a manuscript;
- establish end-to-end Lean verification;
- authorize release, publication, submission, arXiv, DOI, tag, novelty, priority, or timestamp claims.

## 4. Exact splice and isolation

### 4.1 Base ancestry

The candidate is a normal descendant of the immutable predecessor:

- merge base: exactly `ec765cd03271abd3588ec36faec3d53d0f8aa03b`;
- candidate: thirteen commits ahead, zero behind;
- no history rewrite is required or observed.

The audit branch was independently checked to be identical to the candidate before the audit files were added.

### 4.2 Exact source splice

The source splice is

`344bf612d5d4231d954cb57fad15d93aa55d27d5`.

Relative to the predecessor, that splice changes exactly four substantive paths:

| Imported path | Exact blob at candidate | Exact blob at `9ce8de5...` |
|---|---|---|
| `projects/affine-cdc/proof-development/AC_PD_B2_3_ORTHOGONAL_ROOT_MODULE_CORRECTION.md` | `a15cb4aeea52b2c44691e2e7184ea67b1094cc94` | identical |
| `projects/affine-cdc/proof-development/AC_PD_B2_3_SOURCE_FIDELITY_REPAIR.md` | `3931acfe5de3672e3a099c83c32bc9556f40c9b0` | identical |
| `projects/affine-cdc/proof-development/AC_PD_B2_FORMULATION_AND_WITNESS_MAP.md` | `50673832956a4b43867010a4b24973693025f778` | identical |
| `projects/affine-cdc/proof-development/AC_PD_B8_SOURCE_FIDELITY_ADDENDUM.md` | `714f4bfc860ef796f8285b0b396b51bf41bf6d5b` | identical |

The splice has the exact fixed predecessor and exact PDL repair checkpoint as its ancestry inputs. The imported four blobs are byte-identical to the authorial checkpoint.

### 4.3 Exclusion of later work

The splice comparison shows no import of:

- `OBLIGATION_DAG.md` moving state;
- PDL Curator handoffs;
- `AC_PD_B9_CORRECTED_GLOBAL_FRONTIER_ASSEMBLY.md`;
- later B9 work;
- moving AC-RL or later PDL mathematics.

The candidate’s substantive B1 and B3–B7 chapters are not changed by this correction. The changed shared surfaces are attribution, migration, supersession, assurance, provenance, status, and entrypoint surfaces needed to propagate the bounded repair.

**Result:** exact splice and isolation verified.

## 5. Orthogonal packet and proposition separation

### 5.1 Exact historical packet

Direct inspection of the complete historical blob `2043ada9...` establishes that it fixes

`Γ=F_2^3`

and proves the following fixed-dimensional package:

- the eight-coordinate even-weight module `H_8`;
- the extended-Hamming kernel and moment map;
- the six-dimensional plus-type quotient `O^+(6,2)`;
- the twenty-eight weight-two anisotropic roots;
- rank-three compatible AffineCDC root lifts;
- five-coordinate four-dimensional minus-type `O^-(4,2)` slices;
- omitted-triple orthogonality and the corresponding five-support formulation.

The packet does not contain:

- arbitrary `Γ=F_2^r` complete-root data;
- a canonical `Γ⊕Γ*` construction;
- the formula `d_h(a)=([a],ev_a)`;
- a universal `O^+(2r,2)` complete-root tower.

Its correct status is therefore:

`THEOREM-LEVEL HISTORICAL SOURCE / EXACT RANK-THREE-EIGHT-SUPPORT AND FIVE-SLICE SCOPE`.

### 5.2 Separate arbitrary-rank proposition

The proposition asserting a universal arbitrary-rank complete-root tower is a different claim. No earlier exact committed theorem source was recovered within the bounded source interval and repository-history search recorded by the repair. The first recoverable committed layer treating it as an earlier theorem is the original B2.3 correction layer itself.

Its honest classification is:

`NO RECOVERED PACKET / SOURCE-UNRECONSTRUCTED / INFERRED-EXTRAPOLATION OR UNCOMMITTED DRAFT / MATHEMATICALLY REFUTED BY B2.3`.

`SOURCE-UNRECONSTRUCTED` is a bounded repository-source conclusion, not an unrestricted claim that no such text could exist anywhere outside the audited source boundary.

### 5.3 Genuine all-rank hierarchy

The all-rank transgression/residue hierarchy over `F_2^r` remains a valid and independently sourced rank-dependent hierarchy. It does not assert that every pair of the `2^r` points is represented by an anisotropic root in dimension `2r`.

**Result:** the candidate correctly separates all three objects:

1. valid fixed-dimensional packet;
2. source-unreconstructed and refuted arbitrary-rank extrapolation;
3. genuine all-rank transgression/residue hierarchy.

## 6. B2 mathematics and source boundary

The B2 mathematical correction was checked independently.

Let `I` have even cardinality `q`, and suppose anisotropic roots satisfy

$$
Q(r_{ab})=1,
\qquad
r_{ab}+r_{bc}+r_{ca}=0.
$$

Fixing a base point gives point potentials

$$
r_{ab}=p_a+p_b.
$$

For the `q-1` nonbase potentials, the polar Gram matrix is `J+I`. Since `q-1` is odd, its kernel is the all-one line and

$$
\operatorname{rank}_{F_2}(J+I)=q-2.
$$

Therefore

$$
\boxed{\dim V\ge q-2.}
$$

For `q=2^r`, a `2r`-dimensional complete-root model is impossible for every `r≥4`. The deleted permutation module

$$
\overline E_I=
\left\{z\in F_2^I:\sum_i z_i=0\right\}/\langle\mathbf1_I\rangle
$$

has dimension `q-2`, carries the roots `[ε_a+ε_b]`, satisfies triangle addition, and attains the lower bound in the relevant `q=2^r`, `r≥2` setting. Rank three is exceptional because

$$
8-2=6=2\cdot3.
$$

The five-support slice remains the exact four-dimensional `O^-(4,2)` model.

The candidate also preserves the pre-existing B2 witness hierarchy:

- indexed five supports;
- `R_5` flow and `K_5` triangles;
- anisotropic `O^-(4,2)` flow;
- quadratic-cycle and cographic graph-level witnesses;
- fixed singular/Schur criteria with retained quotient and torsor data;
- stress/Fredholm and Fourier fixed-orbit boundaries.

Comparison with the predecessor shows that the repair changes the false attribution and associated assurance language, not the mathematical strength of these B2 results.

**B2 conclusion:** theorem content preserved; source boundary repaired; no new B2 mathematical defect found.

## 7. Seventy-eight-packet accounting

The candidate’s retired-source classification was checked from the explicit packet list, not merely from its displayed class equation.

The disjoint classes contain:

- 39 controlling theorem/mechanism packets;
- 10 independent proof/representation packets;
- 18 finite theorem/witness/certificate packets;
- 10 correction/negative-boundary packets;
- 1 programme-synthesis packet.

Thus

$$
39+10+18+10+1=78.
$$

The named orthogonal packet appears exactly once among the thirty-nine theorem/mechanism packets. The arbitrary-rank extrapolation is not a recovered packet and is excluded from the packet count and packet assurance classes.

The predecessor’s two conflicting partitions are correctly superseded. There is no remaining packet-wide false-theorem class.

**Packet conclusion:** exact population `78`; partition and orthogonal placement verified.

## 8. B8 assurance

The candidate preserves the six B8 assurance classes:

- `F-PROVED`;
- `F-CERT-PUBLIC`;
- `F-CERT-PRIVATE`;
- `F-CENSUS`;
- `CODE-PARTIAL`;
- `AFFECTED`.

The source-fidelity correction does not globally relabel the orthogonal packet or unrelated finite data. In particular:

- fixed rank-three/eight-support and five-slice theorems retain their actual proof or certificate classes;
- the `q-2` lower bound, deleted permutation module, rank-three exceptionality, and exact five-support model remain `F-PROVED`;
- only a computation or deduction that truly presupposes the nonexistent arbitrary-rank tower is `AFFECTED` for that inference;
- pre-existing `AFFECTED` labels for BBD groupoid inference, defect variation, and the old matching representative remain independently justified and are not conflated with this repair;
- `CODE-PARTIAL` remains explicitly non-load-bearing;
- no finite count is promoted to a universal theorem.

**B8 conclusion:** assurance effects are dependency-local and source-honest; unrelated B8 results remain unchanged.

## 9. Migration, supersession, and frontier honesty

The candidate’s active B2 chapter, root-lifting chapter, proof-family chapter, finite-assurance chapter, retired-source classification, recovery audit, migration ledger, supersession map, dependency map, active-surface map, current-best summary, formal-status file, and dedicated correction ledgers all agree on the following:

1. the exact packet is retained in fixed-dimensional theorem scope;
2. the arbitrary-rank proposition is a separate non-packet extrapolation;
3. the extrapolation is mathematically superseded by the `q-2` theorem and deleted permutation module;
4. the genuine all-rank transgression/residue hierarchy remains valid;
5. the packet count is `39+10+18+10+1=78`;
6. B8 effects are restricted to actual dependencies;
7. Programme A, B1, and B3–B7 mathematics retain their prior status;
8. all six AC-RL frontier obligations remain open;
9. B9 is excluded and remains blocked on localisation/composition;
10. the global five-support theorem remains open.

No active surface inspected retains the predecessor’s false attribution or packet-wide false-theorem classification.

No canonical, Lean, manuscript, novelty, priority, publication, release, arXiv, DOI, tag, or submission status is created by the candidate.

## 10. Defects and non-blocking repairs

### Material defects

None found.

### Bounded candidate corrections

None required.

### Non-blocking repairs

None required for source fidelity or canonical readiness.

Two interpretive boundaries remain standing controls rather than defects:

- `SOURCE-UNRECONSTRUCTED` is bounded to the audited committed source boundary;
- `READY FOR AC-DIR CANONICAL DISPOSITION` authorizes Director disposition only and no downstream public action.

## 11. Stop-condition assessment

No stop condition was triggered.

- every load-bearing source blob was reconstructible;
- the historical packet and disputed proposition cease to be contradictory after exact scope separation;
- the mathematical correction is independently checkable without new frontier mathematics;
- the seventy-eight-packet partition is reconstructible;
- the candidate can be audited without modification;
- independence was maintained by completing source reconstruction before candidate comparison.

## 12. Canonical-readiness conclusion

The candidate has repaired the exact source, proposition, packet, migration, supersession, and B8-assurance defects identified by Audit B while preserving the actual B2 mathematics and all unrelated Programme A/B1/B3–B8/frontier statuses.

Final class:

`SOURCE-FIDELITY REPAIR VERIFIED / READY FOR AC-DIR CANONICAL DISPOSITION`.

Receiver: `AffineCDC — Director` (`AC-DIR`).

## 13. Isolation and lifecycle confirmation

No candidate, predecessor, authorial PDL source, prior audit branch, AC-RL branch, `main`, Lean repository, Paper A/manuscript, release, tag, arXiv, DOI, publication, or submission surface was modified.

No force-push, rebase, squash, branch deletion, branch rename, or history rewrite was performed.

This one-time audit terminates upon return to issue #46.
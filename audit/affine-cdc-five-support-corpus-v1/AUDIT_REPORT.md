# AffineCDC — Independent Audit B report

## 1. Control and fixed object

Role: `AffineCDC — Independent Audit B` (`AC-AUDIT-B`).

Controlling authority:

- `Yuren-Tang/research-workbench#36`;
- Tier A retarget supplement comment `5026232609`;
- current `projects/affine-cdc/DIRECTOR_STATE.md` and `STATUS.md`.

Fixed candidate:

`Yuren-Tang/mathematics:curation/affine-cdc-programme-a-b1-b8-unified-v1@ec765cd03271abd3588ec36faec3d53d0f8aa03b`.

Audit branch:

`Yuren-Tang/mathematics:audit/affine-cdc-five-support-corpus-v1`, created at the exact candidate SHA.

The audit did not reopen Programme A theorem truth, did not attempt the global five-support theorem, and treated B9 only as a frontier boundary.

## 2. Audit method

The audit compared the unified candidate against the fixed checkpoints:

- B1: `4d7b9c74ea4377a58e219a7c6c3cb569a8229276`;
- B2: `08b662ecc31848456b9904f4d5b85c3801d090a1`;
- B3–B8: `0100895d57aab7d21153c580fa9bdc45fafc832e`;
- retired source checkpoint: `dad218dd18ed05d1b7cb730c2dc2431b4db5ec9c` where needed.

The B1, B2, and six substantive B3–B8 active chapters are blob-identical to their fixed accepted Curator checkpoints. The unified delta after B3–B8 changes no substantive `five-support/**` path; it changes Programme A/unified entrypoints, provenance, dependency, assurance, and status surfaces only.

The review reconstructed the claim surface through the active chapters, theorem/dependency maps, provenance and supersession ledgers, B1–B8 proof-development maps/dossiers, B8 assurance ledger, retired source blobs, and public issue #2.

## 3. Final acceptance classification

`MATERIAL THEOREM OR MIGRATION DEFECT`.

This classification is specifically a **material source-migration and supersession defect**, not a finding that the corrected `q-2` lower bound, deleted permutation module, or the fixed B1–B8 theorem layers are mathematically false.

The candidate is largely status-honest at the theorem/frontier level, but it is not source-complete as claimed because one exact historical source packet is materially misdescribed and misclassified.

## 4. Material migration defect

The candidate and B2.3 repeatedly attribute the following false claim to

`projects/affine-cdc/research/FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md`

at

`dad218dd18ed05d1b7cb730c2dc2431b4db5ec9c`:

- a rank-free model `V=Γ⊕Γ*` of dimension `2r`;
- a displayed map `d_h(a)=([a],ev_a)`;
- an additive anisotropic-root model for all pairs of the `2^r` points of `Γ=F_2^r`;
- a universal `O^+(2r,2)` tower.

The exact historical source blob is

`2043ada9d28789ecc5f4f0028e62133f37835bc1`,

introduced by commit

`a172b2acb13fc0b042ecb099a08b9631ab1db59a`.

Direct inspection of the complete 480-line blob shows instead:

- `Γ=F_2^3` throughout the universal eight-index construction;
- the even-weight `H_8`, Hamming kernel, six-dimensional `O^+(6,2)` quotient, and rank-three moment map;
- five-coordinate minus-type slices `O^-(4,2)`;
- no arbitrary-rank `Γ⊕Γ*` construction;
- no formula `d_h(a)=([a],ev_a)`;
- no theorem asserting a universal `2r`-dimensional tower.

Accordingly, the active classification

`RETIRED / SUPERSEDED / FALSE THEOREM / HISTORICAL PROVENANCE`

is not faithful to the named exact source blob. It suppresses a valid rank-three/eight-support source packet by charging it with a different proposition not present in that packet. The same inaccurate attribution is propagated through the active B2 chapter, root-flow chapter, proof-family chapter, source classification, migration ledger, supersession map, B2 final audit, unified status surfaces, and public correction quarantine.

The mathematical correction proved in B2.3 may be valid as a correction of some other draft, extrapolation, or source. The candidate does not presently identify that actual source by exact commit, path, and blob. Until it does, source fidelity and the claimed exhaustive 78-packet disposition fail.

This triggers the controlling stop condition: repairing the defect requires mutation of the fixed candidate/public correction surfaces outside the audit write boundary.

## 5. Secondary bounded defect

`SOURCE_RECOVERY_AUDIT.md` retains the old class equation

`39+10+18+10+1=78`,

while the correction-aware classification elsewhere is

`38+10+18+10+1+1=78`.

The total population remains 78 and ordinary Git recovery remains intact. Standing alone this would be a non-blocking accounting repair. In the presence of the material packet-attribution defect, however, the corrected class partition itself cannot yet be accepted as source-faithful.

## 6. Per-layer mathematical audit

### Programme A unified entrypoint

The unified entrypoint accurately reports the prior Audit A result and the three closed expository/source repairs. No contradiction with the B corpus was found. Programme A theorem truth was not reopened.

### B1 — object, quantifiers, and fixed-data scope

Verified in stated scope.

- indexed five-support even double covers, `R_5`-flows, and `K_5` triangle labels are mutually reconstructible;
- the matching/four-flow formulation retains the second even support or the exact component endpoint-parity condition;
- fixed-flow/fixed-plane criteria are not confused with graph-level existential quantification;
- fixed-lift, prescribed-surface, and graph-level surface statements are separated;
- `T_g^(1)` and `J_g` are not conflated.

### B2 — witness hierarchy and orthogonal correction

The active witness hierarchy is internally status-honest:

- graph-level witnesses retain reconstruction data;
- singular/Schur criteria are fixed-data criteria;
- stresses and Fourier counts are dual/counting layers, not source witnesses;
- the `q-2` lower bound and deleted permutation module are mathematically supported by the authorial dossier.

The B2 layer nevertheless fails source fidelity because it attributes the false higher-rank theorem to the wrong exact packet.

### B3 — full dual and factorable quotient

Verified in stated scope.

- `T_g^(1)→A_5` is full componentwise same-embedding compression;
- `J_g→A_5` is the old-colour-factorable subclass;
- factorable failure is not full-dual failure;
- common-link/capacity results are theorem-level;
- flower and numerical core data remain certificate/census material;
- the corrected all-parallel representative is `{01,23,45}` with orbit sizes unchanged.

### B4 — vertical/horizontal motion

Verified in stated scope.

- gauge/Petrial motion is vertical and fixed-flow;
- a support pivot is a connected horizontal move giving one explicit lift in the new fibre;
- disconnected support yields a commuting path, not one adjacency;
- the new fibre must be recomputed;
- linear internal/external images classify composite endpoints;
- `7737` and `2801` are correctly separated.

### B5 — cuts, four-poles, and routing

Verified in stated scope.

- local law, three-cut gluing, ten-state alphabet, profile-intersection gluing, cap forcing, pairing alignment, routing weights, and uniform-routing elimination are theorem-level;
- cap forcing is not full-cap containment;
- abstract transitions are not source path-pair realizability;
- mismatch kernels, policies, monodromies, and small census remain finite/certificate material.

### B6 — holonomy, defects, and atoms

Verified after the installed corrections.

- individual-loop affine holonomy is not promoted to a physical global group;
- simultaneous BBD origin is conditional on `AC-RL-BBD-GROUPOID-CLOSURE`;
- the vacuous fixed-terminal defect minimization does not yield a nontrivial forest;
- a replacement requires `AC-RL-BBD-VARIATION-SLICE`;
- DDD triality and unique-bad-route results remain independent;
- local K6/Petersen geometry is conditional on an independently supplied defect strip.

### B7 — rank, curvature, and localisation

Verified in stated scope.

- rank one is excluded;
- rank two gives a Tait/root-triangle escape;
- full rank has the stated flat/nonflat curvature duality;
- scalar-sheet cuts are not represented as source graph cuts;
- finite potentials are not represented as bounded transfer interfaces;
- global composition remains open.

### B8 — finite assurance

The theorem/certificate/census/code-partial distinctions are explicit and generally correctly propagated. No finite count is silently promoted to a universal theorem. The source-classification portion remains affected by the material packet-attribution defect.

## 7. Frontier and B9 boundary

Exactly six obligations remain open:

1. `AC-RL-BBD-GROUPOID-CLOSURE`;
2. `AC-RL-BBD-VARIATION-SLICE`;
3. `AC-RL-TYPE-T-SERIALISATION`;
4. `AC-RL-FLAT-POTENTIAL-INTERFACE`;
5. `AC-RL-COMMON-CUT-LOCALISATION`;
6. `AC-RL-TYPE-H-COMMON-WITNESS`.

No one was silently closed or weakened. B9 and RL working-ahead mathematics were not consumed or dispositioned. The global five-support theorem remains open.

## 8. Required disposition

Before this candidate can receive a source-complete Audit B acceptance, a correction-aware replacement candidate must:

1. identify the actual exact source, if any, of the arbitrary-rank `Γ⊕Γ*` / `d_h(a)` / `2r` claim;
2. if no such source exists, retract the attribution and describe B2.3 as a correction of an extrapolation or uncommitted draft rather than of the named packet;
3. restore the named packet's valid rank-three/eight-support and five-slice source role;
4. revise all propagated supersession, classification, migration, assurance, and public-quarantine statements;
5. repair the stale class equation in `SOURCE_RECOVERY_AUDIT.md`;
6. return the revised fixed candidate for a bounded re-audit of B2/B8 source fidelity.

No new proof of the global five-support theorem is required for this repair.

## 9. Isolation and lifecycle

Only the three required audit deliverables are written under

`audit/affine-cdc-five-support-corpus-v1/**`.

The fixed candidate, `main`, AC-RL, AC-PDL, Curator, Programme A, Lean, manuscript, release, tag, publication, arXiv, and DOI surfaces were not mutated. No branch was rebased, force-pushed, deleted, or renamed.

This one-shot audit role terminates with the return to AC-DIR. No canonical movement follows automatically.
# AC-SCOPE-TERMINALS-01 — Heawood witness application-scope ledger

**Role:** `AffineCDC — Terminal-Exhaustion Scope Auditor` (`AC-TE-AUD`)  
**Controlling negative digest:** `d24c63ab56320803b9c795ac08389674b6f67edf89dc5c4ef9a729c5436a3e61`

## 1. Purpose

The frozen corpus contains three logically different Heawood uses which must not be conflated:

1. the #78 genuine co-root same-arc witness against arbitrary-equal-face `Xi` totality;
2. the #81 three-cut/six-terminal witness against the broad component-chain theorem;
3. the explicit zero-parent `H_35` escape model.

This ledger determines which are genuine application carriers for the present terminal-exhaustion question and which are only rooted-carrier counterexamples or positive finite models.

## 2. Common stable Heawood root state

The fourteen-vertex root-valued graph has stable edges and roots

```text
1-2:23    1-6:12    1-14:13
2-3:34    2-11:24   3-4:13
3-8:14    4-5:12    4-13:23
5-6:13    5-10:23   6-7:23
7-8:24    7-12:34   8-9:12
9-10:13   9-14:23   10-11:12
11-12:14  12-13:13  13-14:12.
```

Its stable-state digest is

`4bfadd66ae43ed73c0cb339524884d295264b5468794406412770788993c799d`.

This is a complete connected simple cubic root-valued graph.  Different witnesses arise only after selecting different physical marked data or different cut/deletion operations.

## 3. #78 co-root same-arc witness

### 3.1 Genuine application data

Fix

```text
h=14,
marked edge p=1-6:12,
marked edge q=2-3:34.
```

The marked roots `12,34` are disjoint and both belong to `H_14`.  They lie on the common Hamiltonian channel cycle

```text
1-6-5-4-3-2-11-10-9-8-7-12-13-14-1.
```

This is a genuine complete co-root application state.

### 3.2 What #78 refutes

At remote equal face `9-14:23`, both endpoint triangles are `123`.  The root branch swap preserves one common marked component and the route

```text
(1,2)|(3,6).
```

Thus #78 permanently refutes:

```text
arbitrary equal bad face
=> route change or one-sided switch component.
```

It does not refute the present terminal census.

### 3.3 Terminal census in the genuine application cut

Cut only the two marked edges `1-6` and `2-3`.  The Hamiltonian `H_14` cycle is cut at two nonadjacent edges and becomes exactly two marked terminal paths with four endpoint darts.  No third channel edge is cut.

Consequently the #78 witness is:

- a genuine co-root application carrier;
- a negative witness against `Xi` source selection;
- a positive instance of exact-two-terminal-path application geometry;
- not a proof of the universal application census.

The later local `6-7:23` movie is a valid route-changing connector between those two arcs, but it likewise does not prove universality by itself.

## 4. #81 third-terminal-path witness

### 4.1 Exact rooted carrier

Fix `h=14` and cut exactly

```text
1-14:13,
10-11:12,
13-14:12.
```

The six ordered terminal darts form three channel paths:

```text
P_0:
  vertices {1,2,3,4,5,6,11}
  terminals 1-14@1, 10-11@11

X_1:
  vertices {7,8,9,10,12,13}
  terminals 10-11@10, 13-14@13

P_1:
  vertex {14}
  terminals 1-14@14, 13-14@14.
```

A shortest quotient chain is

```text
P_0 -- 3-8:14 -- X_1 -- 9-14:23 -- P_1.
```

The first root NNI

```text
134+124 -> 234+123,
central 14 -> 23,
retain 2-3@3 and 8-9@8,
move 7-8@8 -> 3 and 3-4@3 -> 8
```

splits the two old `P_0` terminal darts.  The exhaustive certificate gives

```text
18 non-cut central edges
36 labelled root-NNI movies
36 category-safe outputs
0 inherited length-one chains for the original P_0,P_1 terminal pairs.
```

Canonical digest:

`d24c63ab56320803b9c795ac08389674b6f67edf89dc5c4ef9a729c5436a3e61`.

### 4.2 Exact relation to the co-root application

The #81 rooted carrier violates the genuine co-root application hypotheses before the component chain is invoked:

1. **wrong cut cardinality:** three channel edges are cut, whereas the application cuts exactly the two marked edges;
2. **wrong terminal cardinality:** six channel terminal incidences occur, whereas the complete application operation creates exactly four;
3. **wrong marked-root datum:** among `13,12,12` there is no pair of distinct disjoint marked parent roots;
4. **unlicensed extra boundary:** the third cut cannot come from the cap, exterior, route/profile or stored prefix, because the genuine complete state is physically closed in those fields.

Therefore #81 is not a co-root application-scope counterexample.

### 4.3 Exact relation to the zero-parent application

The #81 rooted carrier also violates the genuine zero-parent boundary certificate:

1. a zero-parent deletion removes two adjacent active vertices joined by a nonchannel central edge;
2. it exposes exactly four channel branch semiedges, two at each deleted vertex;
3. its ordered root word is support-equivalent to `(13,13,23,23)`;
4. the #81 witness instead cuts three remote channel edges and exposes six terminal darts.

More concretely, two #81 cut edges meet vertex `14`, while the third is `10-11`.  If vertex `14` were deleted, its selected `H_14` incidences would be `1-14` and `13-14`.  Deleting vertex `10` to obtain `10-11` also exposes `9-10`; deleting vertex `11` also exposes `2-11`.  Thus no two-vertex active-cell deletion has exactly the #81 cut set.

A support permutation changes labels but not this incidence structure or the number of exposed channel semiedges.  Hence no genuine zero-parent complete-state embedding exists.

### 4.4 Controlling status

The #81 witness remains permanent and controlling for the statement:

```text
two distinguished terminal paths in an arbitrary rooted carrier
=> every internal quotient node is an inactive singleton or closed cycle.
```

It is excluded only by the two independently proved application censuses.  It must remain in every future application-scoped addendum as a quantifier guard.

## 5. Explicit zero-parent `H_35` model

### 5.1 Genuine application data

Use active edge

```text
4-5:12
```

with ordered branches

```text
A=3-4:13,
B=5-6:13,
C=4-13:23,
D=5-10:23,
h=35.
```

The active central root `12` is outside `H_35`; all four branches lie in `H_35`.  Deleting the active two-vertex cell therefore exposes exactly four ordered channel terminal incidences.

The retained cap is the physical block on vertices `7,12` with cap edge `7-12:34` and ordered cap darts.  It is internal complete-state data, not an extra terminal boundary.

### 5.2 Finite escape movie

The verified model performs:

```text
root branch swap at 13-14
-> H_35 components
   Z_0=1-2-3-4-13-1
   Z_1=5-6-7-12-14-9-10-5
-> switch Z_0 by 35
-> active word (15,13,25,23)
-> literal AB|CD parent NNI with central root 35.
```

It verifies one exact source movie and all cap/dart/category fields.  It does not establish the universal zero-parent terminal census; that census is proved separately from the closed complete channel and the exact active-cell boundary.

## 6. Side-by-side scope table

| Heawood datum | Physical operation | Channel terminals | Terminal paths | Application status |
|---|---|---:|---:|---|
| #78 co-root | cut the two genuine marked `H_14` edges | 4 | 2 | genuine co-root application; negative only for arbitrary-equal-face `Xi` |
| #81 chain witness | cut three selected `H_14` edges | 6 | 3 | counterexample to broad rooted-carrier theorem; not either genuine application |
| zero-parent model | delete active two-vertex cell at `4-5:12` | 4 | 2 | genuine zero-parent application model; positive finite movie only |

## 7. Application-counterexample search result

No complete labelled Heawood application-scope counterexample was found or inherited from the frozen authorities.

- #78 satisfies co-root scope and has exactly two terminal paths.
- #81 has the required third path only by introducing a third cut absent from both complete application operations.
- the zero-parent model has exactly the four active boundary terminals and two channel paths.

Accordingly no `APPLICATION-SCOPE COUNTEREXAMPLE` certificate is attached.  The permanent #81 certificate is preserved unchanged as a counterexample to the failed general theorem.

## 8. Ledger verdict

```text
#78 same-arc witness:
  genuine co-root state / terminal exhaustion passes / Xi totality remains false

#81 third-terminal witness:
  general rooted-carrier counterexample / co-root embedding no / zero-parent embedding no

zero-parent H_35 model:
  genuine zero-parent model / terminal exhaustion passes / universality not inferred from model
```

No source witness, digest or negative classification has been altered.
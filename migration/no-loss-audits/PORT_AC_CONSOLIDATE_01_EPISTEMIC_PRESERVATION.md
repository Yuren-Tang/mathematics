# PORT-AC-CONSOLIDATE-01 epistemic preservation and non-upgrade audit

## 1. Scope

This audit covers the six frozen sources in `sources/manifests/PORT_AC_CONSOLIDATE_01_SOURCE_MANIFEST.json` and the 33 units in `registry/units/graph-theory/cycle-covers/PORT_AC_CONSOLIDATE_01_UNITS.json`.

It checks preservation of negative, failed, withdrawn, heuristic, experiment/computation, rough/authorial, conditional, and open material. It does not independently re-prove any mathematical claim.

## 2. Permanent negative evidence retained

| Unit | Exact scope retained | Non-overgeneralization |
|---|---|---|
| `disjoint-double-atom-obstruction` | refutes the broad one-atom disjoint scheduler/input class displayed by the certificate | does not refute five-CDC; genuine single-pop image membership remains open |
| `component-chain-third-terminal-counterexample` | refutes the broad rooted-carrier inherited-chain theorem with a third terminal path | does not refute the exact-two-terminal-path theorem or five-CDC |
| `xi-same-arc-counterexample` | refutes arbitrary-equal-face / matching-only Xi totality | does not refute the Xi frame, invariant, six strict rows, bad-free contradiction, or a different co-root return |

All three witnesses retain their exact source SHA, file pointer, scope, and recovery ref.

## 3. Failed and withdrawn material retained

- `one-atom-prefix-scheduler`: retained as a tested/refuted-in-scope heuristic; the withdrawn v1 READY classification remains recoverable in RL branch history.
- `component-chain-broad-rooted-carrier`: retained as a refuted-in-scope claim; it is not rewritten into the valid specialization.
- `xi-arbitrary-equal-face-totality`: retained as a refuted-in-scope claim; verified local Xi subtheorems are separate units.
- `v741-proof-candidate`: retained as an assessed, scope-split authorial proof candidate; the full audit rejects end-to-end completeness while preserving positive components.
- `xi-same-arc-escape-question`: retained as a dormant historical discovery question, superseded as an active route but not deleted.

No failed object is represented as an accepted theorem, and no negative result is deleted because a narrower theorem survives.

## 4. Heuristic, computation, experiment, and rough-result preservation

### Heuristic

`one-atom-prefix-scheduler` is explicitly typed `heuristic`, maturity `assessed`, disposition `refuted-in-scope`.

### Computation and finite reproduction

- `component-chain-local-tables` preserves inactive/active rows, connector inheritance, and 84 ordered final-matching computations.
- `single-pop-algebra` preserves the independently reproduced `180=120+60` borrowing partition.
- the double-atom and third-terminal certificates retain their exact digests and finite enumeration scopes.
- explicit `6-7` and `H_35` models are typed `example`, not universal proof.

### Rough/authorial results

- the complete post-pop state interface retains authorial provenance and independent qualification where available;
- the v7.4.1 proof remains authorial in origin even where individual downstream units have separate independent support;
- open source-image and second-failure alternatives are typed question/problem, not theorem.

## 5. Conditional mathematics retained without premise flattening

- `one-atom-conditional-disjoint-commutation` requires the literal predecessor to be independently root-valued under returned labels.
- `component-chain-exact-two-terminal-path` requires exactly two terminal paths and closed remaining components.
- `conditional-singular-parent-moves` requires a complete root-valued prescribed-parent endpoint.
- `general-multigraph-outer-shell` is a conditional suffix from a cubic five-support theorem.

Each condition appears in the unit summary, evidence scope, logical relations, and `frontier.five-cdc` view.

## 6. Assurance non-upgrade checks

Passed checks:

1. author-complete PDL material is not labelled independently verified as a whole;
2. finite computation is not labelled proof outside its enumerated domain;
3. independent audit support is attached only to the exact audited unit/scope;
4. audit rejection of a proof is not converted into rejection of the conjecture;
5. local counterexamples do not erase weaker surviving theorems;
6. Curator-mapped logical relations are distinguished from source-asserted relations;
7. discovery relations and addendum seeds are not traversable as proof dependencies;
8. new observations are `captured/unreviewed`;
9. workflow states do not alter truth or assurance;
10. integration status does not imply canonical acceptance.

## 7. Form coverage

The registry contains and preserves:

- theorem and conditional theorem units;
- one open conjecture;
- open problem and question units;
- a scope-split proof candidate;
- refuted claims;
- counterexamples;
- a heuristic;
- computations;
- examples;
- a definition/interface;
- methods.

No source category required by the START packet is absent.

## 8. No-loss result

The six packet maps classify `80/80` substantive items. Aggregate result:

```text
source packets:       6 / 6 classified
substantive items:   80 / 80 classified
canonical units:     33
unclassified:         0
```

Existing project paths and source branches remain intact as recovery evidence. This additive registry does not authorize their deletion, relocation, or retirement.

## 9. Verdict

`PASS — SOURCE MATERIAL RETAINED / EPISTEMIC STRENGTH NOT UPGRADED / INDEPENDENT AUDIT STILL REQUIRED`.

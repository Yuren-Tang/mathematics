# AC-CORPUS-V2 final integration audit

## 1. Exact controls

- role: `Mathematics — Curator` (`MATH-CUR`);
- controlling issue: `Yuren-Tang/research-workbench#62` and latest AC-DIR retarget;
- standing inbox: issue #24;
- ACKs: #62 comment `5073559172`, #24 comment `5073561089`;
- branch: `curation/affine-cdc-global-rebaseline-v2`;
- exact base: `f4d6f801fac69746ca0b2ce9351735a43c79b482`;
- source-fidelity audit: `ea8ec33d49294ac31a53f46aed7a62c7b9b81908`;
- v9 RL source: `02b37476198e9eaa2b4cd8d2a2edd76782bdcd49`;
- PDL reconstruction: `e36ba22f09e3a9fc6358f3b03615f8fde6c00d96`;
- component-chain audit: `a4f20f05532c307a3a8f9d98ac4c5162e7b8ad57`.

The final branch SHA is recorded in the issue return because a commit cannot contain itself.

## 2. Branch and path audit

Before this final file, the candidate was:

- `58` commits ahead of the exact base;
- `0` behind;
- merge base exactly `f4d6f801fac69746ca0b2ce9351735a43c79b482`;
- `29` changed paths.

This final audit adds one Curator control path only. The expected final delta is `59` commits and `30` paths.

Changed paths consist only of:

- repository/project entrypoints;
- mathematical architecture/status/provenance/supersession/frontier controls;
- three new five-support spine/frontier chapters;
- the component-chain antecedent map;
- the permanent counterexample ledger;
- source-recovery and cross-repository authority maps;
- seven external-review packet files;
- this final audit.

No `proof-development/`, `research/`, `audit/`, Lean, manuscript, release, tag, workflow, arXiv or DOI path is added or modified.

## 3. Four-tier assurance audit

### Tier I

The candidate includes only exact independently supported units:

- Programme A ordinary CDC line;
- B2/B8 source-fidelity repair and packet accounting;
- OR1 subject to D1--D6;
- R0/R1/R2.1/R2.2;
- repaired local seam/genealogy units;
- conditional general outer shell;
- `Xi` local arithmetic and counterexample;
- component-channel parity/local rows/final two-path table;
- co-root and zero-parent explicit models;
- the third-terminal-path counterexample.

### Tier II

The old PDL full draft, v9 RL source and PDL v7.4 reconstruction remain exact failed/authorial candidate provenance. None is represented as accepted theorem authority.

### Tier III

The general component-chain implication has returned to the active frontier as

`AC-FRONTIER-COMPONENT-CHAIN-TERMINAL-EXHAUSTION`.

The universal co-root row, zero row, inverse table, stored-prefix integration and ordinary induction remain open.

### Tier IV

Lean, manuscript, workbench, audit, release, tag, arXiv and DOI surfaces are read-only pointers.

## 4. Material-failure status switch audit

The branch initially reserved pass/repair/failure statuses for #80/#81. During intake:

- #80 returned the complete PDL candidate at `e36ba22...`;
- #81 returned `BLOCKED — MATERIAL GAP OR FALSE SCOPE` at `a4f20f05...`.

The material-failure switch was fully applied:

1. the general theorem is a failed candidate;
2. local channel rows and two explicit models remain independently supported;
3. the third-terminal-path witness is permanent Tier-I negative evidence;
4. both universal singular-fibre corollaries and end-to-end induction are open;
5. the external packet requests a repaired theorem rather than presenting the failed theorem for acceptance.

No stale `#80/#81 active` status controls the current corpus.

## 5. Exact counterexample audit

The permanent new witness is:

```text
h=14
cut edges: 1-14:13, 10-11:12, 13-14:12
P_0={1,2,3,4,5,6,11}
X_1={7,8,9,10,12,13}
P_1={14}
chain: P_0 -- 3-8:14 -- X_1 -- 9-14:23 -- P_1.
```

Here `X_1` is a third terminal path. The first NNI splits the old `P_0` terminal darts. The finite audit found:

```text
18 non-cut central edges
36 labelled root-NNI movies
36 category-safe outputs
0 inherited length-one chains for the original terminal pairs.
```

Digest:

`d24c63ab56320803b9c795ac08389674b6f67edf89dc5c4ef9a729c5436a3e61`.

This is integrated in the theorem DAG, formal status, supersession map, frontier, antecedent map, finite manifest and both negative ledgers.

## 6. Local component-channel assurance audit

Audit `a4f20f05...` independently supports:

- channel degree `0/2` and unique inactive triangle;
- all six active/inactive rows;
- twenty-four ordered inactive-continuation cases;
- six distinct active/active rows;
- nine equal-endpoint root swaps;
- eighty-four final two-path configurations, forty-two per crossed matching and zero preserving the old matching;
- co-root `6-7` model;
- zero-parent `H_35` model.

Local and aggregate digests are retained in the finite manifest. None is promoted to universal terminal-component totality.

## 7. Historical and antecedent audit

`COMPONENT_CHAIN_ANTECEDENT_MAP.md` compares strip assembly, raw insertion/support switching, marked welds, state walks, constant runs, seams, equality annuli, Pachner descent, channel quotients, old distances, inactive absorption, active contraction and route-changing connectors.

It records:

- exact files/SHAs;
- original statements;
- historical assurance;
- retention/correction/withdrawal reason;
- logical relation to v9;
- rename duplication;
- regression risk.

The failed theorem first appears at `c106d903...`, parent `d7009b74...`. No earlier exact theorem with the same quantifiers was found.

## 8. One-line spine audit

The canonical line is:

```text
definitions and root-flow semantics
→ structural reduction
→ local finite core
→ forward root-NNI history
→ actual lower target and one inverse pop
→ complete prescribed-parent state
→ root/co-root/zero inverse table
╳ component-chain terminal-exhaustion gap
→ stored-prefix return
→ ordinary induction
→ general multigraph outer shell.
```

Every arrow has theorem/source/assurance/defect/supersession/projection/consumer metadata. The conditional outer shell is not used to bridge the broken cubic arrow.

## 9. Source-fidelity and packet accounting audit

The candidate preserves:

- valid fixed-dimensional orthogonal packet role;
- separation from the source-unreconstructed arbitrary-rank tower;
- sharp `q-2` replacement;
- packet count
  $$39+10+18+10+1=78;$$
- Programme A/B1/B3--B8 states;
- dependency-local B8 assurance.

No source-fidelity repair is overwritten by the global rebaseline.

## 10. External-review packet audit

The packet under `projects/affine-cdc/external-review/` contains exactly the required seven files:

1. `ENTRYPOINT.md`;
2. `MATHEMATICAL_SPINE.md`;
3. `COMPONENT_CHAIN_REVIEW_PACKET.md`;
4. `COUNTEREXAMPLE_AND_SUPERSESSION_LEDGER.md`;
5. `FINITE_CERTIFICATE_MANIFEST.md`;
6. `QUESTIONS_FOR_EXTERNAL_REVIEWERS.md`;
7. `REPOSITORY_AND_BRANCH_AUTHORITY_MAP.md`.

It enables a first-time graph theorist to understand the theorem goal, root-flow language, assured prefix/local data, failed chain theorem, all known witnesses, finite reproduction and the exact repair questions without reading issue chronology.

## 11. Cross-repository authority audit

The corpus records exact roles for:

- mathematics source/candidate/audit refs;
- `Yuren-Tang/affine-cdc:main@ebd7098...` as partial Lean boundary;
- research-workbench issues as lifecycle controls;
- manuscripts/releases/tags/arXiv/DOI as projections.

No projection surface is allowed to upgrade theorem assurance.

## 12. No-wholesale-import and isolation audit

Only the Curator branch is written. The integration uses exact-source reading and new synthesis; it does not merge RL, PDL, audit, Lean or manuscript trees.

Unchanged by Curator action:

- source-fidelity base branch and SHA;
- `mathematics:main@960c92b7ff231c78b387894149779083060a75eb`;
- RL `02b374...`;
- PDL `fee97446...` and `e36ba22...`;
- audits `ea8ec33...`, `6c20cead...`, `53be22a0...`, `a4f20f05...`;
- Lean `main@ebd7098...`;
- all manuscript, release, tag, workflow, arXiv and DOI surfaces.

No merge, rebase, squash, force-push, history rewrite, PR, branch deletion or canonical movement occurs.

## 13. Stop-condition audit

No blocker prevents the rebaseline itself.

- The failed theorem is classified honestly rather than repaired silently.
- The local independently supported content remains usable.
- The precise new frontier is stated.
- All historical, branch and assurance lines are recoverable.
- The external packet is immediately usable for outside assistance.

The mathematical blocker applies to the five-support theorem, not to corpus integration.

## 14. Final classification

`CURATOR-INTEGRATED GLOBAL MATHEMATICAL REBASELINE / MIXED ASSURANCE EXPLICIT / READY FOR INDEPENDENT INTEGRATION AUDIT`.

More precisely:

`GENERAL COMPONENT-CHAIN THEOREM FAILED INDEPENDENT AUDIT / LOCAL ROWS AND MODELS RETAINED / TERMINAL-EXHAUSTION INTERFACE ACTIVE / GLOBAL FIVE-SUPPORT THEOREM OPEN`.
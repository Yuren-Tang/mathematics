# Programme B2 final curation audit — historical record with source-fidelity supersession

## 1. Historical audit retained

The original B2 curation audit records the integration based on `4d7b9c74...`, source checkpoint `d629747...`, splice `9799d8a...`, and candidate `08b662...`. Its audits of graph-level witnesses, fixed singular/Schur data, stress/Fourier scope, $q-2$ lower bound, deleted permutation module, B3+ exclusion, and repository isolation remain valid.

## 2. Superseded findings

The original §§7–8 findings are superseded insofar as they:

- classified `FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md` in toto as a false theorem/history-only packet;
- attributed the arbitrary-rank tower to that packet;
- partitioned the packet population as $38+10+18+10+1+1=78$.

## 3. Corrected source finding

The packet at blob `2043ada9d28789ecc5f4f0028e62133f37835bc1` is theorem-level historical provenance for the fixed rank-three/eight-support $O^+(6,2)$ model and five-coordinate $O^-(4,2)$ slices. It contains no arbitrary-rank $\Gamma\oplus\Gamma^*$ / $d_h(a)$ / $O^+(2r,2)$ tower.

No exact committed source was recovered for that arbitrary-rank proposition. It is a separate non-packet item:

`SOURCE-UNRECONSTRUCTED / INFERRED-EXTRAPOLATION OR UNCOMMITTED DRAFT / MATHEMATICALLY REFUTED BY B2.3`.

## 4. Mathematical correction retained

The original audit correctly retained:

1. the type-correctness warning for the extrapolated formula;
2. $\dim V\ge q-2$;
3. the deleted permutation module of dimension $q-2$;
4. impossibility of dimension $2r$ for $r\ge4$;
5. rank-three exceptionality and exact five-support $O^-(4,2)$ geometry.

These conclusions do not depend on locating an earlier source for the extrapolation.

## 5. Corrected packet accounting

The exact seventy-eight-packet partition is

$$
39+10+18+10+1=78.
$$

The named orthogonal packet belongs among the thirty-nine theorem/mechanism packets. The unreconstructed extrapolation is outside the packet count.

## 6. Current authority

The controlling current surfaces are the source-fidelity-repaired B2 dossiers at `9ce8de5ca5b7b41e139be4c94572de7725446046`, the active B2/B8 chapters and ledgers, and `AC_CUR_B2_B8_SOURCE_FIDELITY_INTEGRATION_MAP.md`.

This historical audit remains useful for its checkpoint-specific integration evidence, but it is not current authority for source attribution or packet accounting.

## 7. Assurance

The source-fidelity replacement preserves Programme A, B1, B3–B8 mathematics, all six frontier obligations, B9 exclusion, and the open global five-support theorem. It requires a separately bounded independent re-audit and authorizes no canonical, Lean, manuscript, publication, release, arXiv, DOI, or tag action.
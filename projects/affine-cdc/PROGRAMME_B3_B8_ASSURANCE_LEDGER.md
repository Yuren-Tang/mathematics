# Programme B3–B8 finite-certificate assurance ledger

## 1. Assurance classes

Every finite result used in the B3–B8 corpus has one of the following statuses.

| Class | Meaning |
|---|---|
| `F-PROVED` | independent mathematical proof in the active theorem spine; computation is corroboration only |
| `F-CERT-PUBLIC` | explicit public object, table, or dependency-free enumeration description is available in principle for checking; no rerun asserted |
| `F-CERT-PRIVATE` | exact statement and refs are public, but supporting code/notebooks remain private or on recovery branches |
| `F-CENSUS` | named finite laboratory count used for calibration, not as a premise of a universal theorem |
| `CODE-PARTIAL` | recovered combined verification is incomplete or contains an unavailable assertion; no theorem depends solely on it |
| `AFFECTED` | finite data may remain correct, but a former downstream inference has been invalidated or narrowed |

Curator inclusion does not upgrade any class.

## 2. `F-PROVED` structures

Independent proofs in the integrated A/B spine include:

- ten four-terminal support-unordered states and `640` ordered assignments;
- cap state sets, routing weights, and the fixed-route graph $P_3\sqcup C_4\sqcup P_3$;
- half-cube clique number, exact common-link table, and dominating-clique capacity;
- $K_8-C_5\not\to\mathscr A_5$;
- ten roots and five co-roots in $H_5$;
- the $K_6$ triangle/one-factor classification;
- DDD atom triality and unique bad route;
- rank-one impossibility and rank-two Tait structure;
- local root-admissibility exclusion sizes `0,1,1,6`;
- additive-root lower bound $q-2$, deleted permutation module, rank-three exceptionality, and the exact five-support $O^-(4,2)$ root model.

## 3. Finite inputs to conditional theorem statements

Routing kernels and policy tables remain `F-CERT-PRIVATE`; they do not prove source path realizability. BBD monodromies remain `F-CERT-PRIVATE / AFFECTED` only with respect to groupoid-closure and simultaneous-origin inferences. The locked challenge quotient and rank-two root sections remain `F-CERT-PUBLIC` named finite premises.

## 4. Laboratory classes

- fixed-flow cube: `F-CERT-PUBLIC`, one fixed-flow obstruction only;
- thirty-vertex laboratory: `F-CENSUS / F-CERT-PRIVATE`; `7737` composite endpoints and `2801` connected one-step neighbours remain distinct;
- Petersen: `F-CERT-PRIVATE`, exact finite classification only;
- flower $J_5$: `F-CERT-PUBLIC + F-CENSUS`, theorem-level only conditional on displayed dual subgraphs;
- small four-poles: `F-CERT-PRIVATE`, finite lower bound but no universal full-cap theorem.

## 5. Correction propagation

### B3 matching display

Orbit sizes `28,168,224` remain valid. The all-parallel representative is $\{01,23,45\}$; the old display is `AFFECTED` only as a representative.

### B4 adjacency scope

`7737` is a composite-endpoint population; `2801` is a connected-support one-edge-neighbour population.

### B6 BBD closure and variation domain

BBD monodromy tables remain valid but do not imply physical groupoid closure or simultaneous origin. Local defect, $K_6$, and Petersen tables do not establish a nontrivial defect-minimal forest on the vacuous fixed-terminal variation domain.

### B2 orthogonal source fidelity

The packet `FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md`, blob `2043ada9d28789ecc5f4f0028e62133f37835bc1`, is

`THEOREM-LEVEL HISTORICAL SOURCE / EXACT RANK-THREE-EIGHT-SUPPORT AND FIVE-SLICE SCOPE`.

Its fixed-dimensional results and finite checks remain usable in their proved or certificate classes.

The arbitrary-rank $\Gamma\oplus\Gamma^*$ / $d_h(a)$ / $O^+(2r,2)$ tower is a separate non-packet item:

`SOURCE-UNRECONSTRUCTED / INFERRED-EXTRAPOLATION OR UNCOMMITTED DRAFT / MATHEMATICALLY REFUTED BY B2.3`.

It receives no packet assurance class and is not counted among the seventy-eight packets. Only deductions actually depending on that extrapolation are `AFFECTED` for the invalid inference.

The $q-2$ lower bound, deleted permutation module, rank-three exceptionality, and $O^-(4,2)$ five-support model remain `F-PROVED`.

## 6. Source-population accounting

The historical source line contains eighty-two commits and exactly seventy-eight packet files. The corrected packet partition is

$$
39+10+18+10+1=78.
$$

The named orthogonal packet belongs among the thirty-nine controlling theorem/mechanism packets by its exact fixed-dimensional contents. The source-unreconstructed extrapolation is outside this partition.

Git recoverability of all seventy-eight bodies remains complete. Source-classification fidelity is corrected by the present replacement candidate and must undergo the separately bounded re-audit required by AC-DIR.

## 7. `CODE-PARTIAL` boundary and minimum metadata

The recovered combined verifier remains incomplete. No theorem may rely solely on hidden notebook state, unavailable assertions, unexplained raw counts, witnesses without edgewise validation, or a table whose consuming theorem changed scope.

A promoted finite result should record exact source/ref, object and labelling, graph/flow/lift identifier, enumeration method, raw output and symmetry convention, explicit witness where applicable, consuming theorem, assurance class, and every scope correction.

## 8. Boundary

B8 is assurance normalization. The source-fidelity correction changes attribution and packet classification only. It does not change B1, B3–B7, B9, Programme A, routing, holonomy, defect, atom, curvature, or code-partial mathematics. It is not independent mathematical review, formal verification, public-code reproducibility, manuscript approval, publication, release, arXiv, DOI, or canonical movement.
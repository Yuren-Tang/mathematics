# Programme B3–B8 integration map

## 1. Exact controls

- authoritative intake: `Yuren-Tang/research-workbench#24`, comment `5019639996`;
- integration base: `curation/affine-cdc-programme-b2-v1@08b662ecc31848456b9904f4d5b85c3801d090a1`;
- writable branch: `curation/affine-cdc-programme-b3-b8-v1`;
- exact source splice: `4dadc7add4743d7da63e28d93806c8d76529c07b`.

The source splice has first parent the B2 candidate and six additional parents, one for each immutable unit:

| Unit | Immutable checkpoint | Internal state retained |
|---|---|---|
| B3 | `d806168bb579dbc13f267f44f631e07de909b706` | `READY-FOR-CURATOR / PROVENANCE-REPAIRED` |
| B4 | `345074690b7a8658c1208ae84f10d709f8b74bcf` | `READY-FOR-CURATOR / FRONTIER-EXPLICIT` |
| B5 | `274970ef9c56cafdbfceeed3c0cc08238d3dfd40` | `READY-FOR-CURATOR / FRONTIER-LOCALIZED` |
| B6 | `404f7511f16d1225e066a91842a57e2084943c72` | `READY-FOR-CURATOR / MATHEMATICAL-CORRECTION / AC-RL-GAPS-EXPLICIT` |
| B7 | `164f7655f9ec7c0e0a73d49303cf66230fb26487` | `READY-FOR-CURATOR / AC-RL-GAPS-EXPLICIT / GLOBAL-COMPOSITION-OPEN` |
| B8 | `989cb002598fd91786029be201c2747c697bb476` | `READY-FOR-CURATOR / ASSURANCE-NORMALIZED` |

Routing handoffs are control documents and were not copied into the candidate tree. The tree imports twenty-one exact B3–B8 dossiers and the B8 checkpoint `OBLIGATION_DAG.md`.

## 2. B3 — target and scope

### Active destinations

- `five-support/surfaces-and-halfcube.md`;
- target portions of `five-support/equivalent-formulations-and-proof-families.md`;
- provenance and supersession controls.

### Integrated theorem layer

- quadratic common-link formula;
- exact half-cube link table;
- dominating-clique capacity for ranks two through five;
- exact eight-vertex criterion using forbidden `K_6` and `K_8-C_5`;
- factorable compression through `J_g`;
- unused-matching sufficient route;
- factorable two-apex/pentagon core and ideal-pivot depth;
- conditional full-dual flower obstruction once its displayed dual certificate is accepted.

### Correction retained

The all-parallel matching representative is `\{01,23,45\}`, not `\{05,14,23\}`. The orbit theorem and sizes `28,168,224` remain valid.

### Scope boundary

`T_g^{(1)}\to\mathscr A_5` is componentwise same-embedding compression. `J_g\to\mathscr A_5` is only old-colour-factorable compression. Factorable failure is not full-dual failure.

## 3. B4 — exact two-level motion

### Active destination

- `five-support/gauge-and-reconfiguration.md`.

### Integrated theorem layer

- labelled root lifts above fixed `f` form a torsor under `H_f^0`;
- the reduced gauge code is `H_f^0/\Gamma^{\pi_0(G)}`;
- gauge bits are exactly accessible face-side swaps / partial Petrials;
- one horizontal reconfiguration edge is a constant switch on one connected source cycle;
- a disconnected cycle word gives a commuting path of connected moves;
- support-cycle pivots are special connected switches with an explicit root-level lift and fixed uncoloured embedding;
- exact used/unused-root update;
- the new gauge fibre must be recomputed after switching;
- internal fixed-plane correction is a fixed-quotient Schur/component-incidence-code problem;
- external correction is quotient reslicing through even supports containing the fixed zero matching.

### Scope boundary

`7737` counts all admissible composite endpoints in the named laboratory. `2801` counts connected-support one-edge neighbours. Linear correction images classify composite same-direction reachability, not one-step adjacency.

## 4. B5 — separators and interfaces

### Active destination

- `five-support/cuts-four-poles-and-routing.md`.

### Integrated theorem layer

- cubic local support law;
- cyclic three-cut gluing;
- ten four-pole states and `640` ordered boundary assignments;
- exact four-cut profile intersection;
- cap forcing;
- Kempe terminal-pairing alignment;
- routing weights and `P_3\sqcup C_4\sqcup P_3` fixed-route sectors;
- connectivity under two routing colours;
- uniform-routing elimination.

### Finite/certificate layer

The four residual mismatch kernels, deterministic policies, Type T/Type H orbit classification, monodromy tables, and small four-pole census remain finite results. Cap forcing is not full-cap containment, and abstract state transitions do not prove source path-pair realizability.

## 5. B6 — holonomy correction and atoms

### Active destinations

- `five-support/holonomy-defects-and-atoms.md`;
- `five-support/frontier-localisation.md`;
- supersession and assurance controls.

### Unconditional theorem layer retained

- individual-loop affine holonomy and cyclic norm;
- root-fibre local system and genuine three-path switch-flow;
- root lifting iff Tait resolution;
- strict Type H escape in the soluble zero-norm branch;
- DDD atom/one-factor bijection and triality;
- unique bad route;
- locked quotient conditional on its exact finite challenge table;
- local `K_6` and Petersen transport geometry when a defect strip is independently supplied.

### Mandatory correction

The BBD physical-loop closure, translation-kernel vanishing, and simultaneous-origin chain are conditional on `AC-RL-BBD-GROUPOID-CLOSURE`.

The stated fixed-terminal variation space contains the original root cover as a zero-defect competitor. Consequently the nontrivial defect-minimal forest is removed from the active theorem layer pending `AC-RL-BBD-VARIATION-SLICE`.

## 6. B7 — rank, curvature, and open localisation

### Active destinations

- `five-support/holonomy-defects-and-atoms.md`;
- `five-support/frontier-localisation.md`.

### Integrated theorem layer

- rank one is impossible;
- rank two is a Tait sector and has a root-triangle section giving Type H escape;
- full rank has a flat/nonflat curvature dichotomy;
- flatness is equivalent to an eight-state affine potential with the exact edge law;
- nonflatness is equivalent to one common terminal-colour edge set that is a cut in all four scalar sheets;
- the terminal cut word is odd and nonzero in `\mathbf F_2^4/E_4`.

### Open localisation layer

The scalar-sheet cut is not yet a source-graph cut, need not be bounded or connected, and has no proved composition-safe factor. Finite potential range is not a bounded-interface theorem. Type T serialisation and the physical identification of the Type H witness remain open.

## 7. B8 — certificate assurance

### Active destinations

- `five-support/finite-laboratories-and-certificates.md`;
- `PROGRAMME_B3_B8_ASSURANCE_LEDGER.md`.

The controlling assurance classes are:

- `F-PROVED`;
- `F-CERT-PUBLIC`;
- `F-CERT-PRIVATE`;
- `F-CENSUS`;
- `CODE-PARTIAL`;
- `AFFECTED`.

B8 is an assurance normalization, not a mathematical independent review and not a reproducibility upgrade.

## 8. Exact open returns

The candidate retains six exact AC-RL obligations:

1. `AC-RL-BBD-GROUPOID-CLOSURE`;
2. `AC-RL-BBD-VARIATION-SLICE`;
3. `AC-RL-TYPE-T-SERIALISATION`;
4. `AC-RL-FLAT-POTENTIAL-INTERFACE`;
5. `AC-RL-COMMON-CUT-LOCALISATION`;
6. `AC-RL-TYPE-H-COMMON-WITNESS`.

They are linked to issue #8 comments `5019401145` and `5019455976`. Curation does not solve, merge, or weaken them.

## 9. Exclusions

This intake consumes no B9 or later moving-branch delta. It does not move `main`, alter fixed Programme A/B1/B2 candidates, change independent-review or Lean status, edit a manuscript, authorize publication, create a release, or mutate arXiv/DOI records.
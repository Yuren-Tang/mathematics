# AffineCDC supersession and correction ledger

Supersession changes current control without erasing valid subtheorems, certificates, failed candidates or priority history.

## 1. Stable controls

Programme A repairs, B1--B8 source-fidelity distinctions, B3--B8 correction boundaries, packet accounting `39+10+18+10+1=78`, and OR1 D1--D6 remain controlling.

## 2. Old full-draft supersession

PDL `1f57422...` is not an active complete proof. Its independently accepted local units and conditional outer shell survive; R2.3/R2.4, old rank and cap assembly do not.

## 3. `Xi` supersession

Arbitrary equal-face totality remains refuted by audit `53be22a0...`. Frame/invariance/six strict rows remain valid.

## 4. v9 general component-chain supersession

RL theorem `02b374...` and PDL reconstruction `e36ba22...` are superseded as controlling universal theorems by independent audit `a4f20f05...`.

### Refuted scope

Two distinguished terminal paths were treated as if every internal chain node were inactive or closed. The audit found a third internal terminal path. The first contraction split the original distinguished terminal darts and destroyed inherited-chain descent.

### Retained

- channel parity and unique inactive triangle;
- all local inactive/active and active/active root rows;
- equal-endpoint root branch alternatives;
- final two-terminal matching table;
- explicit co-root and zero-parent models.

### Removed from active theorem status

- general component-chain theorem;
- universal co-root chain corollary;
- universal zero-parent chain corollary;
- complete inverse-table totality;
- ordinary-induction closure.

### Replacement frontier

`AC-FRONTIER-COMPONENT-CHAIN-TERMINAL-EXHAUSTION`.

A replacement must prove application-specific exactly-two-terminal geometry, handle internal terminal paths, or supply another source-faithful return.

## 5. Permanent negative routes

Noncontrolling:

- same-boundary parent reinsertion;
- arbitrary terminal recolouring;
- generic exit flags;
- SCC/no-sink ranks;
- exterior-orbit `Omega` control;
- arbitrary-equal-face `Xi` totality;
- zero-parent omission;
- track erasure as progress;
- generic connectivity;
- global C6/C8 simultaneous-state use;
- generic marked-weld return;
- two distinguished terminal paths treated as exhaustive without proof.

## 6. Assurance non-supersession

A complete PDL reconstruction does not override a later independent counterexample. A failed universal theorem does not invalidate independently checked local rows. Curator integration creates no Lean, manuscript, release, publication, arXiv, DOI or `main` status.
# AffineCDC supersession and correction ledger

Supersession changes current control; it does not erase valid lemmas, finite certificates, discovery history, or priority records.

## 1. Programme A Audit A repairs

### Seymour convention

Earlier compressed wording cited Seymour for a generic graph with no isthmus.

Controlling statement: apply the six-flow theorem to each finite connected loopless multigraph component, with parallel edge objects permitted and no isthmus. Isolated vertices and the empty graph are inert; loops have already been removed.

This is an explicit source convention, not a new hypothesis.

### A4 reverse local-family classification

Earlier active prose compressed the converse from an arbitrary even triple of affine lines to a deleted-point family.

Controlling statement: pairwise intersections $x_{12},x_{13},x_{23}$ are forced by parity and distinct directions; with $m=x_{12}+h_3$, the three lines are uniquely $\Phi_v(m)$.

This completes exposition and changes no theorem.

### Old Tutte route

The derivation in `reduction/outer-shell-and-binary-flow.md`, §§4.1–4.2, is superseded as a controlling proof route.

It remains historical provenance only. The controlling A3 route proves modular reduction and equal-order transport internally. Tutte is not a logical premise of Programme A.

All three items are `CLOSED / EXPOSITORY-REPAIR / NO-THEOREM-CHANGE`.

## 2. Full dual versus old-colour quotient

Controlling statement:

$$
T_g^{(1)}\to\mathscr A_5
$$

classifies componentwise same-embedding compression. $J_g$ is only the old-colour-factorable quotient. Factorable failure is not full-dual failure.

## 3. Universal orthogonal-root theorem

`FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md` remains

`RETIRED / SUPERSEDED / FALSE THEOREM / HISTORICAL PROVENANCE`.

It is replaced by the $q-2$ dimension lower bound, deleted permutation module, exceptional eight-support $O^+(6,2)$ model, and denial of a universal $O^+(2r,2)$ tower.

## 4. B3 provenance and matching repairs

Early B3 allegations about noncontrolling packet names and a positive $K_8-C_5$ certificate are withdrawn. The actual link, capacity, and eight-vertex packets remain valid.

The matching orbit sizes $28,168,224$ remain valid. The all-parallel representative $\{05,14,23\}$ is replaced by $\{01,23,45\}$.

## 5. Factorable terminology

Unused-root, two-apex/pentagon, defect-cover-depth, and ideal-pivot claims are controlled as old-colour-factorable statements. Ideal target escape is not source-cycle realizability.

## 6. Switch and fibre corrections

- connected support: one horizontal edge;
- disconnected support: a commuting path;
- `7737`: composite endpoints;
- `2801`: connected-support one-step neighbours;
- linear internal/external images describe composite endpoint sets, not automatically neighbours;
- a support pivot supplies one distinguished lift, not the whole new fibre.

## 7. Interface corrections

Cap forcing proves that each shore profile meets each cap set; it does not prove full-cap containment. Abstract Kempe transitions require source path-pairing alignment. Uniform routing is eliminated, but Type T serialization and Type H common-witness localization remain open.

## 8. BBD common-origin correction

Individual-loop ambient affine data are theorem-level. Physical root-preserving groupoid closure is not proved. Simultaneous-origin conclusions are conditional on

`AC-RL-BBD-GROUPOID-CLOSURE`.

Finite BBD monodromy generation does not supply the missing closure theorem.

## 9. Defect-forest correction

The original root cover lies in the stated fixed-terminal variation domain and has zero defect. The claimed nontrivial defect-minimal forest is therefore not an active theorem. A replacement requires

`AC-RL-BBD-VARIATION-SLICE`.

Local zero/co-root and Petersen transport geometry survives only when an appropriate defect strip is independently supplied.

## 10. Rank/curvature boundaries

Route lock does not imply a graph two-cut or automatic flatness.

- $\Omega=0$: eight-state affine potential;
- $\Omega\ne0$: common scalar-sheet cut with odd terminal parity.

The scalar-sheet cut is not yet a source graph cut, bounded, connected, or composition-safe. Finite potential range is not a bounded transfer theorem.

Open returns remain:

- `AC-RL-COMMON-CUT-LOCALISATION`;
- `AC-RL-FLAT-POTENTIAL-INTERFACE`;
- `AC-RL-TYPE-H-COMMON-WITNESS`;
- `AC-RL-TYPE-T-SERIALISATION`.

## 11. Finite assurance

Every finite result retains one of `F-PROVED`, `F-CERT-PUBLIC`, `F-CERT-PRIVATE`, `F-CENSUS`, `CODE-PARTIAL`, or `AFFECTED`. Packet inclusion does not upgrade reproducibility or independent review.

## 12. Assurance non-supersession

Programme A’s original theorem spine has passed Audit A; the repaired prose has not been separately re-audited. B1–B8 remain pending Audit B. None of these corrections changes Lean, manuscript, peer-review, publication, release, arXiv, DOI, novelty, timestamp, or canonical `main` status.

The global five-support theorem remains open.
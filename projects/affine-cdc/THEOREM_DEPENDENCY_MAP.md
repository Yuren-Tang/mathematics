# AffineCDC theorem and obligation dependency map

## 1. Programme A DAG

```text
A0 foundations
├─ A1 loop deletion/reinsertion
└─ A2 cubic expansion/collapse
   └─ A3 Seymour six-flow + internal order-eight transport
      └─ A4 affine pair complex
         └─ A5 rank-three compatibility
            └─ A6 graph/dart support extraction
A0 + A6 → A7 parity bridge
A2 + A6 + A7 → A8 collapse
A0 + A8 → A9 circuit decomposition
A1–A9 → A10 complete CDC theorem.
```

Programme A and Audit A status are unchanged.

## 2. B1/B2 graph-level witnesses

```text
five supports
↔ R₅ flow
↔ K₅ triangles
↔ exact matching/four-flow data
↔ anisotropic O⁻(4,2) flow
↔ quadratic cycle solution
↔ cographic cycle-continuous edge map.
```

Fixed singular/Schur branches require the quotient flow, kernel line or plane, quotient graph, incidence solve, and lift torsor. Stress is a dual solvability criterion and Fourier is an exact fixed-orbit count.

## 3. Orthogonal source/proposition separation

### Exact packet node

```text
FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md
→ H₈ + Hamming kernel/moment map
→ O⁺(6,2) + 28 roots
→ rank-three/eight-support root lifts
→ five-coordinate O⁻(4,2) slices.
```

This is theorem-level historical provenance in exact fixed-dimensional scope.

### Separate correction node

```text
source-unreconstructed arbitrary-rank tower
→ triangle addition forces point potentials
→ Gram rank q−2
→ dim V ≥ q−2
→ deleted permutation module attains q−2
→ rank three exceptional; no universal O⁺(2r,2) tower.
```

The first node does not imply the second proposition’s antecedent. The unreconstructed tower is not a packet node.

## 4. B3 target DAG

```text
fixed compatible lift g
↔ cycle-face surface
↔ full dual T_g
→ componentwise T_g^(1) → 𝒜₅
→ five-support cover.
```

The old-colour quotient $J_g$ is a narrower factorable projection.

## 5. B4 motion DAG

```text
fixed f
→ H_f^0 torsor
→ reduced gauge code
→ partial Petrials.

connected source cycle + direction
→ one horizontal switch
→ recompute new lift fibre.
```

Disconnected cycle words give paths, not one edges. Internal/external linear images classify composite endpoints, not automatically neighbours.

## 6. B5 interface DAG

```text
local support law
→ three-cut gluing
→ ten four-pole states
→ profile intersection
→ cap forcing
→ path-pairing alignment
→ routing weights
→ uniform-routing elimination
→ residual Type T / Type H.
```

Finite transition tables do not imply source realizability.

## 7. B6 corrected holonomy DAG

Unconditional individual-loop, path-family, root-fibre/Tait, Type H soluble escape, and DDD atom results remain active.

```text
AC-RL-BBD-GROUPOID-CLOSURE
→ physical root-preserving groupoid
→ possible simultaneous-origin argument.
```

```text
AC-RL-BBD-VARIATION-SLICE
→ nontrivial variation domain
→ possible defect-pruning theorem.
```

## 8. B7 rank/curvature DAG

```text
route-locked quotient
├─ rank 1 → impossible
├─ rank 2 → Tait/root-triangle escape
└─ full rank → curvature Ω.
```

$\Omega=0$ gives an eight-state potential; $\Omega\ne0$ gives a common scalar-sheet cut. Source-cut localization, bounded interface, Type H common witness, and Type T serialization remain open.

## 9. B8 assurance and source population

All finite inputs retain one of the six B8 classes. The packet population remains exactly seventy-eight:

$$
39+10+18+10+1=78.
$$

The named orthogonal packet is in the theorem/mechanism class. The unreconstructed extrapolation is outside the packet DAG and packet count.

## 10. Global frontier and exclusions

The six exact AC-RL obligations remain open. B9 remains excluded and blocked on localization/composition. The source-fidelity replacement does not alter Programme A, B1, B3–B8 mathematics, Lean, manuscript, publication, release, arXiv, DOI, or canonical status.
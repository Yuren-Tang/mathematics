# AffineCDC theorem dependency map

## 1. Complete CDC line

$$
\text{bridgeless finite-active graph}
\to
\text{cubic binary-flow shell}
\to
\text{rank-three affine compatibility}
\to
\text{even double cover}
\to
\text{collapse transport}
\to
\text{cycle double cover}.
$$

## 2. Five-support line

$$
\text{five indexed even supports}
\Longleftrightarrow
\text{root-valued }E_5\text{ flow}
\Longleftrightarrow
\text{$K_5$/matching--four-flow/quadratic/cographic data}.
$$

For a fixed lift, the complete target is

$$
T_g^{(1)}\to\mathscr A_5.
$$

## 3. Route-locked reduction

Gauge, cut interfaces, Type T/Type H routing, holonomy, defect forests, Petersen atoms, and unique-bad-route reduction give

$$
\begin{cases}
\operatorname{rank}=2&\Rightarrow\text{Tait escape},\\
\operatorname{rank}=3,\Omega=0&\Rightarrow\text{flat potential},\\
\operatorname{rank}=3,\Omega\ne0&\Rightarrow\text{common-cut witness}.
\end{cases}
$$

The nonflat witness is singleton, marked transition, or quartic. Quartic incidence satisfies bounded split or strict peel `k\mapsto k-1`.

## 4. Physical carriers and bounded residue

Quotient Tait/Kempe data, the physical quotient `\Gamma_g`, and

$$
F=\mathcal L(\widehat Q)
$$

give four scalar transition systems. Touch-shadow closure compresses every nonflat witness to one cross transition at each cap.

Minimal full completion is coupled or separated. Cocircuit support satisfies

$$
r(D)=|L(D)|+\rho(L(D)),
\qquad
n(D)=|L(D)|-\rho(L(D)).
$$

## 5. Natural physical response

The natural transversal has touch-graph `\widehat Q`, and

$$
\boxed{
0\to\operatorname{Cut}(\widehat Q)
\to\operatorname{Cocycle}(M_\tau(F))
\to\operatorname{Cycle}(\widehat Q)\to0.
}
$$

A zero-projection cocircuit is a physical bond.

Writing a cocycle as `(z,a,z+a)` gives a symmetric form

$$
a\cdot z'=\mathcal B(z,z').
$$

## 6. Ribbon and gauge geometry

The response form is the mod-two intersection pairing of the signed ribbon realization:

$$
\operatorname{rad}\mathcal B=\text{boundary homology},
\qquad
\operatorname{rank}\mathcal B=\text{Euler genus}.
$$

A Petrial word `\lambda` adds

$$
\sum_e\lambda_ez_ez'_e.
$$

For admissible code `C`, radicalisation of `z` fails exactly when there is a physical cycle `z'` with

$$
\boxed{
\mathcal B(z,z')=1,
\qquad
w=z\odot z'\in C^\perp.
}
$$

## 7. Hyperbolic dual-flow hierarchy

For the active Fano gauge code,

$$
C^\perp=\mathcal C(G)*\mathcal F_f.
$$

Hence

$$
\boxed{w_e=\langle u(e),f(e)\rangle}
$$

for a dual-valued flow `u:E\to\Gamma^*`. The minimum dimension of `\langle u(E)\rangle` is the dual rank.

### Rank one

$$
w=c*(\alpha\circ f).
$$

Odd intersection localises to one odd scalar circuit or one odd marked scalar interval.

### Rank two

After quotient by the annihilator colour `h`, the fixed flow and `u` are two four-flows. At fully Tait vertices the norm support is the moved-point set of a permutation in `S_3`; three-cycles are forbidden. Odd intersection localises to one closed norm circuit or one defect-to-defect path.

### Rank three

No comparable localisation is yet proved.

## 8. Exact open bridge

$$
\boxed{
\text{dual rank three hyperbolic norm carrier}
+
\text{prime coupled cap response}
+
\text{locked route}.
}
$$

The next theorem must give a physical separator, bounded handle, connected-cycle switch, or a genuine DDD support-label comparison. The already localised rank-one and rank-two carriers require composition-safe gluing/replacement.

After this bridge come the flat-potential interface, defect-forest pruning, four-pole transfer, and horizontal escape/decomposition.

No arrow automatically transfers Lean, audit, peer-review, release, DOI, arXiv, or publication status.

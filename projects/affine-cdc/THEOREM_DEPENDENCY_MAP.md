# AffineCDC theorem and obligation dependency map

## 1. Programme A complete CDC DAG

```text
A0 finite-active multigraph, cut, circuit, and occurrence semantics
├─ A1 loop deletion and exact reinsertion
└─ A2 port-cycle cubic expansion and collapse data
   └─ A3 Seymour six-flow + internal order-eight transport
      └─ A4 affine pair complex and local-family classification
         └─ A5 rank-three compatibility
            └─ A6 graph/dart indexed support extraction
A0 + A6 → A7 loopless parity bridge
A2 + A6 + A7 → A8 collapse and multiplicity transport
A0 + A8 → A9 circuit decomposition
A1–A9 → A10 complete CDC theorem.
```

### Audit A repair overlay

```text
Audit A item 1
→ A3 explicitly uses finite connected loopless multigraphs
→ parallel edges allowed
→ componentwise assembly explicit.

Audit A item 2
→ arbitrary even local line family
→ unique pairwise intersections
→ unique deleted point m
→ complete converse P = Φ_v(m).

Audit A item 3
→ old Tutte companion route historical only
→ A3 internal equal-order transport remains controlling.
```

These arrows clarify existing nodes and introduce no new theorem premise. Seymour remains the sole non-elementary external logical input.

## 2. B1–B2 foundation DAG

```text
five supports
↔ R₅ flow
↔ K₅ triangles
↔ exact matching/four-flow data
↔ ∃ Fano flow/plane with empty obstruction
↔ ∃ cycle-face embedding with full-dual map to 𝒜₅
↔ anisotropic O⁻(4,2) flow
↔ quadratic cycle solution
↔ cographic cycle-continuous edge map.
```

Fixed singular/Schur branches retain their quotient data. Stress and Fourier remain dual/counting interfaces. The false universal $2r$ orthogonal theorem is replaced by the $q-2$ lower bound and deleted permutation module.

## 3. B3 target DAG

```text
fixed lift g
↔ cycle-face surface S_g
↔ full dual T_g
→ componentwise same-embedding map T_g^(1) → 𝒜₅.
```

The old colouring gives $T_g^{(1)}\to J_g$, hence

```text
J_g → 𝒜₅
→ T_g^(1) → 𝒜₅
→ five-support cover.
```

No reverse implication to $J_g$ is automatic. Target links lead to dominating-clique capacity; the eight-colour factorable route leads to the exact $K_6/K_8-C_5$ criterion, unused matching, factorable cores, and ideal target pivots.

## 4. B4 motion DAG

```text
fixed f
→ H_f^0 torsor
→ reduced gauge code H_f^0 / Γ^{π₀(G)}
→ accessible partial Petrials.
```

```text
connected source cycle + direction
→ one horizontal switch
→ solve/construct the new lift fibre.
```

A disconnected cycle word decomposes into a commuting path. A support pivot is one special connected switch with an explicit lift and exact used/unused-root update. Internal and external formulas classify composite endpoints; connected support is an additional adjacency condition.

## 5. B5 interface DAG

```text
cubic local law
→ cyclic three-cut gluing
→ ten four-pole states / 640 ordered assignments
→ exact profile intersection
→ cap forcing
→ path-pairing alignment
→ routing weights
→ uniform-routing elimination
→ Type T or Type H residual mechanism.
```

Finite mismatch kernels and routing policies do not imply source realizability.

## 6. B6 corrected holonomy DAG

Unconditional:

```text
individual physical loop
→ affine monodromy and cyclic norm
→ root-fibre local system
→ genuine three-path switch flow
→ root lifting iff Tait resolution
→ soluble Type H escape.
```

DDD:

```text
co-root atom
→ three resolutions
→ unique bad route
→ locked quotient, conditional on finite challenge data.
```

Conditional BBD:

```text
AC-RL-BBD-GROUPOID-CLOSURE
→ physical root-preserving groupoid
→ global translation/cohomology argument
→ simultaneous common origin.
```

Any nontrivial forest replacement requires:

```text
AC-RL-BBD-VARIATION-SLICE
→ valid nontrivial variation domain
→ possible defect-pruning theorem.
```

## 7. B7 rank/curvature DAG

```text
route-locked quotient
├─ rank 1 → impossible
├─ rank 2 → Tait/root-triangle escape
└─ full rank → curvature Ω(c).
```

```text
Ω(c)=0 ↔ affine local words ↔ eight-state potential.
Ω(c)≠0 ↔ common terminal-colour support cut in all four scalar sheets with odd terminal parity.
```

Open arrows:

- `AC-RL-TYPE-T-SERIALISATION`;
- `AC-RL-FLAT-POTENTIAL-INTERFACE`;
- `AC-RL-COMMON-CUT-LOCALISATION`;
- `AC-RL-TYPE-H-COMMON-WITNESS`.

## 8. B8 assurance and B9 boundary

Every finite input retains one of `F-PROVED`, `F-CERT-PUBLIC`, `F-CERT-PRIVATE`, `F-CENSUS`, `CODE-PARTIAL`, or `AFFECTED`.

B9 remains outside this candidate and blocked at:

```text
persistent bad data
→ good escape
or
→ strict source progress and composition.
```

## 9. Assurance

Programme A’s original theorem spine passed Independent Audit A; its three non-blocking repairs are closed and integrated but not separately re-audited. B1–B8 remain pending dedicated Audit B. No end-to-end Lean, manuscript, publication, release, arXiv, DOI, novelty, timestamp, or canonical movement follows.
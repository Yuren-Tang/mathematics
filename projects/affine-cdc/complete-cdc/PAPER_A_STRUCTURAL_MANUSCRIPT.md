# Paper A as a standalone structural manuscript

## Exact status

The sole reviewed manuscript checkpoint is

`Yuren-Tang/affine-cdc-workbench:paper/paper-a-revision-v1@fae5e34a77e09e2adec632d46349340db5413a1b`.

Independent fixed-text review found no fatal or material mathematical defect. The two bounded repairs synchronized the rank-three scalar scope and added delimited structural precedent. The manuscript is therefore a reviewed standalone mathematical article candidate. It is **not** submitted, on arXiv, assigned a DOI, externally accepted, end-to-end Lean verified, or evidence of priority.

## Exact article identity

Paper A is not principally a new proof of the public CDC endpoint. Its mathematical centre is the affine obstruction--moduli system attached to a finite cubic nowhere-zero binary flow.

Let the local even affine families define the distinguished affine datum `κ` in the pair complex `P_f`. Then:

- `H^1(P_f)` contains the obstruction class `[κ]`;
- `H^0(P_f)` is the homogeneous compatible-family space;
- when nonempty, the full solution set is an affine torsor under `H^0(P_f)`;
- quotient, gauge and equilibrium-stress tests are canonical presentations of the same obstruction;
- in rank three, the edge quotients carry canonical anisotropic quadratic forms, the vertex translation spaces are Fano Lagrangians, and the local affine families are their characteristic torsors;
- the characteristic torsor meets the totally singular edge Lagrangian, so `[κ]=0`;
- the complete compatible-family moduli admit the canonical perfect pairing and self-duality

  `H^0(P_f) ≅ Stress(f) ≅ H^1(P_f)^*`.

The public cross-bit/support-parity cancellation is the scalar shadow of this quadratic mechanism, not a stronger CDC endpoint.

## Sharp scalar boundary

The repaired manuscript keeps two scopes distinct.

1. If `Γ ≅ F_2^3`, Fano polarity identifies the cross-bit with the restriction `q_W|_{L_W}` of the canonical quotient quadratic.
2. In an arbitrary finite-dimensional binary ambient space, cross-bit equals support parity for every legal configuration exactly when `dim(Γ/W) ≤ 1`.

The second statement is a codimension boundary. It must not be read as an all-rank quadratic/Fano theorem.

## Compressed ordinary CDC application

The structural theorem supplies compatible indexed even supports. The graph-theoretic shell then uses the already integrated Programme A chapters:

1. active-edge finiteness, loop separation and no-singleton-cut semantics;
2. port-cycle cubic expansion and a nowhere-zero `F_2^3` flow;
3. affine compatibility and exact double-support extraction;
4. cut-even collapse, circuit decomposition and two singleton loop occurrences.

The normalized ordinary endpoint is:

> Every multigraph with finite active edge set and no singleton cut has a circuit double cover.

For a nonloop edge, being the sole member of a cut is equivalent to being a bridge. Hence the same theorem may be stated as “finite active edge set and no nonloop bridge”. Generic “no bridges” wording is avoided because loops are allowed and are never bridge edges.

Paper A uses this as one compressed application. It claims neither a stronger CDC conclusion nor priority over the public CDC endpoint.

## Formal boundary

The exact formal anchor is

`Yuren-Tang/affine-cdc:main@ebd7098a7a8b824e8c0a511d3c71f61705672aa8`.

It checks selected local affine, quotient/gauge/dual, branching, cross-bit and indexed support constructions and a cubic-flow corollary. It does not machine-check the invariant Fano/characteristic-torsor presentation, the full collapse shell, loop deletion/reinsertion, the finite-active natural statement, or the unconditional full multigraph theorem. The manuscript must therefore not be called end-to-end Lean verified.

## Exact boundary with deferred higher-rank mathematics

Paper A contains the rank-three coincidence and a bounded higher-rank frontier remark. It does **not** contain the substantive Paper B theorems: the all-rank dual-Fano residue localization, rank-four alternating-form quotient, Pfaffian cubic or the triangular-prism/`K_{3,3}` examples.

The strongest mathematical bridge is that Paper A's obstruction `[κ]` is the same obstruction paired in Paper B's localization formula, and rank three is exactly the residue-free balanced quadratic point. This is a mathematical dependency, not a commitment to publish an A/B series.

## Canonical corpus sources

- `core/affine-incidence-and-obstruction.md`;
- `core/rank-three-fano-compatibility.md`;
- `complete-cdc/foundations-expansion-and-flow.md`;
- `complete-cdc/affine-compatibility-and-extraction.md`;
- `complete-cdc/collapse-decomposition-and-assembly.md`;
- fixed review/repair records: research-workbench #99 and #102;
- publication-architecture disposition: research-workbench #103.

## Prohibited status inferences

This corpus does not imply submission, arXiv posting, DOI assignment, journal acceptance, external specialist acceptance, complete formal verification, priority, an A/B series, or any publication action.
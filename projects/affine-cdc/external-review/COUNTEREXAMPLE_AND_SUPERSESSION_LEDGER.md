# External-review counterexample and supersession digest

The complete active ledger is `../COUNTEREXAMPLE_AND_SUPERSESSION_LEDGER.md`. This digest lists the negative results that must be checked before any global proof synthesis.

| Refuted inference | Smallest controlling witness/source | What survives | Active replacement |
|---|---|---|---|
| same exterior boundary implies literal predecessor topology | equality/good-disjoint alternative insertion; `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md@02b374...` | one pop gives predecessor-order root/one-atom state plus target field | fixed-order target normalization |
| some terminal root flow discharges inherited-flow call | equality word `(p,p,p,p)`; shell audit `a94c4021...`, K-01 | unlabelled terminal existence | literal zero-parent return from inherited state |
| generic exit flag is automatically consumed | shell audit `a94c4021...`, K-02 | named concrete consumers | exhaustive terminal ledger |
| sink SCC is horizontal/token-only | mixed root/token return; `PRE_CANCELLATION_C6...@02b374...` | local C6 charts and movies | physical complete-state rank |
| finite saturated relation has no sink | singleton sink and distance-restoring 2-cycle; audit `492eea3e...` | local macro witnesses | prove source totality before rank |
| local `Omega` shift controls exterior switch orbit | `G_14` orbit minima `20` versus `27`; `DDD_LOCK_OMEGA...SCOPE_CORRECTION@02b374...` | local coefficient rows and route contradiction | no switch-orbit energy in v9 |
| arbitrary equal face supplies `Xi` progress | Heawood same-arc face `9-14:23`; audit `53be22a0...` | frame, invariance, six strict rows | select a connector between actual marked arcs |
| co-root row completes inverse table | zero word `(13,13,23,23)` | co-root local theory | separate zero-parent chain candidate |
| track erasure itself is strict target progress | active-mark repair and old rank failure; audit `492eea3e...` | finite seams and state genealogy | use only on a fixed witnessed state walk |
| local support-switch lift gives global root-flow connectivity | `WELD_INSERTION_SUPPORT_SWITCH_LIFT_V1.md` scope | exact lift of a specified lower switch | literal source history only |
| existential lower theorem is a relative call | audits `00b4b376...`, `492eea3e...`, `a94c4021...` | lower unlabelled truth | witnessed inherited-flow proposition |
| compatible C6/C8 charts are one contextual state | physical-incidence conflict; `PRE_CANCELLATION_C6...` | coherent history-interface movie | cellwise seams or explicit source state |
| ordinary exterior preservation includes internal weld | unproved `MWR`; `MARKED_WELD_RELATIVE...@02b374...` | inverse weld if exact word retained | complete prescribed-parent state |
| fixed-dimensional orthogonal packet proves all-rank `2r` tower | source-fidelity audit `ea8ec33...` | rank-three/eight-support packet | sharp `q-2` deleted-permutation theorem |

## Review rule

A proposed proof that invokes any refuted inference must either:

1. point to an exact later theorem whose hypotheses exclude the witness; or
2. be rejected at that interface.

Calling the same inference by a new name, rank or quotient does not constitute a repair.
# Current five-support proof spine

## 1. The one-line dependency line

```text
definitions and root-flow semantics
→ structural reduction
→ local finite core
→ forward root-NNI history
→ actual lower target and one inverse pop
→ complete prescribed-parent state
→ root/co-root/zero inverse table
→ component-chain return candidate
→ stored-prefix return
→ ordinary induction
→ general multigraph outer shell
```

The line is a dependency map, not a statement that every arrow is accepted.

## 2. Arrow ledger

### S0 → S1 — definitions and root-flow semantics

**Theorem.** Five indexed even supports on a finite loopless cubic multigraph are equivalent to an $R_5$-valued root flow, with each cubic vertex carrying a support triangle and every edge occurring in exactly two indexed supports.

**Source.** Active B1/B2 chapters on base `f4d6f801...`; old PDL unit R0 at `1f57422...`.

**Assurance.** `I-VERIFIED` by `AC-5CDC-AUDIT-CORE-01@00b4b376...`, Unit A.

**Defects/supersessions.** Empty and repeated supports are occurrences, not distinct support values. File presence does not change the exact index multiplicity.

**Projection.** Partial Lean/root-flow interfaces only; no end-to-end five-support formalization.

**Consumer.** R1 structural reduction and all later root-labelled histories.

### S1 → S2 — structural reduction

**Theorem.** At a selected simple edge, the one-cross construction has at least one connected loopless bridgeless cubic closure, with exact treatment of disconnected exterior cases, loops, parallel edges and bridges; theta supplies the base object.

**Source.** `AC_PD_5CDC_R1_ONE_CROSS_STRUCTURAL_REDUCTION.md@1f57422...`.

**Assurance.** `I-VERIFIED` by core audit Unit B.

**Defects/supersessions.** This is one selected cross, not all reconnections and not a complete profile theorem.

**Projection.** Manuscript and Lean pointers remain Tier IV.

**Consumer.** The finite boundary and route core.

### S2 → S3 — local finite core

**Theorem family.** The conserved four-terminal boundary has ten support-unordered states and $640$ ordered assignments; the exact $J_i/K_i$ intersections, route rows and equality/DDD local potentials hold in their finite scope.

**Sources.** PDL R2.1/R2.2 at `1f57422...`; B5/B8 chapters on the assured base.

**Assurance.** `I-VERIFIED` by core audit Units C and D.

**Known defects.** R2.3 missing-index entrance and R2.4 physical six-port promotion were not accepted by that audit. Finite coefficient geometry does not supply source realization.

**Superseded alternatives.** Generic finite-state reachability, target-table-only routing and raw coefficient no-sink inferences are prohibited.

**Consumer.** The authorial forward-history line and all future repaired complete-state proofs.

### S3 → S4 — forward root-NNI history

**Candidate.** Equality/DDD potentials produce a finite same-order history of root-valued NNIs to route/category output or the first equal-face cancellation, together with a complete labelled history prefix.

**Sources.** `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`, `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_2_STATE_WALK_CELLWISE_ADDENDUM.md`, and v9 index at `research/...@02b374...`.

**Assurance.** `II-RL`. Local tables and some seam/genealogy interfaces have independent support, but the full arrow is not independently accepted.

**Known defects.** A coherent annular movie is not one contextual state; sink SCCs may mix root and token states. Track erasure is not target progress.

**Consumer.** The first-cancellation actual-target theorem.

### S4 → S5 — actual lower target and one inverse pop

**Candidate.** At the unique selected cancellation, solve the literal smaller target, then perform exactly one inverse insertion. The output is a predecessor-order complete state carrying a recorded target topology.

**Source.** `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md@02b374...` and its target-cap parents.

**Assurance.** `II-RL`.

**Known defect boundary.** Equality, good-disjoint and missing-index rows need not already lie on the literal predecessor topology. Same exterior boundary is not literal parent reinsertion.

**Superseded alternative.** Arbitrary lower root flows or unlabelled terminal existence may not replace the inherited labelled flow.

**Consumer.** Complete prescribed-parent state and fixed-order normalization.

### S5 → S6 — complete prescribed-parent state

**Candidate.** A state records source topology, literal target parent, ordered stable darts, active cell, support labels, cap, route/profile, graph category, marks and stored prefix. Root or one-atom normalization is performed at fixed order before the next source move is inverted.

**Source.** `PURE_NNI_PRESCRIBED_PARENT_STATE_INTERFACE_NORMAL_FORM_V1.md@02b374...` and v9 control index.

**Assurance.** `II-RL`.

**Known defects.** Every transition must preserve or explicitly update all state fields. Coefficient-equivalent endpoints are not enough.

**Consumer.** The exact inverse-parent table.

### S6 → S7 — root/co-root/zero inverse table

**Candidate.** Distinct-intersecting parent values give a literal root inverse; co-root $(4,2,2)$ and zero $(0,2,2)$ locks are reduced to exact fixed-channel return problems, with category/cap exits retained.

**Sources.** `PURE_NNI_PRESCRIBED_PARENT_PHASE_AB_CONTROLLING_CORRECTION_V2.md`, `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`, `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`, all at `02b374...`.

**Assurance.** `II-RL` for complete totality. The `Xi` strict distinct-neighbour rows are `I-VERIFIED`; arbitrary equal-face totality is `I-NEGATIVE`.

**Known defects.** Older tables omitted or did not close zero-parent. The co-root same-arc counterexample must remain active.

**Consumer.** Fixed-channel component-chain theorem.

### S7 → S8 — component-chain return candidate

**Candidate.** For a fixed support channel $H_h$, contract a shortest witnessed physical chain between two marked arcs or terminal paths by ordinary root NNIs. Inactive vertices are absorbed, intervening channel components merged, and the final connector changes the terminal matching.

**Source.** `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md@02b374...`.

**Assurance.** `II-RL / #80 PDL ACTIVE / #81 AUDIT ACTIVE`.

**Highest-risk interfaces.** Inherited connector stability, active/active local table, equal-face root branch, final matching, route/category outputs, literal darts and prefix.

**Superseded alternatives.** Arbitrary equal-face selection, $\Omega$ minima, $\Xi$ totality, generic NNI connectivity, SCC distance and recomputed abstract quotient distance.

**Consumer.** Both singular-fibre corollaries and stored-prefix return.

### S8 → S9 — stored-prefix return

**Candidate.** After the fixed-flow parent is realized or a named terminal occurs, return through the retained pure root-NNI prefix using same-order target normalization and complete state-walk seam/run coherence.

**Sources.** v7.2 state-walk package and v9 index at `02b374...`.

**Assurance.** `II-RL`. Independent audit accepted important finite seam and genealogy subunits but rejected the old global no-sink rank.

**Known defects.** A suspended mark on an active diagonal requires lineage, and the return may not reset a target rank or discard accepted exits.

**Consumer.** Ordinary induction.

### S9 → S10 — ordinary induction

**Candidate.** One selected order drop to the actual smaller target, followed by same-order return and literal prefix inversion, closes the cubic theorem by ordinary strong induction.

**Source.** `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V9_COMPONENT_CHAIN_REPAIR.md@02b374...`.

**Assurance.** `II-RL`. It is a complete authorial candidate, not an established theorem.

**Known defects.** The induction closes only if #80/#81 validate the chain theorem, both corollaries, every terminal consumer and the no-reset stored-prefix integration.

**Consumer.** General multigraph outer shell.

### S10 → S11 — general multigraph outer shell

**Conditional theorem.** A cubic five-support theorem implies the at-most-five-member cycle double cover statement for every finite bridgeless multigraph by componentwise port-cycle expansion, memberwise cut-even collapse, and loop reinsertion into two fixed supports.

**Source.** PDL outer-shell dossier at `1f57422...`.

**Assurance.** `I-VERIFIED` by shell audit Unit L at `a94c4021...`.

**Known boundary.** It is conditional on the cubic theorem. It does not repair an upstream inverse-parent or cap-consumer gap.

**Projection.** No Lean or manuscript acceptance is inferred.

## 3. Current endpoint

The dependency line is complete as an ontology. Its load-bearing middle remains mixed-assurance. The strongest global statement is therefore:

`COMPLETE RL AUTHORIAL CANDIDATE / COMPONENT-CHAIN RECONSTRUCTION AND AUDIT ACTIVE / NO INDEPENDENT FIVE-CDC ACCEPTANCE`.
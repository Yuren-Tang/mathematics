# Finite laboratories and certificate assurance

## 1. Controlling assurance classes

Every finite result in the active five-support corpus is classified as one of:

- `F-PROVED` — independently proved in the mathematical spine;
- `F-CERT-PUBLIC` — explicit public certificate or checkable finite description;
- `F-CERT-PRIVATE` — exact public statement/refs with code or notebook on private/recovery branches;
- `F-CENSUS` — named laboratory calibration, not a universal premise;
- `CODE-PARTIAL` — incomplete recovered verifier or unavailable assertion;
- `AFFECTED` — finite data retained but a former inference has been invalidated or narrowed.

See `PROGRAMME_B3_B8_ASSURANCE_LEDGER.md` and the source-fidelity repair checkpoint `9ce8de5ca5b7b41e139be4c94572de7725446046`.

## 2. Independently proved finite structures — `F-PROVED`

The active theorem spine proves, independently of private computation:

- ten four-terminal states and `640` ordered boundary assignments;
- standard cap sets;
- routing-weight coordinates and $P_3\sqcup C_4\sqcup P_3$ fixed-route sectors;
- connectivity under two routing colours;
- half-cube clique number and exact common-link table;
- dominating-clique capacity for ranks two through five;
- $K_8-C_5\not\to\mathscr A_5$;
- root/co-root counts and the $K_6$ triangle/one-factor model;
- DDD atom triality and unique bad route;
- rank-one impossibility and rank-two Tait structure;
- local root-admissibility exclusion sizes `0,1,1,6`;
- the $q-2$ additive-root lower bound and deleted permutation module;
- rank-three exceptionality and the exact five-support $O^-(4,2)$ root model.

Any computation of these objects is corroboration only.

## 3. Fixed-flow cube — `F-CERT-PUBLIC`

An explicit Fano flow has no old-colour-factorable five-coordinate slice. It proves only that one fixed flow may require more than five global support indices. It does not show that the graph lacks another successful Fano flow, a componentwise full-dual compression, or a five-support cover.

## 4. Thirty-vertex laboratory — `F-CENSUS / F-CERT-PRIVATE`

The graph is explicitly Tait-colourable. A selected flow nevertheless has bad fibres and renewal modules. Exact data include a two-state bad gauge fibre, three-cube and six-cube renewal modules, reserve-code and occurrence-locus distributions, a corrected componentwise fibre census, `7737` composite endpoints, and `2801` connected-support one-step neighbours. No count proves universal escape, trapping, or target completeness.

## 5. Petersen classification — `F-CERT-PRIVATE`

The exact finite classification records `28560` nowhere-zero Fano flows, `170` $GL(3,2)$ flow orbits, five combined graph/colour orbits, the complete reduced-fibre table, a connected cycle-switch graph, and three surface types. This is a complete finite result about Petersen, not a universal theorem or public-code reproducibility claim.

## 6. Flower $J_5$ — `F-CERT-PUBLIC + F-CENSUS`

The displayed flow and dual graphs give explicit certificate inputs for one $K_6$ obstruction and one $K_6$-free obstruction containing a dominating chromatic core. The short target no-go is theorem-level conditional on the displayed dual subgraph. The `712` and `528` neighbourhood counts remain census data.

## 7. Small four-poles — `F-CERT-PRIVATE`

The exhaustive range through six internal vertices contains thirty connected cap-admissible multigraph classes, eight complete boundary profiles, a full cap set in every realized profile, and none of the four abstract mismatch kernels. This proves the finite lower bound of eight internal vertices for a minimal-counterexample shore, not a universal full-cap theorem.

## 8. Routing and monodromy tables

### Routing kernels — `F-CERT-PRIVATE`

The four minimal mismatch kernels, unique safe routes, Type T/Type H policy-pair orbits, and physical BBD/DDD triangle profiles are finite inputs to conditional arguments. They do not prove path-pair realizability or global four-pole reduction.

### BBD monodromy — `F-CERT-PRIVATE / AFFECTED`

The sixteen BBD monodromies and cycle types remain valid individual-loop data. They do not prove physical root-preserving groupoid closure, a global translation-kernel theorem, or one simultaneous common origin.

### DDD monodromy — `F-CERT-PRIVATE`

The DDD table remains an exact finite input to the DDD atom and Tait-escape arguments in its stated scope.

## 9. Locked quotient and rank-two sections — `F-CERT-PUBLIC`

The locked $K_{2,4}$ challenge table supplies the exact finite quotient used by B6.4 and B7. The three rank-two planes through the terminal colour have explicit root-triangle sections. Once these tables are supplied, the consuming Tait/root arguments are theorem-level.

## 10. Matching-orbit correction

The affine matching orbit sizes remain $28,168,224$. The correct all-parallel representative is $\{01,23,45\}$. The old display $\{05,14,23\}$ is `AFFECTED` only as a representative.

## 11. Atom and curvature witnesses — `F-CERT-PUBLIC`

A rank-two locked four-pole has minimum pair-separating cut four, so route-lock does not imply a graph two-cut. A full-rank locked four-pole has nonzero curvature, so route-lock does not imply automatic flatness. Neither witness localizes the residual sector.

## 12. Defect/Petersen tables — `AFFECTED`

Local $K_6$ coefficients, weight patterns, co-root states, and Petersen transition tables remain valid finite geometry. They cannot support the old nontrivial defect-minimal forest claim because the fixed-terminal variation domain contains a zero-defect root cover. A future forest certificate must first identify a valid nontrivial variation slice.

## 13. Orthogonal source-fidelity classification

### Exact historical packet — theorem-level fixed-dimensional source

`FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md`, blob `2043ada9d28789ecc5f4f0028e62133f37835bc1`, is not classified in toto as `AFFECTED`, false, or history-only. Its exact rank-three/eight-support and five-slice results remain usable in their existing proof/certificate classes:

- $H_8$, Hamming kernel, and moment map;
- the six-dimensional $O^+(6,2)$ quotient;
- twenty-eight anisotropic roots;
- rank-three compatible root lifts;
- five-coordinate $O^-(4,2)$ slices and omitted-triple orthogonality.

Finite checks in that exact scope remain valid.

### Separate non-packet extrapolation — mathematically refuted

The arbitrary-rank $\Gamma\oplus\Gamma^*$ / $d_h(a)$ / $O^+(2r,2)$ tower has no recovered packet. It is

`SOURCE-UNRECONSTRUCTED / INFERRED-EXTRAPOLATION OR UNCOMMITTED DRAFT / MATHEMATICALLY REFUTED`.

It receives no packet assurance class and is not counted among the seventy-eight packets. Only a computation or inference that actually presupposes this nonexistent arbitrary-rank tower is `AFFECTED` for that inference.

The $q-2$ lower bound, deleted permutation module, rank-three exceptionality, and exact $O^-(4,2)$ model remain `F-PROVED`.

## 14. Renewal and reserve codes — `F-CENSUS / F-CERT-PRIVATE`

Point cores, reserve lines, $2K_3$ interfaces, shortened code dimensions, and fibre occurrence distributions are graph- and flow-specific data. The general affine occurrence-locus theorem is independent mathematics.

## 15. `CODE-PARTIAL` boundary

The recovered combined verifier was incomplete and one historical failing assertion is unavailable. Publicly reconstructed modules may supply evidence, but the whole historical verifier is not a public green end-to-end certificate. No theorem may rely solely on hidden notebook state, unavailable assertions, unexplained raw counts, or a table whose consuming theorem changed scope.

## 16. Minimum metadata

Every finite claim used by the active corpus should retain exact source commit/path, finite object and labelling convention, graph/flow/lift identifier, enumeration method or code location, raw output and symmetry convention, explicit witness where applicable, consuming theorem, assurance class, and all scope corrections.

## 17. What finite results do not prove

They do not prove physical BBD groupoid closure, a valid nontrivial defect variation slice, connected one-step escape for every bad flow, universal full-cap containment, scalar-sheet/source-cut equivalence, bounded transfer semantics for finite potentials, or the global five-support theorem.

## 18. Provenance and status

Programme B8’s original assurance checkpoint is `989cb002598fd91786029be201c2747c697bb476`; its controlling source-fidelity addendum is imported from `9ce8de5ca5b7b41e139be4c94572de7725446046`.

B8 is assurance normalization, not independent mathematical review, formal verification, public-code certification, manuscript approval, publication, release, arXiv, or DOI status. All unrelated B8 classes remain unchanged.
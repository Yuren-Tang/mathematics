# AC-PD-5CDC — fixed snapshot manifest for AC-CORPUS-V2

**Producer:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Receiving authority:** `AffineCDC — Director` / planned `AC-CORPUS-V2` intake  
**PDL branch:** `Yuren-Tang/mathematics:proof-development/affine-cdc-rigour-v1`  
**Frozen RL consumer input:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1@71a10f9ba86c1d2b8b7885e78fa9baa303c77818`  
**Exact PDL snapshot SHA:** the commit containing this manifest, as returned in the controlling issue comment.  
**Classification:** `FIXED COMPLETE-DRAFT SNAPSHOT / READY FOR INDEPENDENT AUDIT AND DIR-CURATOR INTAKE / NOT CANONICAL ACCEPTANCE`.

---

## 1. Snapshot result

The PDL branch contains a complete human proof draft of:

\[
\boxed{\text{Every finite bridgeless multigraph has a five-cycle double cover.}}
\]

No known mathematical proof obligation remains open inside the PDL dependency
graph.  The result has not yet passed independent adversarial audit and has not
been accepted into a Curator/canonical corpus.

---

## 2. Controlling PDL intake files

### Global index

- `projects/affine-cdc/proof-development/AC_PD_5CDC_PROOF_DAG_AND_STATUS.md`

### Structural and finite boundary core

- `AC_PD_5CDC_R1_ONE_CROSS_STRUCTURAL_REDUCTION.md`
- `AC_PD_5CDC_R2_1_BOUNDARY_ROUTE_CERTIFICATE.md`
- `AC_PD_5CDC_R2_2_SINGULAR_MORSE_DESCENT.md`
- `AC_PD_5CDC_R2_3_FIRST_FAILURE_AND_LOCAL_CONFLUENCE.md`

### Full-state fixed-order core

- `AC_PD_5CDC_R2_4_FULL_STATE_FORCED_BACKBONE.md`
- `AC_PD_5CDC_R2_5_ORIENTED_ODD_EXCLUSION.md`
- `AC_PD_5CDC_R2_6_CELLWISE_TRACK_ERASURE.md`

### Variable-order / contextual-return interfaces

- `AC_PD_5CDC_R2_7_WITNESSED_INNERMOST_BUBBLE_COMPRESSION.md`
- `AC_PD_5CDC_R2_7_MUTUAL_INDUCTION_HISTORY_WITNESS.md`
- `AC_PD_5CDC_R2_7_CHILD_CONTEXT_FIDELITY.md`
- `AC_PD_5CDC_R2_7_FULL_LABELLED_GENEALOGY_GLUING.md`
- `AC_PD_5CDC_R2_7_FIXED_ORDER_CONSUMER_COMPATIBILITY.md`
- `AC_PD_5CDC_R2_7_RESOLVED_CALL_CONTEXTUAL_TRANSFER.md`

### Final consumers

- `AC_PD_5CDC_R2_TO_R1_CAP_ROUTE_CONSUMER_AUDIT.md`
- `AC_PD_5CDC_GENERAL_GRAPH_OUTER_SHELL.md`

### Supporting audit/precedent notes

- `AC_PD_5CDC_TARGETED_LITERATURE_AND_PRECEDENT_NOTE.md`
- earlier R2 finite/local dossiers retained by the global index where explicitly
  cited.

The receiver should consume the exact files above through the frozen snapshot,
not infer current authority from every older research or proof-development file
on the branch.

---

## 3. Relation to RL master v6/v6.1

### Accepted and reconstructed

RL v6/v6.1 supplied the correct final architecture:

- witnessed lower-order calls;
- simultaneous `Q_N/P_N` induction;
- suspended ancestor marks;
- normalized support-switch/pop collars;
- innermost variable-order compression;
- finite resolved parent macro relation;
- rank `(N,d_N)`;
- original-prefix contextual transfer.

PDL independently reconstructed these as source-level proof units and supplied
the missing fixed-order `R2.6` consumer.

### PDL hardening of v6/v6.1

PDL makes the following points explicit.

1. **Target-boundary preservation.**  Track erasure is applied to one fixed
   ordinary parent-edge obligation.  At zero singular complexity it realizes
   that parent topology root-valuedly; it is not an argument that arbitrary
   root-NNI loops are impossible.
2. **Resolved relation saturation.**  The finite parent relation is closed under
   the proved boundary-equivalent macros before the shortest-distance rank is
   taken.
3. **Support-switch scope.**  A raw singular support-switch lift is never an
   interior Ptolemy cell; it appears only inside a switch--pop endpoint collar
   or a wholly root-valued terminal history.
4. **Central marks.**  A raw lower graph has no descendant of a cancelled central
   mark.  Successful bubble replacement carries it as a suspended predecessor-
   order boundary mark.
5. **Disconnected cancellation.**  It is a cyclic two-edge-cut gluing exit, not
   a synchronized multi-component child call.
6. **Descendant profiles.**  A route-changing `K_i` state on a descendant is a
   terminal for contextual return, not immediately a state in the original
   four-pole profile.

### R2.6 replacement

The controlling R2.6 proof is now cellwise:

\[
\text{pivot-change root seams}
+
\text{constant-pivot run sections}
\Longrightarrow
\text{closed/open/periodic track erasure}.
\]

It supersedes the need for a global canonical `C6` history section,
simultaneous six-star states or `C8=C6 triangle C6` as a global annulus theorem.
RL's bounded `C6/C8` movies and PDL R2.5 remain valid independent mathematics,
but are not load-bearing in the shortest final route.

---

## 4. Final proof spine

1. Five indexed supports are equivalent to an `R_5`-valued `E_5` flow on a
   loopless cubic graph.
2. At every simple edge, R1 supplies one smaller connected loopless bridgeless
   cubic cross closure.
3. One specified root flow on that cross has boundary state `A`, `B_j` or `C_j`.
4. `B_j` glues immediately; otherwise exact route rows give a separating rescue
   or one original-cap fixed-route equality/DDD lock.
5. Positive current-flow potentials produce a finite history of root NNIs and
   equal-face cancellations.
6. Failed inverse transfer has one normalized atom and one full-labelled
   nonbranching physical track.
7. Cellwise root seams and constant-run sections erase every fixed-order closed
   or normalized open track and discharge repeated outer endpoints.
8. Strict cancellations call lower witnessed `Q_n` instances; nested calls
   compress source-faithfully with suspended marks and switch--pop collars.
9. The finite resolved parent relation has no terminal-free sink; `d_N` gives a
   strict parent macro rank.
10. Contextual inverse transfer returns every descendant cap-compatible terminal
    to the original cap context.
11. One global `S_5` permutation aligns a common support-unordered boundary state
    and glues the cap.
12. Theta base and `Q_N`-then-`P_N` induction prove the cubic theorem.
13. Port-cycle expansion, memberwise cut-even collapse and loop insertion into
    two fixed supports prove the general finite bridgeless theorem with exactly
    five cover members.

---

## 5. Exact cap/route obligations

All are closed at PDL complete-draft level.

- `J_j cap K_i={B_j}` for a selected cross `j ne i`.
- Equality and DDD route rows have one unique non-`K_i` route, literally the
  original cap matching `M_i`.
- Separating channels give immediate cap rescue.
- Before the first route change, internal root surgery preserves the complete
  ordered boundary word.
- A descendant route change lands in `K_i` and is returned by R2.7 before any
  original-profile conclusion.
- Every direct matching gives a root theta terminal; the same-matching
  loop/bridge case uses a `(0,2,2)` zero-to-root crossed resolution.
- Disconnected cancellation is a two-cut gluing exit.
- Child cap block, route, support and dart data restrict and return literally.
- Common support-unordered states glue after one global support permutation.

---

## 6. Supersession ledger for intake

Do not use the following as controlling claims.

- contextual/confluence masters v1--v5 where cancellation is treated as an
  automatically completed lower-order obligation;
- any arbitrary terminal-flow or cover-independent reinsertion shortcut;
- generic marked-weld/two-edge flow-selection as a required theorem;
- child-prefix versus parent-prefix comparison;
- central-mark-count descent as the global rank;
- raw support-switch correction cells inside a closed Ptolemy core;
- global simultaneous-star / canonical `C6` history-section claims;
- proper Petersen subcycles treated as complete physical annuli;
- immediate original-profile conclusions from descendant route changes;
- bridge observation as completion of the same-matching direct terminal;
- the all-three-reconnection profile route as a necessary input.

Retain older packets only where the final DAG cites a local theorem, finite
certificate, bounded movie, conceptual interpretation or historical route.

---

## 7. Assurance and acceptance boundary

### PDL assertion

- complete human proof draft;
- no known internal mathematical gap;
- exact fixed snapshot suitable for independent audit and AC-CORPUS-V2 intake.

### Not asserted

- independent correctness;
- source-fidelity audit acceptance;
- Curator/canonical corpus acceptance;
- Lean formalization;
- manuscript or literature/novelty acceptance;
- release, tag, arXiv, DOI, peer review or publication.

### Recommended independent audit attack points

1. target-boundary preservation in fixed-order track erasure;
2. legal source realization and identity gluing of every cellwise seam;
3. resolved-call relation saturation and no-sink proof;
4. child-context cap/route fidelity;
5. switch--pop collar endpoint scope;
6. direct-terminal zero-to-theta correction;
7. one-global-permutation labelled cap gluing;
8. port-cycle bridgelessness and memberwise cut-even projection;
9. definition-level equivalence between five indexed even supports and standard
   `5-CDC`.

---

## 8. Receiver instructions

- Freeze the exact PDL commit reported with this manifest.
- Do not consume later moving branch content without a new AC-DIR retarget.
- Do not merge directly into `main` or the current Curator corpus.
- Launch independent assurance before canonical mathematical acceptance.
- Preserve the noncontrolling R2.5, bounded `C6/C8`, projectivity and historical
  MWR materials as optional theory; do not restore them as dependencies of the
  shortest proof.

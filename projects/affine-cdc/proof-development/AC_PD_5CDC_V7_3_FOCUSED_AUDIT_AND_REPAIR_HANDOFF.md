# AC-PD-5CDC v7.3 — focused audit and repair handoff

**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Receiver:** `AffineCDC — Director`, Research Lead repair lane, and one future independent focused auditor  
**Classification:** `XI REPAIR READY FOR FOCUSED AUDIT / FULL PROOF BLOCKED BY ZERO-PARENT INTERFACE`.

---

## 1. Exact reconstructed packet

The v7.3 PDL packet consists of:

1. `AC_PD_5CDC_V7_3_FIXED_CHANNEL_XI_RECONSTRUCTION.md`;
2. `AC_PD_5CDC_V7_3_CO_ROOT_PRESCRIBED_PARENT_RETURN.md`;
3. `AC_PD_5CDC_V7_3_ZERO_PARENT_REINSERTION_OBSTRUCTION.md`;
4. `AC_PD_5CDC_V7_3_TERMINAL_AND_INDUCTION_INTEGRATION.md`;
5. updated `AC_PD_5CDC_PROOF_DAG_AND_STATUS.md`.

The frozen RL input is

`research/affine-cdc-five-cdc-v1@212d789a5967813e7277fb3e269060926c99cb0e`.

---

## 2. Focused independent audit of the Xi theorem

An independent auditor should recompute the following without trusting the RL
or PDL tables.

### A. Frame

1. For every ordered disjoint pair `p,q`, unused support `m`, and rescue root
   `h_*` meeting both, prove uniqueness of the support bijection to
   `(12,34,5,14)`.
2. Under a nonexit common-marked `H_{h_*}` switch, verify
   `eta' = tau_14 o eta` and literal transport of the same support channel.
3. Confirm that a component containing exactly one marked occurrence is already
   the separating parent-repair output.

### B. Whole-component invariance

1. Recompute all `tau_14` triangle orbits.
2. Check `xi(tau_14 T)=xi(T)` for all ten types.
3. Audit the unmarked and common-marked component-switch formulas vertex by
   vertex over the **entire** closed graph, including cap and exterior vertices.
4. Confirm that no old `Omega` orbit-minimum statement is used.

### C. Strict local rows

1. Enumerate every distinct pair of triangles sharing `23` or `25`.
2. Find the unique root-valued opposite pairing.
3. Recompute all six `Delta Xi=-2` values.
4. For the four equal bad types, reconstruct the three local dart pairings,
   identify the two root placements and the forbidden zero/co-root placement,
   and verify the final strict NNI.

### D. Source semantics

For every branch-swap, support switch and final NNI, audit:

- complete labelled source topology;
- ordered local and exterior darts;
- cap vertices and cap darts;
- prescribed parent `P`;
- route/profile and component recomputation;
- prefix map `alpha`;
- category test and exact terminal result.

No coefficient-only movie is acceptable.

### E. Global descent

1. Reconstruct the bad-free `124` / roots `12,24` path contradiction.
2. Verify that every nonabsorbing macro returns a state in the same theorem
   class and lowers the transported nonnegative integer by exactly two.
3. Check the SCC logic carefully: the proof chooses a deterministic rescue
   channel at a state; it does not require one `Xi` to orient arbitrary
   missing-index migration edges.
4. Confirm the bound of at most `N` strict macros at order `N`.

A successful audit should return only a **co-root/DDD prescribed-parent**
result.  It must not promote the whole inverse-parent table.

---

## 3. Research Lead repair target

The smallest remaining theorem is

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}.}
\]

The canonical local word is

\[
(A,B,C,D)=(13,13,23,23),
\]

with prescribed parent pairing `AB|CD`, central value zero, and two crossed
root topologies with central root `12`.

Required complete-state conclusion:

> Every category-safe fixed-order zero-parent state reaches the literal parent,
> a cap-compatible route/profile output, or one named cyclic `2/3/4`-cut or
> bounded terminal by a finite source-faithful history using no cancellation
> and no lower-order call.

High-priority possible routes:

1. a fixed-channel support-switch rank for the `(0,2,2)` fibre;
2. an equality-face three-matching theorem upgraded from one local macro to a
   globally strict complete-state descent;
3. an ambient theorem forcing a separating component or named terminal;
4. a constrained target arborescence which proves zero parent edges are never
   selected, with exact quantifiers and no generic coloured-connectivity claim.

Forbidden substitutions:

- actual equal-face `2--0` cancellation during fixed-order return;
- lower witnessed `Q_N`;
- `M_N`, `d_N`, nested bubbles or terminal frames;
- “use the other crossed root NNI” without target progress;
- track erasure or periodic crosscut with the fixed zero boundary word;
- finiteness or SCC distance without proved reachability.

---

## 4. Zero-parent finite witness to reproduce

Use the Heawood root flow and active edge `4-5` specified in
`AC_PD_5CDC_V7_3_ZERO_PARENT_REINSERTION_OBSTRUCTION.md`.

An independent checker should verify:

1. both active vertices have type `123` and central root `12`;
2. the four external roots are `13,23,13,23`;
3. current and branch-swapped topologies are root-valued;
4. the equal-pairing parent has central value zero;
5. all three uncoloured labelled topologies are connected, simple and
   bridgeless;
6. no topology has a cyclic edge cut of size at most four;
7. the active cell is disjoint from the retained cap vertices/darts.

This certificate does not claim a closed full-alphabet SCC.  It proves that
category safety alone does not consume the zero row and that any repair must
use genuine route/support/ambient mathematics.

---

## 5. Assurance boundary

### Ready for focused independent audit

- fixed-channel frame and transport;
- switch-invariant `Xi`;
- six strict rows;
- four equal-neighbour root-only macros;
- bad-free route contradiction;
- co-root all-index SCC exclusion;
- integration with the v7.2 co-root parent sector.

### Not ready for full-theorem audit

- zero prescribed-parent escape;
- complete pure-NNI return through every stored prefix;
- ordinary strong induction;
- cubic five-support theorem;
- universal bridgeless five-CDC theorem.

No Curator/canonical, Lean, manuscript, release, tag, arXiv, DOI, peer-review or
publication consequence is authorized by this handoff.

# AC-PD-B7 — route-lock, curvature, and localisation map

**Proof-development state:** `READY-FOR-CURATOR / AC-RL-GAPS-EXPLICIT / GLOBAL-COMPOSITION-OPEN`  
**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Exact initial corpus base:** `main@960c92b7ff231c78b387894149779083060a75eb`  
**Constituent units:** B7.1–B7.2  
**External mathematical inputs:** none  
**Assurance:** complete authorial proof-development checkpoint for the route-lock rank/curvature theorem layer; localisation remains new mathematics

## 0. Input object

A universally route-locked DDD atom yields, conditional on its finite challenge quotient, a nowhere-zero flow

$$
c:E(Q)\to\mathbf F_2^3\setminus\{0\}
$$

with all four terminal values equal and four scalar even subgraphs all pairing the terminals by the same matching.

## 1. Rank reduction

- Rank one is impossible at an internal cubic vertex.
- Rank two makes the quotient a Tait three-edge-colouring.
- Every two-plane through the terminal colour has a root-triangle section through the challenge quotient.
- The resulting root-valued flow has all four terminal roots equal and therefore boundary state $A$.

Thus every rank-two route-locked state strictly escapes the Type H triangle profile. The only surviving locked sector has full rank three.

## 2. Full-rank curvature

The four scalar sheets carry side choices. On common terminal-colour edges $E_g$, failure of the four side values to satisfy the affine parity relation gives

$$
\Omega(c)\in\mathbf F_2^{E_g}/W,
$$

where $W$ is generated by closed scalar-component toggles.

### Flat branch

$$
\Omega(c)=0
$$

if and only if there is an eight-state potential

$$
p:V(Q)\to E_4
$$

with terminal values $0,0,g,g$ and

$$
p(u)+p(v)\in\{0,c(uv)+g\}.
$$

### Nonflat branch

$$
\Omega(c)\ne0
$$

if and only if there is

$$
\eta\subseteq E_g
$$

simultaneously in the cut space of all four scalar sheets and carrying an odd four-bit terminal word. This odd word represents the nonzero class of

$$
\mathbf F_2^4/E_4\cong\mathbf F_2.
$$

## 3. What is not yet localised

The flat potential need not have bounded same-fibre subgraphs. The nonflat witness need not be a cut of the underlying four-pole, need not have bounded size, and has not been canonically identified with the physical DDD $D_8$ class.

Thus curvature gives a complete primal/dual description, not a graph replacement theorem.

## 4. Type T boundary

The residual Type T policy is an acyclic deterministic $P_5\mid P_5$ routing automaton. No theorem yet turns this abstract policy into nested source paths, a serial four-pole composition, or a smaller separator.

## 5. Exact AC-RL gaps

Returned in issue #8 comment `5019455976`:

1. `AC-RL-TYPE-T-SERIALISATION`;
2. `AC-RL-FLAT-POTENTIAL-INTERFACE`;
3. `AC-RL-COMMON-CUT-LOCALISATION`;
4. `AC-RL-TYPE-H-COMMON-WITNESS`.

Earlier independent BBD gaps remain:

5. `AC-RL-BBD-GROUPOID-CLOSURE`;
6. `AC-RL-BBD-VARIATION-SLICE`.

## 6. Correct global frontier

The source-side closure theorem must turn persistent badness into at least one of:

- a Type T serial interface;
- a common Type H obstruction witness;
- a bounded flat-potential transfer object;
- a localised nonflat common-cut/DDD factor;
- a strict profile escape.

Every successful output must preserve indexed support data and decrease an explicit graph/interface measure.

## 7. Programme transition

B8 becomes active only as an assurance programme: inventory exact finite certificates, recover code/hash/provenance where available, and separate theorem dependencies from illustrative counts. B8 does not close the localisation gaps.

B9 will then assemble the corrected global dependency frontier without claiming the five-support theorem.

## 8. Completion assessment

`AC-PD-B7` is `READY-FOR-CURATOR / AC-RL-GAPS-EXPLICIT / GLOBAL-COMPOSITION-OPEN`.
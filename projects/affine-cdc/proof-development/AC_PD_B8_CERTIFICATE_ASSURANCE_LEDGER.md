# AC-PD-B8 — finite-certificate assurance ledger

**Proof-development state:** `READY-FOR-CURATOR / ASSURANCE-NORMALIZED`  
**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Exact initial corpus base:** `main@960c92b7ff231c78b387894149779083060a75eb`  
**Source checkpoint:** `research/affine-cdc-five-cdc-v1@dad218dd18ed05d1b7cb730c2dc2431b4db5ec9c`  
**Depends on:** B1–B7 corrected theorem hierarchy  
**Immediate consumer:** B9 final corrected frontier; Curator assurance synchronization  
**External mathematical inputs:** none

## 0. Assurance classes

Every finite claim is assigned one class.

### `F-PROVED`

The finite statement now has an independent mathematical proof in the active proof-development spine. Computation is corroboration only.

### `F-CERT-PUBLIC`

The public packet contains a complete explicit object, table, or dependency-free enumeration description sufficient in principle for independent checking, but AC-PDL has not rerun a public verifier.

### `F-CERT-PRIVATE`

The exact statement, refs, and outputs are public, but supporting code/notebooks remain on private or recovery branches. The claim may be used only with its certificate qualification.

### `F-CENSUS`

The count calibrates a named finite laboratory and is not a premise of a universal theorem.

### `CODE-PARTIAL`

A recovered combined verifier is incomplete or contains an unavailable historical failing assertion. No theorem may depend solely on this code state.

### `AFFECTED`

The finite data may remain correct, but a former downstream inference has been invalidated or narrowed by AC-PDL correction.

## 1. Public recoverability

All seventy-eight discovery packets are exactly recoverable from

$$
\texttt{dad218dd18ed05d1b7cb730c2dc2431b4db5ec9c}.
$$

There are eighty-two source commits and seventy-eight packet files. Exact source recovery, ancestry, and successor mapping are complete.

This establishes provenance and body availability. It does not establish public-code reproducibility or independent mathematical audit.

## 2. `F-PROVED` finite structures

The following finite statements have independent proofs in A/B proof units.

1. Ten four-terminal support-unordered states $A,B_i,C_i,D_i$.
2. Exactly 640 ordered four-terminal assignments.
3. Standard cap state sets $\mathcal K_i$.
4. Routing-weight coordinate table and fixed-route graph
   $$
   P_3\sqcup C_4\sqcup P_3.
   $$
5. Connectivity under two routing colours.
6. Half-cube clique number five.
7. Half-cube common-link types, including triangle link $K_2\sqcup K_1$.
8. Exact dominating-clique capacity for ranks two through five.
9. The no-go $K_8-C_5\not\to\mathscr A_5$.
10. The anisotropic/root/co-root counts in $H_5$: ten roots and five co-roots.
11. The $K_6$ triangle/one-factor zero-sum classification.
12. DDD atom/one-factor bijection and three-resolution triality.
13. Unique bad route under a full challenge.
14. Rank-one impossibility and rank-two Tait structure.
15. The four local root-admissibility exclusion sizes $0,1,1,6$.
16. The sharp additive-root dimension lower bound $q-2$ and deleted permutation module.

Any corresponding computation is redundant assurance, not a load-bearing premise.

## 3. Finite inputs used by theorem arguments

These results are finite inputs to later conditional theorem statements. They must remain cited and scoped.

### 3.1 Routing kernel and policy tables — `F-CERT-PRIVATE`

Claims:

- four minimal Kempe-closed mismatch kernels;
- unique safe route for each state/challenge;
- Type T and Type H policy-pair orbits;
- BBD and DDD physical triangle profiles.

Downstream use:

- B5 identifies the residual Type T/H mechanisms;
- B6.2's BBD Tait escape uses the deterministic distinct-route policy;
- DDD Tait escape uses the certified DDD triangle profile.

The general routing-weight and uniform-routing theorems are independent of these tables.

### 3.2 Rainbow monodromy table — `F-CERT-PRIVATE / AFFECTED`

Claims:

- sixteen BBD monodromies, four of each cycle type;
- four DDD monodromies;
- BBD permutations generate $S_5$;
- no common affine $\mathbf F_5$ structure.

Valid use:

- classifying individual loop permutation types;
- selecting the finite raw-choice quotient and parity square;
- motivating B6.1 local root-fibre cases.

Invalidated use:

- the table does not prove that the individual physical loops compose into one root-preserving affine group;
- it therefore does not by itself imply BBD translation-kernel vanishing or one simultaneous common origin.

### 3.3 Locked challenge quotient — `F-CERT-PUBLIC`

Claims:

- fixed co-root fibres are $K_{2,4}$;
- at a locked $C$ state, the challenge map has kernel $\langle q\rangle$;
- roots map to all nonzero values of $E_4$;
- all terminal roots map to $g=1111$.

Downstream use:

- B6.4's locked $F_2^3$ quotient;
- B7 rank and curvature theorem.

The unique-bad-route theorem itself is independently proved.

### 3.4 Rank-two root sections — `F-CERT-PUBLIC`

The three two-planes through $g$ in $E_4$ admit explicit root-triangle sections. This finite table is the only finite input in the rank-two lift; once supplied, the global Tait/root argument is theorem-level.

## 4. Laboratory classifications

### 4.1 Fixed-flow cube — `F-CERT-PUBLIC`

One explicit Fano flow has no globally factorable five-coordinate slice. This disproves the universal fixed-flow strengthening only. It does not obstruct another flow or a nonfactorable full-dual map.

### 4.2 Thirty-vertex laboratory — `F-CENSUS / F-CERT-PRIVATE`

Exact data include:

- a graph-level Tait colouring;
- a chosen bad two-state gauge fibre;
- renewal cubes and reserve codes;
- corrected componentwise fibre census;
- 7737 nonzero admissible switch pairs;
- 2801 connected-support one-step neighbours.

The numbers calibrate fixed-flow and one-step behavior. They prove no global trapping or universal clique theorem.

Required reading:

- 7737 is composite-endpoint population;
- 2801 is actual reconfiguration adjacency population.

### 4.3 Petersen complete classification — `F-CERT-PRIVATE`

Claims:

- 28560 nowhere-zero Fano flows;
- 170 $GL(3,2)$ orbits;
- five combined graph/colour orbits;
- complete reduced-fibre table;
- every Fano flow has a componentwise-good lift;
- connected-cycle flow graph is connected;
- three surface types.

This is a complete finite theorem about Petersen, not a universal argument. Public corpus records exact counts; public rerunnable code is not part of the active tree.

### 4.4 Flower $J_5$ — `F-CERT-PUBLIC + F-CENSUS`

The explicit flow and two duals provide concrete witnesses:

- one $K_6$ obstruction;
- one $K_6$-free obstruction containing $K_2\vee W_5$.

The human half-cube no-go is theorem-level conditional on the displayed dual. The 712-neighbour and 528-good-neighbour counts remain finite census data.

### 4.5 Small four-poles — `F-CERT-PRIVATE`

Claims:

- thirty connected cap-admissible multigraph classes through six internal vertices;
- eight complete profiles;
- every such profile contains a full cap set;
- none realizes the four residual abstract kernels.

This gives the finite lower bound of eight internal vertices for a minimal-counterexample cyclic four-cut. It does not prove the universal full-cap theorem.

Larger samples are exploratory only.

## 5. Renewal, reserve, and arrangement data

Point cores, $K_4$ reserve lines, $2K_3$ interfaces, shortened reserve-code dimensions, and fibre occurrence distributions are `F-CENSUS / F-CERT-PRIVATE`.

The general affine occurrence-locus theorem is independent mathematics. Detailed distributions remain graph- and flow-specific.

## 6. Atom and curvature witnesses

### No-two-cut witness — `F-CERT-PUBLIC`

An explicit rank-two locked four-pole has all scalar sheets routed identically but minimum pair-separating cut four. This validly refutes route-lock $\Rightarrow$ graph two-cut.

### Nonflat witness — `F-CERT-PUBLIC`

An explicit full-rank locked four-pole has nonzero curvature. This validly refutes automatic flatness.

These witnesses protect trust boundaries; they do not localize the residual sector.

## 7. Defect and forest status — `AFFECTED`

Finite local checks of $K_6$ coefficients, local weight patterns, co-root atoms, and Petersen transitions remain valid.

The claimed nontrivial defect-minimal forest is not supported by the stated variational problem: the original root cover is a zero-defect competitor. No finite forest census may be used to repair that logical issue.

Any future forest certificate must identify a valid nontrivial holonomy-compatible variation space first.

## 8. Orthogonal hierarchy status — `AFFECTED`

Finite rank-three and rank-four root tables remain valid. They do not support the false universal $2r$-dimensional orthogonal-root theorem. The correct universal dimension is $q-2$.

Any computation generated under the false higher-rank model requires reclassification before use.

## 9. Matching-orbit display correction

The orbit counts

$$
28,\qquad168,\qquad224
$$

remain valid. The displayed all-parallel representative must be

$$
\{01,23,45\}
$$

or another genuine three-parallel matching; $\{05,14,23\}$ is not all-parallel.

## 10. `CODE-PARTIAL` boundary

The recovered combined verifier was incomplete, and one historical failing assertion is unavailable. Reconstructed public modules were rerun in the recovery process, but the whole old verifier is not a public green end-to-end certificate.

No universal theorem in A/B may rely solely on:

- hidden notebook state;
- an unavailable historical assertion;
- an unexplained raw count;
- a witness table without edgewise validation;
- a finite table whose scope has changed under B1–B7 corrections.

## 11. Minimal certificate metadata

Every finite result promoted during curation should record:

1. exact source commit and packet path;
2. exact finite object and labelling convention;
3. input graph/flow/root-lift hashes or canonical edge lists;
4. code or dependency-free enumeration procedure, when available;
5. raw output and orbit-action convention;
6. explicit witness or counterexample for existential claims;
7. the theorem statement that consumes the certificate;
8. assurance class from this ledger;
9. any B1–B7 scope correction affecting its interpretation.

## 12. Completion assessment

`AC-PD-B8` is `READY-FOR-CURATOR / ASSURANCE-NORMALIZED`. It adds no global mathematics. B9 may now assemble the corrected theorem and frontier DAG without treating finite evidence as a missing proof.
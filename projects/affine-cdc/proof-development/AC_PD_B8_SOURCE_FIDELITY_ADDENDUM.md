# AC-PD-B8 — source-fidelity assurance addendum

**State:** `CONTROLLING ADDENDUM / READY-FOR-CURATOR`  
**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Controls:** source-classification statements in `AC_PD_B8_CERTIFICATE_ASSURANCE_LEDGER.md`, especially §§1 and 8  
**Trigger:** Audit B source-migration defect and issue #37 comment `5028845743`  
**Mathematical theorem/certificate classifications:** unchanged except for provenance attribution

## 1. Exact supersession

This addendum supersedes only the following B8 implications:

1. “exact recovery and successor mapping are complete” must be read as **Git recovery/body availability complete**, not as accepted source-classification fidelity;
2. the phrase “false universal `2r`-dimensional orthogonal-root theorem” must refer to the **source-unreconstructed inferred extrapolation**, not to `FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md`;
3. the historical packet must not be classified in toto as `AFFECTED`, false, or history-only.

All theorem/certificate/census/private-code distinctions elsewhere in B8 remain controlling.

## 2. Correct assurance classification

### 2.1 Historical packet

`FIVE_CDC_UNIVERSAL_ORTHOGONAL_ROOT_LIFT_V1.md` at blob `2043ada9d28789ecc5f4f0028e62133f37835bc1` is:

`THEOREM-LEVEL HISTORICAL SOURCE / EXACT RANK-THREE-EIGHT-SUPPORT AND FIVE-SLICE SCOPE`.

Its valid contents include:

- `H_8`, Hamming kernel and moment map;
- the six-dimensional `O^+(6,2)` quotient;
- twenty-eight anisotropic roots;
- rank-three AffineCDC root lifts;
- five-coordinate `O^-(4,2)` slices and omitted-triple orthogonality.

Finite checks concerning those exact fixed-dimensional objects remain usable in their stated assurance classes.

### 2.2 Source-unreconstructed extrapolation

The arbitrary-rank `Γ⊕Γ* / d_h(a) / O^+(2r,2)` tower is:

`NO RECOVERED PACKET / SOURCE-UNRECONSTRUCTED / INFERRED-EXTRAPOLATION OR UNCOMMITTED DRAFT / MATHEMATICALLY REFUTED`.

It is not one of the seventy-eight recovered packets and must not receive a packet assurance class.

### 2.3 Mathematical replacement

The following remain `F-PROVED`:

- the sharp lower bound `dim V≥q-2`;
- the deleted permutation module of dimension `q-2`;
- rank-three exceptionality `8-2=6`;
- the exact five-support `O^-(4,2)` root model.

## 3. Source-population accounting

The facts

- eighty-two source commits;
- seventy-eight packet files;
- exact Git recoverability from `dad218dd...`

remain verified.

What is withdrawn is the conclusion that the current packet-to-class/successor disposition is source-faithful. The packet partition must be recomputed by Curator after restoring the named packet's valid class. The uncommitted extrapolation is not added to the packet count.

Neither

`39+10+18+10+1=78`

nor

`38+10+18+10+1+1=78`

is accepted merely by renaming one class. The final category counts must follow the corrected packet-by-packet table.

## 4. Finite evidence consequence

No finite result may be invalidated merely because it occurs in the named packet. Instead:

1. verify whether the result belongs to its exact rank-three/eight-support or five-slice scope;
2. retain it if it does, with its existing proof/certificate class;
3. classify only computations or deductions that actually presuppose the nonexistent arbitrary-rank tower as `AFFECTED`;
4. require an exact source and fixed dimension for every later orthogonal-rank claim.

## 5. Curator propagation requirement

The replacement B8/source ledger must:

- restore the packet's exact theorem-level provenance;
- attach the `SOURCE-UNRECONSTRUCTED` class to the extrapolation, outside the seventy-eight-packet partition;
- repair migration, supersession, recovery, assurance, and status maps;
- retain all unrelated B8 classifications;
- return for bounded B2/B8 source-fidelity re-audit.

## 6. Scope boundary

This addendum changes no B1, B3–B7, B9, Programme A, finite routing, holonomy, defect, atom, curvature, or code-partial result. It modifies no fixed candidate, audit branch, Curator branch, `main`, public issue #2, Lean, manuscript, release, publication, arXiv, DOI, tag, or Research Lead surface.
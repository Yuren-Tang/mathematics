# AC-PD v7.4.1 — co-root and zero-parent application reclosure

**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Workstream:** `AC-PD-5CDC-V7.4.1-01`  
**Application-scope provider:** `AC_PD_V7_4_1_APPLICATION_SCOPED_COMPONENT_CHAIN.md`  
**Terminal-exhaustion authority:** `audit/affine-cdc-terminal-exhaustion-v1@c954f2220c082bc3b47821657b7a1164876aeaab`  
**Local-row authority:** `audit/affine-cdc-component-chain-v1@a4f20f05532c307a3a8f9d98ac4c5162e7b8ad57`  
**Classification:** `BOTH INVERSE-PARENT FIBRES RECLOSED IN EXACT APPLICATION SCOPE / FULL AUDIT REQUIRED`.

---

## 1. Shared rule of use

Neither fibre invokes the failed broad rooted-carrier theorem.  The only chain
lemma used is

`FC-EXACT-TWO-TERMINAL-PATH-COMPONENT-CHAIN`,

and it is called only after the relevant #82 constructor has proved literally

\[
H_h(R)=P_0\dot\cup P_1\dot\cup\text{closed channel cycles}.
\]

The independently checked local rows are then exhaustive: every internal
nontrivial quotient node is either one inactive singleton or one closed channel
cycle.  A third terminal path is impossible at the callsite rather than silently
ignored by the contraction proof.

---

## 2. Co-root complete-state input

Let the current fixed-order state be a complete closed category-safe co-root/DDD
prescribed-parent state.  The two live marked parent roots are disjoint roots

\[
p,q,\qquad p+q=Q_m.
\]

Choose a current physical rescue support `h` meeting each marked root once.  A
nonlocked state is already consumed by the exact Phase-B object: separating
channel, `K_i`/cap-compatible route, cut or bounded category.  The continuing
case is therefore a common-component lock in which the two marked `H_h` edges
lie on one closed physical channel cycle with the blocked ordered route.

The state retains literally:

- complete labelled cubic topology and every stable dart;
- the two marked stable edge identities and ordered endpoint darts;
- selected physical channel and current component partition;
- cap vertices, cap edge, cap darts and order;
- route/profile and category fields;
- literal prescribed-parent topology;
- support transport, missing-index data and stored-prefix ancestry.

---

## 3. Co-root application constructor and chain

Delete exactly the interiors of the two marked channel edges and retain their
four ordered endpoint darts.

By the independently verified co-root terminal census at `c954f222...`:

1. the marked roots are disjoint, so the marked edges are nonadjacent;
2. cutting their common channel cycle produces exactly two canonically ordered
   marked arcs `P_0,P_1`;
3. every other nontrivial `H_h` component remains a closed cycle;
4. cap, exterior, route, parent and prefix fields create no extra terminal;
5. a disconnected cut carrier is consumed first by a bridge, exact two-cut or
   bounded low-port output.

Thus every continuing connected carrier satisfies the exact hypothesis of the
conditional chain lemma.

Choose one witnessed quotient chain between `P_0,P_1`.  Every nonfinal move
absorbs one inactive singleton or one closed cycle while preserving the four
marked terminal darts.  The connector list is selected once and shortened by
literal deletion of its first stable edge.  No chain rank is reinitialized before
this parent obligation terminates.

The final connector changes the ordered matching of the four marked darts.
Reinsert the two marked edges.  Exactly one of the following occurs:

1. the marked edges lie in distinct closed `H_h` components;
2. they lie in one component with the opposite ordered marked route;
3. a graph/category/cap terminal fired at the current descendant.

Case 1 is the separating-channel repair.  Case 2 is the cap-compatible fixed-
route output.  No fourth matching exists.

---

## 4. Co-root final consumers

### Separating channel

Switch the complete closed `H_h` component containing exactly one marked edge.
The switch changes exactly one marked root by `h`.  Since `h` meets the two
disjoint roots once,

\[
Q_m+h
\]

is a root.  The ordinary NNI on the literal stored parent is therefore
root-valued.  The output uses the same four marked darts and exact parent
pairing, not an isomorphic replacement.

### Opposite route

Apply the existing current-descendant `K_i`/fixed-route consumer.  The route
output is read from the actual ordered marked darts after the final connector;
it is not transported backward by an unproved topology identification.

Every loop, parallel edge, bridge, cyclic `2/3/4` cut or bounded degeneration is
consumed immediately by its existing exact row.

### Co-root theorem

`FC-PURE-NNI-CO-ROOT-APPLICATION-ESCAPE`:

every complete category-safe co-root prescribed-parent state has a finite
source-faithful same-order history to the literal stored parent, a cap-compatible
route/profile output, or a named cut/bounded terminal, provided the component-
chain call is made only through the #82 co-root constructor above.

---

## 5. Permanent co-root negative and positive models

### #78 negative boundary

Retain permanently:

- `h=14`;
- marked edges `1-6:12`, `2-3:34`;
- equal face `9-14:23`, endpoint types `123,123`.

Its root branch swap preserves the common marked component and route

\[
(1,2)\mid(3,6).
\]

It remains a counterexample to arbitrary-equal-face `Xi` totality.  No step of
the new application proof selects it merely because it is an equal bad face.

### `6-7` positive local model

For the same genuine application cut, stable edge `6-7:23` joins the two actual
marked arcs.  The independently checked row is

\[
123+234\longrightarrow124+134,\qquad23\longrightarrow14,
\]

with inside-dart reattachments

\[
5-6@6\to7,\qquad7-8@7\to6.
\]

The marked matching changes

\[
(1,2)\mid(3,6)\longrightarrow(1,3)\mid(2,6).
\]

The output is connected, simple, cubic, bridgeless, girth five and cyclically
five-edge-connected.  This is a checked model of the final-connector row, not a
universal totality proof.

---

## 6. Zero-parent complete-state input

Normalize the ordered active branches to

\[
(A,B,C,D)=(13,13,23,23),
\]

with current crossed central root `12`, selected physical channel `H_35`, and
literal stored parent `AB|CD` whose central value is zero.

All four branch roots belong to `H_35`; the active central edge does not.  The
complete state retains both crossed sheets, the active edge/darts, cap, route,
category, parent, support and prefix fields.

---

## 7. Zero-parent application constructor

Delete exactly the active two-vertex cell and retain exactly the four outside
branch darts `A,B,C,D`.  The nonchannel central edge creates no channel terminal.

By the independently verified zero-parent terminal census at `c954f222...`:

1. these four branch darts are all and only the selected-channel terminals;
2. deleting the active cell leaves exactly two ordered terminal paths;
3. every other nontrivial `H_35` component is a closed cycle;
4. cap, exterior, route, alternate sheet, parent and prefix fields create no
   hidden terminal;
5. a disconnected complement is consumed by bridge, exact two-cut or bounded
   low-port rows before any chain invocation.

The outside matching is exactly one of

\[
AB\mid CD,\qquad AC\mid BD,\qquad AD\mid BC.
\]

A crossed matching is consumed immediately.  Only the residual `AB|CD` lock
calls the conditional chain, with paths

\[
P_{13}:A--B,\qquad P_{23}:C--D
\]

and all remaining components closed.

---

## 8. Zero-parent scoped contraction and parent realization

In the residual lock invoke `FC-EXACT-TWO-TERMINAL-PATH-COMPONENT-CHAIN`.
Every prefinal move consumes one inactive `124` singleton or one closed `H_35`
cycle.  The one inherited stable connector list is never reselected or reset.
The final connector changes `AB|CD` to one crossed ordered matching.

Choose the active crossed root sheet with the same matching, using zero or one
root branch-swap NNI on the active central edge.  This alignment is not counted
as progress; progress is the already exposed outside matching change.

Closing the aligned active cell with the two outside paths produces exactly two
closed `H_35` components.  Each contains one `13` branch and one `23` branch.
Switch either complete component by `35`.  Exactly one active occurrence of each
kind changes:

\[
13\mapsto15,\qquad23\mapsto25.
\]

Up to the fixed ordered symmetry, the active word becomes

\[
(15,13,25,23),
\]

and

\[
15+13=25+23=35.
\]

Perform the ordinary NNI on the literal stored `AB|CD` parent with central root
`35`.  The same four branch stable edges retain their outside endpoints and
ancestry; only the displayed inside darts are reattached.  The stored-prefix map
is updated by this actual source movie and is the identity elsewhere.

### Zero-parent theorem

`FC-PURE-NNI-ZERO-PARENT-APPLICATION-ESCAPE`:

every complete category-safe zero-parent state has a finite same-order history
to the literal stored parent, a cap-compatible route/profile output, or a named
cut/bounded terminal, provided the chain is invoked only through the #82 active-
cell constructor above.

---

## 9. Zero-parent Heawood model boundary

The checked model at active edge `4-5:12` remains:

```text
root branch swap at 13-14
    -> two closed H_35 components
    -> switch Z_0=1-2-3-4-13-1 by 35
    -> active word (15,13,25,23)
    -> literal AB|CD parent NNI, central root 35.
```

All displayed states are connected, simple, cubic and bridgeless; the model
preserves the retained cap block and exact stable darts.  It is evidence for the
local alignment/switch/parent collar, not the universal provider of the #82
terminal census.

---

## 10. Shared source and recursion boundary

For both fibres:

- chain moves are ordinary root NNIs/equal root branch swaps at fixed graph
  order;
- any support switch occurs only after the terminal matching has changed;
- no `2--0` cancellation occurs in fixed-order return;
- no lower-order root-flow call occurs inside either fibre;
- no same-level root-solubility recursion occurs;
- no track erasure, finite-state/SCC distance, generic NNI connectivity,
  `Q_N`, `M_N`, `d_N`, nested bubble or terminal frame is used;
- cap, route, category, parent, active-dart and stored-prefix fields are literal
  current-descendant data at every consumer.

---

## 11. Assurance boundary

Closed at PDL reconstruction level:

- exact co-root constructor, scoped chain call and Phase-B return;
- exact zero-parent constructor, scoped chain call, alignment, switch and parent;
- permanent preservation of #78 and #81 negative boundaries;
- no active use of the failed broad theorem.

Still required:

- independent end-to-end audit of the exact v7.4.1 packet;
- no five-support or five-CDC acceptance is claimed.
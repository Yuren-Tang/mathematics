# AC-PD v7.4.1 — inverse table, prefix return and induction reclosure

**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Workstream:** `AC-PD-5CDC-V7.4.1-01`  
**Scoped fibre theorem:** `AC_PD_V7_4_1_CO_ROOT_AND_ZERO_RECLOSURE.md`  
**Classification:** `COMPLETE PROOF-DEVELOPMENT RECLOSURE / NEW FULL AUDIT REQUIRED`.

---

## 1. Exact module boundary and lower-order discipline

The controlling first-cancellation module is

`AC_PD_5CDC_V7_2_FIRST_CANCELLATION_AND_SINGLE_POP.md`.

Its input is one **specified root flow** on one valid smaller cross closure.  That
specified flow is the upstream R1/minimal-counterexample input with which the
fixed-route return module begins; it is not opened recursively from inside the
return history.

Inside this module there is exactly one fresh lower-order existence invocation:
when the first equal-face cancellation produces the genuine connected loopless
bridgeless target cap closure of order `N-2`, ordinary lower existence supplies
one arbitrary root flow on that actual target.  This is the unique graph-order
descent opened by the return module.

After that call:

- exactly one inverse pop is performed;
- no second lower-order call is made;
- no lower-order history is followed;
- all remaining work is at predecessor order;
- every later cut/bounded output is sent to an already named strictly-smaller
  terminal consumer rather than recursively solved inside the fixed-order
  return.

This is the precise sense in which the controlling return has exactly one
lower-order call.  A new full auditor must separately confirm that the upstream
R1 provision of the specified cross flow and the first-cancellation module are
composed with this same boundary, rather than counted as two nested calls.

---

## 2. Single inverse pop and complete prescribed-parent state

Let the arbitrary lower target flow be fixed.  The source-level inverse-pop
table has exactly the following outputs:

1. a root-valued same-order contextual state;
2. one standard co-root atom after the missing-index doubled-disjoint row;
3. an exact current-target terminal.

All four/five exterior darts, cap block, route data and target topology are
retained literally.  The missing-index source movie leaves at most one persistent
atom; no second atom and no second lower-order call is introduced.

If one atom remains, apply only the retained v7.2 one-token coherence layer:

- nonbranching state walk;
- maximal constant-pivot physical root sections;
- six-port pivot-change seams;
- two-seam backtrack treatment;
- literal run/seam gluing on complete labelled boundaries.

This layer exposes a complete root-valued prescribed-parent state or a named
terminal.  It does not realize the parent by track erasure and does not invoke
root solubility at the same graph order.

---

## 3. Exhaustive inverse-parent value table

At one stored inverse NNI, the forced parent central value is the symmetric
difference of two roots.  Its weight is exactly `0`, `2`, or `4`:

| central value | fibre | active disposition |
|---|---|---|
| weight `2` root | ordinary root row | perform the literal stored parent NNI |
| weight `4` co-root `Q_m` | DDD `(4,2,2)` | invoke `FC-PURE-NNI-CO-ROOT-APPLICATION-ESCAPE` |
| weight `0` | equality `(0,2,2)` | invoke `FC-PURE-NNI-ZERO-PARENT-APPLICATION-ESCAPE` |
| route/category flag | terminal | exact current-descendant consumer |

There is no fourth algebraic row.

The co-root and zero rows invoke no broad rooted-carrier theorem.  Each first
constructs its genuine #82 application carrier and only then invokes the
conditional exact-two-terminal-path chain.

---

## 4. Root row

If the stored parent central value is a root, perform the ordinary labelled NNI
on the exact stored four darts.  The output topology is the literal predecessor
at that prefix stage.

After graph/category and route tests:

- success lowers the stored-prefix length by one;
- a named output terminates through its exact consumer;
- no auxiliary rank or chain is opened.

---

## 5. Co-root row

For a co-root parent:

1. consume every nonlocked state by the existing Phase-B object;
2. in the common-component lock cut exactly the two marked channel edges;
3. import the #82 co-root census: exactly two ordered marked arcs, every other
   selected-channel component closed;
4. initialize one stable-connector list between those arcs;
5. invoke the conditional chain once;
6. consume the final matching as separation or the opposite cap-compatible
   route;
7. use the Phase-B switch/parent collar or exact terminal.

The #78 same-arc face is never a totality provider.  The `6-7` Heawood move is a
checked local model only.

A successful literal parent NNI lowers the stored-prefix length by one before
any new chain rank may be initialized.

---

## 6. Zero row

For a zero parent:

1. normalize to `(13,13,23,23)`, current root `12`, selected `H_35`;
2. delete exactly the active two-vertex cell;
3. import the #82 zero census: exactly two ordered terminal paths, every other
   selected-channel component closed;
4. consume a crossed outside matching immediately;
5. only in residual `AB|CD`, initialize one stable-connector list between
   `A--B` and `C--D`;
6. invoke the conditional chain once;
7. after the final crossed outside matching, use zero or one active-sheet root
   NNI;
8. switch one of the two closed `H_35` components;
9. perform the literal stored `AB|CD` NNI with central root `35`.

A successful literal parent NNI lowers the stored-prefix length by one before
any new chain rank may be initialized.

---

## 7. No-rank-reset invariant

Let

- `L` be the number of stored source NNIs still to invert;
- `C` be the number of remaining connectors in the currently active application
  chain, when a singular fibre is open.

At fixed graph order use the lexicographic control

\[
(L,C),
\]

with `C=0` when no chain is active.

The protocol is:

1. at one parent obligation, inspect its value exactly once;
2. if root, cross it and lower `L`;
3. if co-root or zero, construct the appropriate #82 carrier and select one
   connector list exactly once;
4. while that obligation remains active, each nonterminal chain move deletes the
   first named connector and strictly lowers `C`;
5. no new chain, channel, quotient path or shortest-distance computation may
   replace the active list at the same parent obligation;
6. final matching progress is consumed immediately into the literal parent or a
   terminal;
7. only after literal parent success lowers `L` may the next stored obligation
   initialize its own local chain rank.

Thus the innermost rank is not reset while the middle rank is fixed.  There is
no same-level scheduler, recurrence, SCC distance or appeal to finiteness.

---

## 8. Stable active-dart lineage and prefix seams

Every stored prefix edge carries four ordered exterior darts and its literal
predecessor pairing.  The one-atom run/seam layer preserves the same complete
boundary.  The application chains occur remotely or on explicitly displayed
connector cells while retaining:

- the active parent darts as boundary identities;
- cap vertices, cap edge and ordered cap darts;
- route/profile and support-channel data;
- graph-category flags;
- stored-prefix ancestry.

The final support switch in a singular fibre changes roots on one complete
closed component but not topology or dart pairing.  The literal parent NNI then
reattaches only the displayed inside darts.  Therefore its output is the exact
next prefix state, not merely an isomorphic root flow.

Seam/run replacement is used only for one-atom coherence.  It never counts as a
parent realization or a decrease of `L`.

---

## 9. Exact terminal consumers

The downstream ledger contains current-descendant consumers for:

- `K_i` and cap-compatible fixed-route states;
- separating support channels;
- bridge and exact two-edge-cut outputs;
- cyclic three-cut and four-cut completions;
- loop, forbidden parallel, triangle, theta and direct-matching bases;
- acyclic one-/two-port bounded shores;
- single-pop coincidence, unavailable-borrow and bounded local rows.

A component-chain NNI introduces no new terminal type.  Every failed category,
route or cap test is consumed immediately before another connector is used.
Cut consumers use strictly smaller completed graphs; no same-order recursive
root-solubility call is hidden in them.

---

## 10. Ordinary induction reclosure

Let `P_N` denote root solubility for connected loopless bridgeless cubic
multigraphs through order `N`.  In the minimal-counterexample/strong-induction
step:

1. dispose of the theta base;
2. choose a simple edge and the valid smaller cross supplied by R1;
3. enter the fixed-route module with one specified root flow on that cross;
4. follow the finite target-synchronized root-NNI prefix;
5. consume an early route/category output, or stop at first cancellation;
6. on the genuine actual target of order `N-2`, make the unique lower-order call
   of the return module;
7. perform the single arbitrary-flow inverse pop;
8. normalize at most one atom to a complete prescribed-parent state;
9. apply the exhaustive root/co-root/zero table with the two scoped application
   lemmas;
10. return through every stored prefix edge under the no-rank-reset invariant;
11. obtain a root flow on the original cubic graph.

After module entry there is no second graph-order call and no fixed-order
root-solubility recursion.  The chain measures are finite physical witness lists,
not theorem calls.

---

## 11. Cubic theorem and outer shell

The reclosed PDL candidate gives:

> every connected loopless bridgeless cubic multigraph admits a root-valued
> `E_5` flow, equivalently five indexed even supports covering each edge twice.

Apply only the previously accepted conditional, support-count-preserving outer
shell:

1. remove loops and later place each loop in two fixed supports;
2. replace noncubic vertices by port-cycle cubic expansions;
3. solve every cubic component using the same five support indices;
4. collapse every expansion memberwise;
5. preserve evenness across each original cut and preserve the count of five
   indexed supports.

This yields the proof-development candidate:

> every finite bridgeless multigraph has a five-cycle double cover, with cycles
> interpreted as even subgraphs and at most five indexed members.

The outer shell is not used to bridge a missing cubic implication; it is applied
only after the exact cubic candidate has been reclosed.

---

## 12. Explicit nondependencies

The active v7.4.1 spine uses none of:

- the failed general rooted-carrier component-chain theorem;
- the #81-invalid inference from two distinguished paths alone;
- arbitrary-equal-face route-or-split;
- `Xi` or `Omega` as unconditional totality;
- a second cancellation in fixed-order return;
- a second lower-order call after the single target call;
- same-level root-solubility recursion;
- track erasure as target progress;
- finite-state/SCC distance or generic NNI connectivity;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames.

---

## 13. Reclosure status

At PDL reconstruction level, the complete active chain is:

```text
specified valid-cross root flow
    -> synchronized root-NNI prefix
    -> first cancellation / unique actual-target lower call
    -> one arbitrary-flow inverse pop
    -> root / one-atom / exact terminal
    -> complete prescribed-parent state
    -> exhaustive root/co-root/zero table
    -> #82 constructor
    -> exact-two-path conditional chain
    -> literal stored parent
    -> lower stored-prefix length
    -> ordinary strong induction
    -> cubic five-support candidate
    -> support-count-preserving outer shell.
```

No known PDL gap remains in this reclosed dependency cone.  Independent full
mathematical audit is still mandatory, especially at the module boundary for the
specified cross flow and the unique lower-order target call.  No theorem
acceptance is claimed.
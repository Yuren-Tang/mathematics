# AC-PD v7.4 — co-root marked-arc component-chain escape

**Owner:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**Negative audit boundary:** `audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`  
**Classification:** `COMPLETE-DRAFT FOR THE CO-ROOT/DDD PRESCRIBED-PARENT FIBRE / NEW AUDIT REQUIRED`.

This theorem permanently retains the Heawood same-arc face as a counterexample
to the old arbitrary-equal-face lemma.  The repaired selection rule chooses a
physical connector chain between the two actual marked arcs.

---

## 1. Complete co-root state

Let the live parent roots be disjoint roots

\[
p,q,\qquad p+q=Q_m.
\]

Choose one physical rescue support `h` meeting both roots once.  In a locked
state the two marked `H_h` edges lie in one physical channel component with the
blocked ordered route.

The complete state retains:

- the labelled graph and stable darts;
- the two marked edge identities and ordered endpoint darts;
- the literal prescribed-parent topology;
- the cap block and route/profile field;
- every `H_h` component and support transport;
- graph-category and terminal flags;
- the stored-prefix map.

Nonlocked states are already consumed by the Phase-B separating-channel,
`K_i`, route or exact category object.  The corollary concerns only a common-
component nonterminal lock.

---

## 2. Cut the actual marked edges

Delete the interiors of the two marked channel edges but retain their four
ordered terminal darts.  Their common channel cycle becomes two distinguished
terminal paths

\[
P_0,P_1.
\]

Every other nontrivial `H_h` component is a closed cycle.  The terminal ordering
remembers which two ends belong to the first marked edge and which two to the
second.

### Lemma 2.1 — cut-carrier category

If the physical carrier after deleting the two marked edge interiors is
disconnected, the two marked edges expose:

- a bridge/single attachment;
- an exact cyclic or ordinary two-edge cut; or
- a bounded shore.

This is a named terminal.  Hence every continuing prime nonterminal state has a
connected cut carrier.

---

## 3. Marked-arc chain contraction

Apply `FC-COMPONENT-CHAIN-ROOT-NNI` to `P_0,P_1`.

Every pre-final move absorbs either one inactive vertex or one intervening
closed channel cycle.  It fixes the two marked terminal darts and the marked
edge identities because those edges are cut boundary data, not active central
edges of the remote chain move.

The inherited connector list is physical and source-labelled.  No arbitrary
equal face is selected.

At the final connector the matching of the four marked terminals changes.
There are only three ordered matchings:

1. the original blocked common-cycle route;
2. the opposite common-cycle route;
3. the matching pairing the two ends of each marked edge separately.

After reinserting the marked edge interiors:

- matching 3 gives two closed `H_h` components, one containing each marked edge;
- matching 2 gives the opposite marked route.

The first is a separating rescue channel; the second is the cap-compatible
route under the fixed-route blocking theorem.

---

## 4. Final consumer

### Separating channel

Switch the closed component containing exactly one marked parent root.  Since
`h` meets both disjoint parent roots once,

\[
Q_m+h
\]

is the complementary root.  The literal prescribed-parent NNI is then
root-valued.

### Opposite route

Use the exact existing `K_i`/fixed-route consumer with the current complete
boundary trace.  The route change is returned on the actual current descendant;
no earlier topology is identified by isomorphism alone.

Any graph/category failure during the chain is consumed immediately by the
named cut/bounded ledger.

---

## 5. Permanent negative Heawood witness

Retain exactly the audit-#78 witness:

- fixed channel `H_14`;
- marked edges `1-6:12` and `2-3:34`;
- equal bad face `9-14:23` with endpoint types `123,123`.

The branch swap at `9-14` leaves one common Hamiltonian `H_14` component and the
same marked route

\[
(1,2)|(3,6).
\]

Both marked edges lie on the same outside arc.  This remains a counterexample
to the arbitrary-equal-face route-or-split theorem and is not reinterpreted as
productive.

---

## 6. Independent repair movie on stable edge `6-7`

The two marked edges cut the Hamiltonian channel into two actual marked arcs.
Stable edge

\[
6-7:23
\]

is a non-`H_14` connector between those two arcs.  Its endpoint triangle types
are

\[
123\quad\text{at }6,\qquad234\quad\text{at }7.
\]

The unique root opposite NNI is

\[
123+234\longrightarrow124+134,
\qquad23\longrightarrow14.
\]

Retain stable darts `1-6:12` and `7-12:34`.  Reattach the other two exterior
stable edges as

\[
5-6\longmapsto5-7,
\qquad
7-8\longmapsto6-8.
\]

The marked `H_14` terminal matching changes from

\[
(1,2)|(3,6)
\]

to

\[
(1,3)|(2,6).
\]

This was independently recomputed directly from the stable edge/root table.
Before and after the move `H_14` is one 14-edge component, but cutting the
marked edges gives the two displayed different ordered matchings.

Independent graph checks of the output give:

- connected;
- simple;
- cubic;
- bridgeless;
- girth five;
- cyclic edge-connectivity five.

Thus the corrected connector gives a genuine route exit.  The RL digest
`72e67089476af09b33daf91316b9b71b687f98291457e4514c8a384bba626ec0`
is retained only as a serialization label; the proof uses the displayed stable
darts and route computation.

---

## 7. Universal co-root theorem

### Theorem `FC-PURE-NNI-CO-ROOT-COMPONENT-CHAIN-ESCAPE`

Every complete category-safe fixed-order co-root prescribed-parent state has a
finite source-faithful same-order history ending in exactly one of:

1. a root flow on the literal prescribed-parent topology;
2. a cap-compatible route/profile state;
3. a named cyclic `2/3/4`-cut or bounded terminal.

The history uses:

- ordinary root NNIs and equal root branch swaps along one witnessed marked-arc
  component chain;
- at most one separating support-component switch after the chain has changed
  the terminal matching;
- the literal parent NNI or exact terminal consumer.

It uses no arbitrary-equal-face selection, unconditional `Xi` totality,
`2--0` cancellation, lower-order call, track-erasure progress, SCC distance or
retired mixed-order machinery.

---

## 8. Retained `Xi` status

The following audited `Xi` mathematics remains valid but noncontrolling for
totality:

- canonical frame and whole-component invariance;
- six strict distinct-neighbour rows;
- bad-free `124` route contradiction;
- strict arithmetic drop when a productive row is source-legally selected.

The arbitrary equal-neighbour totality conclusion and unconditional `N`-macro
bound remain withdrawn.
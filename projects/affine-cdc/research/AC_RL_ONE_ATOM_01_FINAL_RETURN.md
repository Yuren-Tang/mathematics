# AC-RL-ONE-ATOM-01 — final return

**Return class:** `[READY-FOR-PDL-ONE-ATOM-RECLOSURE AC-RL-ONE-ATOM-01]`  
**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Issue:** `Yuren-Tang/research-workbench#85`  
**Exact frozen issue start:** `research/affine-cdc-five-cdc-v1@02b37476198e9eaa2b4cd8d2a2edd76782bdcd49`  
**Live append-only epoch parent:** `16f599cefbf08bf89d78006d90598043b326afea`  
**Exact pre-return head:** `3dec0fb03db4be05c09e1eb05d770d4eb3687068`  
**Controlling defect boundary:** `audit/affine-cdc-five-cdc-v7-4-1-full-v1@1e7ee9d14608bfdd3524d588d96bfc3470654686`.

The final branch SHA is recorded in the issue return because a commit cannot
contain its own SHA.

---

## 1. Bounded epoch disposition

The focused theorem

\[
\boxed{\texttt{ONE-ATOM-COMPLETE-ENDPOINT-TOTALITY}}
\]

is closed at Research Lead authorial level.

Every complete labelled post-single-pop contextual state with at most one
standard co-root atom, one literal live prescribed parent and one finite stored
root-NNI prefix has a finite same-order source-faithful disposition to:

1. a complete root-valued crossed endpoint retaining every
   parent/cap/route/support/dart/prefix field; or
2. an exact current-descendant terminal.

No component-chain theorem, PDL integration theorem or old SCC/supplied-
comparison totality claim is used to construct the endpoint.

---

## 2. First task completed: exact interface and alphabet

The complete state tuple is

\[
(G,x,\mathfrak a,R^-,R^+,P,B,\kappa,\sigma,\mathcal D,\alpha,W),
\]

with literal labelled topology, atom edge, crossed root sheets, parent, cap,
route/profile, support transport, stable darts, ancestry and remaining source
word.

The legal post-pop alphabet is exhausted by:

- the standard single-pop atom collar;
- disjoint atom/source commuting square;
- identical-support crossed root resolution;
- one-vertex five-leaf overlap;
- good co-root relocation;
- one transient bad two-co-root row plus immediate root-lowering normalization;
- zero third-resolution immediate rootification;
- identity runs and six-port seams used only in final comparison compilation;
- exact route/cut/bounded terminal return.

The literal source word and local normalization word are separate.  A source
obligation is deleted only at its fixed other-order exit.

---

## 3. New source-prefix scheduler

Let:

- `L` be the number of remaining literal source-prefix obligations;
- `mu` be the distance, at most three, from the current atom edge to the fixed
  other-order exit edge in the active labelled five-leaf local cycle.

The well-founded rank is

\[
\boxed{(L,\mu)}
\]

in lexicographic order.

At fixed `L`:

1. the head source NNI is fixed;
2. its five stable exterior darts are fixed;
3. the entry atom edge and other-order exit are fixed;
4. one canonical shortest local path is fixed;
5. every persistent atom relocation lowers `mu`;
6. reaching the exit deletes the exact head source step and lowers `L`.

There is no local-rank reset while the same source obligation remains live.
The construction is induction on one already given finite source word, not
recurrence on the state graph.

---

## 4. Bounded local cycle theorem

For one shared-vertex overlap, the fixed conserved five-root boundary has root
topology sector exactly `C5` or `C6`.

A persistent standard co-root atom is a co-root-labelled edge of that cycle.

- `C5`: all five singular edges are co-root; diameter two.
- mixed `C6`: four singular edges are co-root and two are zero; diameter three.
- meeting a zero edge gives immediate root endpoint.
- all-zero `C6` and the empty sector cannot contain the input co-root atom.

Thus the fixed other-order exit is reached in at most three atom-edge advances,
unless root success or an exact terminal occurs first.

The finite certificate exhausts all `6240` conserved ordered boundary words and
has canonical digest

`6d51dab3f1fe4cf10411fdf8d7137e22d0d061b9ca0fac941ad922b673df04f3`.

---

## 5. Exact repair of the supplied-comparison quantifier

The retained v7.2 theorem has only the conditional form:

```text
given a finite normalized one-token comparison
    -> compile it to a root rectangle.
```

The new scheduler proves the missing existential statement:

```text
every complete post-pop atom state with its literal finite source word
    -> a finite normalized comparison or exact terminal.
```

Only after `(L,mu)` terminates is v7.2 invoked once to compile the comparison.
Track erasure is not target progress and does not lower the scheduler rank.

At `L=0`, the one nonbranching atom track has two standard endpoint collars.
Their crossed root-valued restrictions are attached as literal short
boundaries.  No non-root graph is used as a vertex of the final root history.

---

## 6. Literal operation-order and source fidelity

For an overlap of atom operation `A` with the next source NNI `f`, the local
comparison boundary is fixed as

```text
entry: A followed by f
exit : f followed by A'.
```

The coefficient table determines whether the exit is root, zero, co-root or an
exact terminal; it does not choose another topology.

The unique bad-overlap normalization is internal to the same five-dart carrier,
fixes all exterior darts and preserves the selected exit operation order.

Every successful exit retains:

- the literal prescribed parent and four active darts;
- cap vertices, cap edge and ordered cap darts;
- current route/profile;
- support transport;
- stable edge/dart identities;
- source-prefix ancestry.

---

## 7. The old four-port witness remains controlling negative evidence

For

```text
(12,34,13,24)
```

the prescribed `AB|CD` parent still has central co-root `Q5`; the crossed
topologies have roots `23` and `14`.

This packet does not claim rootification of that parent.  It constructs a
complete crossed root endpoint carrying the unchanged co-root parent field.
The downstream co-root row is the later consumer.

Therefore the witness still refutes:

```text
track rootification => prescribed-parent rootification.
```

It does not refute the new endpoint-totality theorem.

---

## 8. Exact terminal discipline

Every terminal is returned as the full tuple

\[
(H,\lambda?,B,\kappa,\mathcal C,\alpha).
\]

The scheduler introduces no new terminal type.  Route/profile changes,
`K_i`, separating channels, cyclic `2/3/4` cuts, loop/parallel/triangle/theta
and acyclic low-port outputs use only the existing current-descendant ledger.

No terminal is represented by an untyped word such as “separator”.

---

## 9. Controlling files

1. `ONE_ATOM_COMPLETE_STATE_INTERFACE_AND_QUANTIFIER_MAP_V1.md`;
2. `ONE_ATOM_PREFIX_DIRECTED_LOCAL_MACRO_CERTIFICATE_V1.md`;
3. `ONE_ATOM_OPERATION_ORDER_EXIT_AND_ENDPOINT_COLLAR_ADDENDUM_V1.md`;
4. `ONE_ATOM_COMPLETE_ENDPOINT_TOTALITY_THEOREM_V1.md`;
5. `ONE_ATOM_SOURCE_FIDELITY_AUDIT_AND_DOWNSTREAM_LEDGER_V1.md`;
6. `finite/one_atom_prefix_overlap_census_v1.py`;
7. this final return.

---

## 10. Prohibited machinery excluded

The controlling proof uses no:

- component-chain theorem;
- PDL integration or ordinary-induction closure;
- same-order root solubility;
- second cancellation;
- second lower-order call;
- generic root-flow/NNI connectivity;
- finite-state or SCC distance;
- supplied repeated-endpoint recurrence as totality;
- track erasure as prescribed-parent progress;
- arbitrary equal-face `Xi/Omega` selector;
- unrelated existential recolouring;
- support switch;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames.

The exact bounded `C5/C6` table is a local operation-order macro with a fixed
entry and fixed exit, not a general connectivity principle.

---

## 11. Exact downstream effect

At authorial level, the first missing #84 audit edge is repaired:

```text
single-pop root / one-atom / terminal
    -> complete crossed root endpoint or exact terminal.
```

The endpoint now has the exact type required by the already separated
root/co-root/zero parent-value table.

This epoch does not reopen or promote:

- the application-scoped component-chain proofs;
- co-root/zero parent realization;
- PDL prefix/induction integration;
- the cubic five-support theorem;
- the general-graph outer shell;
- the five-cycle-double-cover conclusion.

---

## 12. Required receiver work

PDL must independently reconstruct:

1. the exact post-pop complete state and source word;
2. all three support-intersection cases;
3. the operation-order definition of the exit atom edge;
4. the `C5/C6` singular-edge profile;
5. the finite certificate and digest;
6. immediate zero rootification;
7. bad-overlap normalization without exit drift;
8. strict `(L,mu)` descent;
9. the `L=0` endpoint-collar semantics;
10. final v7.2 comparison compilation;
11. cap/route/support/dart/parent/prefix fidelity;
12. exact terminal typing;
13. separation from downstream parent realization.

Then a fresh independent audit is required.  The `(12,34,13,24)` witness must
be included as mandatory negative evidence.

---

## 13. Assurance boundary

This is an AC-RL authorial repair packet for the one-atom endpoint frontier.
It is not an independently reconstructed or independently audited universal
proof.

No accepted cubic five-support theorem or five-CDC theorem is claimed.  No
Lean theorem, Curator/canonical movement, manuscript, release, tag, arXiv, DOI,
peer-review or publication status changes.

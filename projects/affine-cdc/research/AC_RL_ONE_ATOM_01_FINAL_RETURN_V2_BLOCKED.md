# AC-RL-ONE-ATOM-01 — corrected final return v2

**Return class:** `[BLOCKED-FRONTIER AC-RL-ONE-ATOM-01]`  
**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Issue:** `Yuren-Tang/research-workbench#85`  
**Exact frozen issue start:** `research/affine-cdc-five-cdc-v1@02b37476198e9eaa2b4cd8d2a2edd76782bdcd49`  
**Controlling audit boundary:** `audit/affine-cdc-five-cdc-v7-4-1-full-v1@1e7ee9d14608bfdd3524d588d96bfc3470654686`  
**Exact pre-return correction head:** `f4ee2ae9827feeec86a3651ae035195cb541d6ed`.

The final branch SHA is recorded in the issue return because a commit cannot
contain its own SHA.

---

## 1. Corrected disposition

`ONE-ATOM-COMPLETE-ENDPOINT-TOTALITY` remains unresolved.

The earlier append-only v1 return

`[READY-FOR-PDL-ONE-ATOM-RECLOSURE AC-RL-ONE-ATOM-01]`

is withdrawn as controlling authority.  Its prefix-directed construction fails
in the disjoint-support case before its claimed rank decreases.

This v2 return supersedes the v1 classification without deleting or rewriting
history.

---

## 2. Complete labelled obstruction to the proposed scheduler

Start with the labelled Heawood root flow and switch

```text
Z=1-2-11-10-5-4-13-14-1
```

by root `25`.  Exactly one standard co-root atom remains:

```text
1-14:1235=Q_4,
```

with crossed root restrictions `25` and `13`.

The next literal source NNI is the stable cell `5-6`.  In the original source
flow its current and literal predecessor sides both have central root `13`.
Its support `{5,6}` is disjoint from `{1,14}`.

Under the switched returned labels, the current `5-6` side remains root-valued,
but the same literal predecessor pairing has forced value

```text
12+35=23+15=1235=Q_4.
```

Crossing the source topology therefore creates a second persistent atom:

```text
1-14:Q_4,
5-6 :Q_4.
```

The output is connected, simple, cubic and bridgeless, with girth `5` and cyclic
edge-connectivity `5`.  No route/cut/bounded category is forced by the displayed
state.

Exact certificate:

- `ONE_ATOM_DISJOINT_PREFIX_STEP_DOUBLE_ATOM_COUNTERCERTIFICATE_V1.md`;
- `finite/one_atom_disjoint_double_atom_countercertificate_v1.py`;
- digest `dd4afccb8578e3ab8e48c33bddaad39e763f18dac398fe8e2d83e80ceeb57aea`.

---

## 3. Exact failure of `(L,mu)`

The proposed scheduler asserted that a disjoint atom/source pair forms a strict
commuting square and permits deletion of the head source step.

What disjointness actually proves is only equality of uncoloured operation
orders.  It does not preserve root admissibility of the literal predecessor
under an arbitrary returned flow.

In the certificate:

1. `L>0` and the head step is fixed;
2. the supports are disjoint;
3. the predecessor side is co-root-valued;
4. the output has two disjoint atoms;
5. `L` cannot be decreased within the one-atom state type;
6. the local rank `mu` belongs to no single five-leaf carrier containing both
   atoms.

Hence the rank is not total on the stated input class.

---

## 4. Strongest retained result

The bounded epoch does retain:

1. the exact complete post-pop state/interface normal form;
2. separation of literal source progress from local normalization;
3. identical-support immediate crossed-root endpoint;
4. the full one-shared-vertex five-leaf `C5/C6` macro;
5. immediate zero rootification inside that macro;
6. immediate normalization of the unique adjacent two-co-root transient;
7. the conditional disjoint commuting square when the literal predecessor is
   independently root-valued under the returned labels;
8. v7.2 run/seam compilation for an already supplied finite one-token
   comparison;
9. the complete source-dart, cap, route, support, parent and terminal typing
   requirements.

These statements do not imply universal endpoint totality.

---

## 5. Smallest exact missing implication

The remaining frontier is

`DISJOINT-SECOND-FAILURE-AVOIDANCE`:

> From one standard co-root atom and one disjoint literal next source NNI whose
> predecessor is zero/co-root-valued under the returned labels, obtain a finite
> source-faithful same-order crossed-root endpoint or exact terminal without
> recording two persistent atoms; or prove that genuine single-pop ancestry
> excludes the configuration.

No frozen source theorem supplies this implication.  The broad complete-state
formulation of the v1 packet is directly refuted by the Heawood certificate.

---

## 6. External-review handoff

The exact whole-core question, state alphabet, source movie, failed routes and
assurance boundary are recorded in

`AC_RL_ONE_ATOM_01_CORRECTION_AND_EXTERNAL_REVIEW_HANDOFF_V1.md`.

Per issue #85 hard stop, no further generalized internal potential, scheduler,
SCC route or two-atom confluence programme is opened in this epoch.

---

## 7. Downstream status

The controlling #84 dependency cut remains:

```text
one standard co-root atom after the single pop
    -/-> complete crossed root endpoint or exact terminal.
```

Therefore universal entry into the root/co-root/zero table remains unavailable.
Stored-prefix return, ordinary cubic induction, the cubic five-support theorem
and the five-CDC conclusion remain blocked.

The independently retained local arithmetic, terminal consumers,
application-scoped component-chain fibres and conditional outer shell are not
reopened or weakened.

---

## 8. No forbidden machinery and no surface expansion

This correction uses no component-chain argument, PDL integration, SCC distance,
generic NNI connectivity, supplied-comparison totality, second cancellation,
lower-order call, `Q_N/M_N/d_N`, nested bubbles or terminal frames.

No PDL, prior audit, Curator/canonical, `main`, Lean, manuscript, workflow,
release, tag, arXiv, DOI or publication surface is modified.

---

## 9. Final return

`[BLOCKED-FRONTIER AC-RL-ONE-ATOM-01]`.

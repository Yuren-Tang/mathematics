# AC-RL-PURE-NNI-02 return ledger

**Return class:** `[READY-FOR-PDL-REPAIR AC-RL-PURE-NNI-02]`  
**Exact branch:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact pre-ledger theorem head:** `f77708fa203919ce5a7a0e57564eac234285c7b5`

## Controlling theorem files

1. `DDD_LOCK_H14_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md`;
2. `PURE_NNI_ALL_INDEX_AMBIENT_ESCAPE_MASTER_THEOREM_V3.md`;
3. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V8_SWITCH_INVARIANT_REPAIR.md`.

## Exact result

The former productive-row plateau blocker is replaced by a stronger fixed-channel theorem.

Choose one physical locked rescue channel and transport it to `H_14`. The complete-state rank

\[
\Xi=\sum_v\xi(\Delta_v),
\]

with

\[
\begin{array}{c|cccccccccc}
T&123&124&125&134&135&145&234&235&245&345\\
\hline
\xi(T)&1&0&1&0&0&0&1&2&1&0
\end{array}
\]

is invariant under every nonexit closed switch of that entire physical channel component, including all exterior vertices and the frame transport caused by common-component migration.

Every lock contains one of `123,234,125,245`. At its internal root `23` or `25`, a distinct neighbour gives one of six ordinary root NNIs with `Delta Xi=-2`; an equal neighbour gives zero or one root branch swap, one complete component switch and one of the same NNIs, also with `Delta Xi=-2`. If the four types are absent, the locked `H_14` path from boundary root `12` is trapped in type `124` and roots `12,24`, so it cannot reach a boundary root `34`.

Thus every nonabsorbing macro strictly lowers a nonnegative integer, and no closed nonterminal all-index SCC exists.

## Closed obligations

- `DDD-PLATEAU-PRODUCTIVE-ROW-ESCAPE`;
- `FC-PURE-NNI-ESCAPE`;
- `FC-PURE-NNI-ALL-INDEX-AMBIENT-ESCAPE`;
- `AC-PD-V7.2-PURE-NNI-TARGET-REINSERTION` at RL authorial level.

## Exact exclusion of prior defects

- no local two-vertex `Omega` shift is asserted;
- no orbit-minimum comparison is used;
- no zero insertion or co-root track occurs in the lock escape;
- no `2--0` cancellation or lower-order call occurs;
- the scope correction remains controlling against the old proof attempt.

## PDL reconstruction surface

PDL must independently reconstruct the transported frame, component-wise rank invariance, six distinct rows, four equal rows, route contradiction, all-index SCC exclusion and v7.2 integration.

No established five-support/five-CDC theorem, independent assurance, Lean theorem, canonical movement, manuscript, release, tag, arXiv, DOI or publication status follows from this authorial return.

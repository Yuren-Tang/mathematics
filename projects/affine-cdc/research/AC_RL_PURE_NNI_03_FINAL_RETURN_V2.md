# AC-RL-PURE-NNI-03 final return v2

**Return class:** `[READY-FOR-PDL-ZERO-REPAIR AC-RL-PURE-NNI-03]`  
**Exact branch:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact pre-return head:** `211d23d1eca0c1c17cc1a3a93df2ca7f2261687a`  
**Receiver:** `AffineCDC — Director` (`AC-DIR`), then standing `AC-PDL`.

This v2 return incorporates the independent `Xi` audit which arrived during
the zero-parent epoch and records the final scope-correct controlling proof.

---

## 1. Mandatory Heawood movie

Verified source-faithfully:

```text
remote equal-face root branch swap at 13--14
    12--13 -> 12--14
    1--14  -> 1--13

physical H_35 split
    Z_0 = 1-2-3-4-13-1
    Z_1 = 5-6-7-12-14-9-10-5

switch Z_0 by 35
    active word (15,13,25,23)

literal AB|CD parent NNI
    central root 35.
```

Every root triangle, stable edge/dart, cap incidence, route update and category
was recomputed.  The initial, post-remote and final graphs are connected,
simple and bridgeless with cyclic edge-connectivity `6,5,5`.

Controlling model certificate:

`ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`.

A second independent standard-library reproducer and certificate were appended
with digest

`d22bce5ea026f5c461575f05ed81ed6e68c71a0c3c459ce6ada93d96f458422e`.

---

## 2. Universal zero-parent theorem

The theorem

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}}
\]

is closed at Research Lead authorial level by physical component-chain
contraction.

Normalize the active word to `(13,13,23,23)` and fix physical `H_35`.  Remove
the active two-vertex cell.  On the prime nonterminal branch the complementary
carrier is connected and its residual full-lock matching is `AB|CD`.

Form the quotient whose nodes are:

- the physical `H_35` terminal paths/cycles;
- individual channel-inactive `124` vertices.

Choose a shortest physical quotient chain from terminal path `A--B` to terminal
path `C--D`.  Each nonterminal step contracts its first witnessed physical edge:

- active/inactive root NNI absorbs one `124` vertex;
- active/active root NNI or source-legal equal branch swap merges one intervening
  channel component.

The rank `Lambda_35`, the actual chain length, decreases by one.  The final
connector has endpoints on different distinguished paths by construction, so it
changes the terminal matching to `AC|BD` or `AD|BC`.  Align the active crossed
sheet, switch one closed `H_35` component, and perform the literal parent NNI
with central root `35`.

Every execution ends in:

1. root flow on the literal prescribed parent;
2. cap-compatible route/profile output;
3. named cyclic `2/3/4`-cut or bounded terminal.

---

## 3. Independent-audit correction

Audit `AC-AUDIT-XI-01` at

`audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`

correctly refuted the old universal arbitrary-equal-face route-or-split lemma.
The missing datum was the placement of marked terminals on the outside channel
arcs.

The correction is fully consumed:

- `Xi` frame/invariance, six distinct rows and bad-free contradiction are
  retained as arithmetic;
- arbitrary equal-face selection and unconditional `Xi` totality are withdrawn;
- the same physical component-chain theorem repairs the co-root marked-arc case;
- the audit witness is explicitly repaired by the stable edge `6--7:23` NNI
  `123+234 -> 124+134`, changing the marked route.

The zero-parent `S_3` orbit rank appended during this epoch has likewise been
scope-corrected: its weights, switch invariance and twelve distinct rows are
valid arithmetic, but its anchored equal-face source-selection claim is not
controlling.  Universal totality comes only from the path-aware component-chain
theorem.

---

## 4. Equality arithmetic improvements

Two finite supplements are available:

1. preferred fixed-frame rank
   
   \[
   Z=2N_1+N_2,
   \]
   
   invariant under complementary support transpositions and tie-free on all
   fifteen distinct root-NNI involutions; digest
   `0b40c884721af76545e1bcb6e08085b6c4f0259e0f0770d1310cba41efc68b09`;
2. supplementary `Sym(U)` orbit rank, independently checking twelve rows;
   digest
   `d22bce5ea026f5c461575f05ed81ed6e68c71a0c3c459ce6ada93d96f458422e`.

Neither rank is used to infer source legality of an arbitrary equal face.

---

## 5. Exact inverse-parent table effect

At Research Lead authorial level:

| forced parent value | controlling disposition |
|---|---|
| root | literal stored parent NNI |
| co-root | fixed-channel marked-arc component-chain escape |
| zero | fixed-channel terminal-path component-chain escape |

Thus the complete prescribed-parent inverse table and the v7.3 ordinary
strong-induction DAG are restored as authorial candidates.

The controlling final DAG is

`ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V10_FINAL_CHAIN_CONTROL.md`.

---

## 6. Prohibited machinery excluded

The controlling proof uses no:

- fixed-order `2--0` cancellation;
- lower-order root-flow call during fixed-order return;
- other crossed NNI as a substitute for target progress;
- track erasure or periodic crosscut as target progress;
- generic root/NNI connectivity;
- finite-state or SCC distance;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames.

The optional crossed NNI occurs only after the physical outside matching has
already changed.

---

## 7. Controlling files

1. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
2. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
3. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
4. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
5. `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`;
6. `ZERO_PARENT_S3_RANK_SCOPE_CORRECTION_AND_CHAIN_CONTROL_V1.md`;
7. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V10_FINAL_CHAIN_CONTROL.md`;
8. this final return.

Supplementary finite/model files remain available but do not alter this order.

---

## 8. Required PDL work

PDL must independently reconstruct:

1. category-safe complementary-carrier connectivity;
2. fixed-channel parity and unique inactive triangle;
3. inactive-vertex absorption;
4. active-component merger and equal-branch source legality;
5. inheritance of every stable quotient-path edge;
6. strict physical chain-length descent;
7. final terminal-matching/marked-route change;
8. legal support switch and literal parent NNI;
9. cap, route, category, dart and stored-prefix fidelity;
10. complete inverse-parent and ordinary-induction integration.

Requested successful PDL return:

`[READY-FOR-INDEPENDENT-V7.4-AUDIT — COMPONENT-CHAIN ZERO/CO-ROOT REPAIR]`.

Otherwise return the smallest exact obstruction.

---

## 9. Assurance boundary

This is a complete Research Lead authorial repair packet, not an independently
accepted five-support/five-CDC proof.  New PDL reconstruction and a fresh
independent audit are required.

No Lean theorem, Curator/canonical movement, manuscript, release, tag, arXiv,
DOI, peer-review, publication or public-theorem status is claimed.

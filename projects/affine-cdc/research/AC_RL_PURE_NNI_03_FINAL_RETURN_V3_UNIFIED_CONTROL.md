# AC-RL-PURE-NNI-03 — final return v3, unified control

**Return class:** `[READY-FOR-PDL-ZERO-REPAIR AC-RL-PURE-NNI-03]`  
**Exact branch:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact pre-return head:** `ffb2936755e47be0c91ee2c19ad0a6b761514ffc`  
**Receiver:** `AffineCDC — Director` (`AC-DIR`), then standing `AC-PDL`.

This v3 ledger is the final append-only Research Lead return for issue #77.  It
supersedes earlier issue-#77 return ledgers only as an exact-head and control
statement.  It incorporates the independent `Xi` audit correction, the
component-chain universal proof, the mandatory Heawood source movie, and the
later compatible-Euler cross-check without assigning two controlling theorem
authorities.

---

## 1. Mandatory Heawood movie — verified

The candidate source movie is correct, with persistent edge/dart identities:

```text
remote equal-face root branch swap at 13--14
    e12_13: 12--13 -> 12--14
    e1_14 : 1--14  -> 1--13
    e13_14 remains 13--14:12

physical H_35 split
    Z_0 = 1-2-3-4-13-1
    Z_1 = 5-6-7-12-14-9-10-5

switch Z_0 by 35
    active word (13,13,23,23) -> (15,13,25,23)
    current central root remains 12

literal stored AB|CD parent NNI
    central root 35.
```

Every root triangle, persistent dart, cap incidence, support-component field,
route/profile update and category test was recomputed.  The original,
post-remote and final-parent uncoloured topologies are connected, simple and
bridgeless, with cyclic edge-connectivity

```text
6, 5, 5.
```

The cap dart at vertex `12` is fixed; only the opposite local endpoint of its
persistent incident edge moves from `13` to `14`.

Controlling and independent certificates:

- `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
- `HEAWOOD_ZERO_PARENT_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
- `finite/heawood_zero_parent_escape_certificate_v1.py`.

The latter certificate has canonical digest

`db160ee18300ead7aaf28c56cf83be1068c243b1e51aeabfc896f74c1615b14b`.

---

## 2. Universal theorem

The exact missing theorem

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}}
\]

is closed at Research Lead authorial level.

Normalize

\[
(A,B,C,D)=(13,13,23,23)
\]

with crossed central root `12` and literal parent `AB|CD` of value zero.  Fix
physical `H_35` and remove the active two-vertex cell.

On the nonterminal prime branch the complementary carrier is connected.  If its
physical `H_35` terminal matching is crossed, align the active crossed sheet,
switch one closed component, and perform the literal parent NNI with central
root `35`.

In the residual full lock the outside matching is `AB|CD`, with distinguished
terminal paths `A--B` and `C--D`.  Form the connected physical quotient whose
nodes are:

- nontrivial `H_35` paths and cycles;
- individual channel-inactive `124` vertices;

and whose edges are exact persistent non-`H_35` connector edges.  Choose a
shortest physical chain between the two distinguished terminal paths.

Every nonterminal step contracts its first witnessed chain edge:

1. an active--inactive root NNI absorbs one `124` vertex;
2. an active--active root NNI or source-legal equal branch swap merges one
   intervening channel component;
3. the final connector has endpoints on different distinguished paths and
   changes the terminal matching to `AC|BD` or `AD|BC`.

Then align the crossed sheet, switch one separated closed `H_35` component and
perform the literal parent NNI.  The strict rank is the displayed physical
chain length `Lambda_35`; it decreases by one per nonterminal connector move.

Every execution ends in exactly one of:

1. a root flow on the literal prescribed-parent topology;
2. a cap-compatible route/profile output;
3. a named cyclic `2/3/4`-cut or bounded terminal.

The source movie is same-order and root-valued throughout.  No actual equality
`2--0` cancellation and no lower-order call occurs.

Controlling universal files:

1. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
2. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`.

---

## 3. Material independent-audit correction

The independent audit

`Yuren-Tang/mathematics:audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`

correctly refuted the former arbitrary-equal-face route-or-one-sided-component
lemma.  Both marked edges may lie on the same outside channel arc, so both root
placements can remain one common component with the same marked route.

Therefore:

- `Xi` frame transport, whole-component invariance, six distinct rows and the
  bad-free route calculation remain valid;
- arbitrary equal-neighbour selection and unconditional `Xi` totality are
  withdrawn;
- the PDL matching-only equal-neighbour reconstruction at `fee97446...` does not
  certify the complete inverse-parent return;
- the physical marked-arc component-chain theorem now controls the co-root
  sector as well.

The audit witness is explicitly repaired by the stable connector
`6--7:23` and the ordinary root NNI

\[
123+234\longrightarrow124+134,
\]

which changes the marked route instead of selecting the unproductive same-arc
equal face.

Controlling correction:

`CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`.

---

## 4. Equality-rank and compatible-Euler supplements

The following remain useful but are not the controlling totality proof:

- `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`;
- the scope-corrected `S_3` rank packet;
- `ZERO_PARENT_H35_COMPATIBLE_EULER_ESCAPE_THEOREM_V1.md`;
- `finite/zero_parent_h35_rank_table_v1.py`.

The compatible-Euler proof supplies an independent global transition argument:
a `tau_35`-invariant rank descends to a persistent four-regular transition
quotient, then a compatible circuit assignment chooses only permitted root
bitransitions and produces the active channel split.  It does not reuse the
false arbitrary-equal-face lemma, but its assurance surface is larger than the
direct shortest-chain construction; it is therefore a cross-check and fallback,
not the PDL control route.

Its finite rank-table digest is

`aa6446dd5a01f8d4048807ff81b5f596c148f5b92224f529220277cf862e6eff`.

Exact scope and supersession are fixed by

`AC_RL_PURE_NNI_03_UNIFIED_CONTROL_AND_SUPPLEMENT_SCOPE_V1.md`.

---

## 5. Exact inverse-parent table effect

At Research Lead authorial level:

| forced parent value | controlling same-order disposition |
|---|---|
| root | literal stored parent NNI |
| co-root | fixed-channel marked-arc component-chain escape |
| zero | fixed-channel terminal-path component-chain escape |

Thus the complete root/co-root/zero inverse-parent table is restored as an
**authorial candidate**, and the ordinary-induction DAG may be returned to PDL
for new reconstruction.

Controlling DAG:

`ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V10_FINAL_CHAIN_CONTROL.md`.

---

## 6. Exact controlling order

1. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
2. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
3. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
4. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
5. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V10_FINAL_CHAIN_CONTROL.md`;
6. `AC_RL_PURE_NNI_03_UNIFIED_CONTROL_AND_SUPPLEMENT_SCOPE_V1.md`;
7. this final return.

Supplementary files remain append-only historical and mathematical evidence;
they do not change this order.

---

## 7. Required PDL reconstruction

PDL must independently reconstruct and line-check:

1. category-safe complementary-carrier connectivity or exact low-port output;
2. fixed-channel parity and the unique inactive triangle;
3. all active--inactive absorption rows;
4. active-component merger, including equal-branch source legality;
5. inheritance of every persistent later quotient-path edge after each NNI;
6. strict physical chain-length descent;
7. the final connector's terminal-matching or marked-route change;
8. crossed-sheet alignment, legal support switch and literal parent NNI;
9. every cap, dart, route/profile, category and stored-prefix identity;
10. co-root audit correction and the complete inverse-parent table;
11. ordinary strong-induction and terminal-ledger integration;
12. a fresh independent audit after reconstruction.

Requested successful PDL return:

`[READY-FOR-INDEPENDENT-V7.4-AUDIT — COMPONENT-CHAIN ZERO/CO-ROOT REPAIR]`.

Otherwise PDL should return the smallest exact obstruction.

---

## 8. Prohibited machinery excluded

The controlling proof uses no:

- fixed-order equality `2--0` cancellation;
- lower-order root-flow call during fixed-order return;
- other crossed NNI as a substitute for target progress;
- track erasure or periodic crosscut as target progress;
- generic root/NNI connectivity;
- finite-state or SCC distance;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames;
- quarantined `Omega` orbit-minimum argument;
- arbitrary equal-face productivity assertion.

---

## 9. Assurance boundary

This is a complete Research Lead authorial repair packet.  It is not an
independently reconstructed or independently audited universal proof.

No established cubic five-support theorem or universal five-CDC theorem is
claimed.  No Lean theorem, Curator/canonical movement, manuscript, release,
tag, arXiv, DOI, peer-review, publication or public-theorem status changes.

# AC-RL-PURE-NNI-03 return ledger

**Return class:** `[READY-FOR-PDL-ZERO-REPAIR AC-RL-PURE-NNI-03]`  
**Exact branch:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact pre-ledger theorem head:** `09549b9b06e86908fc2b7c7b0cea97d91ede9e51`

---

## 1. Exact inputs consumed

- issue `research-workbench#77`;
- standing zero-parent retarget in issue `#61`;
- PDL v7.3 return `proof-development/affine-cdc-rigour-v1@fee97446ee8b99f07740f394e99ef4a2ecc3e40e`;
- frozen RL start `212d789a5967813e7277fb3e269060926c99cb0e`;
- independent co-root audit `audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`.

The audit returned during this epoch and was consumed because it exposed a literal shared-interface contradiction to the prior co-root equal-neighbour theorem.

---

## 2. Controlling appended files

1. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
2. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
3. `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`;
4. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
5. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
6. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V9_COMPONENT_CHAIN_REPAIR.md`;
7. this ledger.

---

## 3. Mandatory Heawood movie

The issue-#77 source movie is verified exactly:

```text
remote root branch swap at stable edge 13-14
    12-13 -> 12-14
    1-14  -> 1-13

H_35 split
    Z_0 = 1-2-3-4-13-1
    Z_1 = 5-6-7-12-14-9-10-5

switch Z_0 by 35
    active word (15,13,25,23)

literal AB|CD parent NNI
    central root 35.
```

All vertex equations, cap identities, stable darts and graph categories were recomputed.  Cyclic edge-connectivity is `6,5,5` on the initial, post-remote-NNI and final parent topologies.

State digests:

- `S_0`: `4bfadd66ae43ed73c0cb339524884d295264b5468794406412770788993c799d`;
- `S_1`: `a7f4f8c0ff4e75310517d4d61776d17633022463a0a5b8fe07d81777d127a543`;
- `S_2`: `c5a50827fd01e692ed56d5e2e8c82f0391018cff2c969c13e15b315edf618d9d`;
- `S_3`: `18526bf3f8736cae7c298305a9d9f3da49bac4be18828a40ac1368f4070843d7`.

---

## 4. Universal zero-parent theorem

Normalize

\[
(A,B,C,D)=(13,13,23,23),
\qquad h=35.
\]

After deleting the active two-vertex cell, a category-safe nonterminal complementary carrier is connected.  In a full zero lock its `H_35` terminal matching is `AB|CD`.

Form the physical quotient whose nodes are:

- `H_35` path/cycle components;
- individual inactive `124` vertices.

A shortest witnessed component chain joins path `A--B` to path `C--D`.

- one active/inactive root NNI absorbs one `124` vertex;
- one active/active root NNI or equal branch swap absorbs one intervening channel cycle;
- the final connector changes the outside terminal matching to a crossed matching.

The chain length falls by one at every nonterminal step.  Align the active crossed sheet, switch one closed `H_35` component, and perform the literal parent NNI with central root `35`.

Therefore

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}}
\]

is closed at RL authorial level.

---

## 5. Equality arithmetic supplement

The fixed-frame complete-graph rank

\[
Z=2N_1+N_2
\]

is invariant under support switches inside `\{3,4,5\}` and gives a unique decreasing direction to every distinct root-NNI involution.  The fifteen-row digest is

`0b40c884721af76545e1bcb6e08085b6c4f0259e0f0770d1310cba41efc68b09`.

This rank verifies strict local arithmetic but is not used to infer productivity of an arbitrary remote equal face.

---

## 6. Independent-audit correction and co-root repair

Audit `AC-AUDIT-XI-01` correctly refuted the old arbitrary equal-face route-or-split lemma.  The previous v8 claim that `Xi` alone provided totality is withdrawn.

Retained:

- transported frame;
- whole-component `Xi` invariance;
- six strict rows;
- bad-free route contradiction;
- complete move contracts.

New totality provider:

- cut the two marked `H_h` edges;
- contract a physical component chain between the two marked arcs;
- the final connector gives route change or separation.

In the exact audit witness, stable edge `6-7` gives the repair NNI

\[
123+234\to124+134,
\qquad23\to14,
\]

changing marked route `(1,2)|(3,6)` to `(1,3)|(2,6)`.  Digest:

`72e67089476af09b33daf91316b9b71b687f98291457e4514c8a384bba626ec0`.

Thus the co-root row is also repaired at RL authorial level, by a theorem logically different from the audited `Xi` totality claim.

---

## 7. Exact closed obligations at RL authorial level

- `FC-PURE-NNI-ZERO-PARENT-ESCAPE`;
- co-root same-arc equal-neighbour repair;
- `FC-PURE-NNI-CO-ROOT-COMPONENT-CHAIN-ESCAPE`;
- the complete prescribed-parent inverse table;
- the v7.3 ordinary-induction DAG as an authorial candidate.

---

## 8. Exact correction boundary

Not controlling:

- arbitrary remote equal-face route-or-split;
- `Xi` as an unconditional source selector;
- the old unconditional at-most-`N` Xi-macro bound;
- the quarantined `Omega` proof.

Controlling:

- physical component-chain source selection;
- explicit root NNI contraction of inactive vertices and channel cycles;
- final terminal-matching change.

---

## 9. Prohibited machinery not used

No:

- `2--0` cancellation;
- lower-order root-flow call during fixed-order return;
- other crossed NNI as a substitute for progress;
- track erasure as target progress;
- finiteness or SCC distance;
- generic root/NNI connectivity;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames.

The optional active crossed NNI is used only after the outside matching has already changed, to align the exposed separating components.

---

## 10. Required receiver work

PDL must independently reconstruct:

1. category-safe carrier connectivity;
2. fixed-channel parity and unique inactive triangle;
3. active/inactive absorption;
4. active/active component contraction;
5. inherited physical quotient path and strict chain-length rank;
6. final terminal-matching change;
7. zero-parent switch and literal parent NNI;
8. co-root marked-arc corollary and audit-witness repair;
9. cap, route, category, dart and stored-prefix fidelity;
10. end-to-end v7.3 ordinary-induction integration.

Then a new independent audit is required.

---

## 11. Assurance boundary

This return is an RL authorial theorem packet, not an independently accepted five-support/five-CDC proof.  No Lean theorem, Curator/canonical movement, manuscript, release, tag, arXiv, DOI, peer-review or publication status is claimed.
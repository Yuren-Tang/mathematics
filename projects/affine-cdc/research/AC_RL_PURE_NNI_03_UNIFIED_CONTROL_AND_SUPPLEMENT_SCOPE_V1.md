# AC-RL-PURE-NNI-03 — unified control and supplement scope

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workstream:** `AC-RL-PURE-NNI-03`  
**Exact parent head:** `79257ceb5b5b0c630cf66d9681fcb3eccddd48f6`  
**Classification:** `CONTROL UNIFIED / COMPONENT-CHAIN TOTALITY CONTROLLING / COMPATIBLE-EULER PROOF SUPPLEMENTARY`.

This note resolves the append-only convergence of two valid Research Lead proof
routes written during the same zero-parent epoch.  It establishes one theorem
authority and one exact downstream reconstruction order without deleting or
rewriting any earlier source.

---

## 1. Controlling universal theorem

The controlling source-selection and totality theorem is

`FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`,

integrated for the zero fibre by

`PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`.

For the canonical word

\[
(A,B,C,D)=(13,13,23,23)
\]

it fixes physical `H_35`, removes the active cell, and records the actual
terminal paths, closed channel cycles, inactive `124` vertices and persistent
nonchannel connector edges.  A shortest physical quotient chain between the
`A--B` and `C--D` terminal paths is contracted one witnessed edge at a time:

1. an active--inactive root NNI absorbs one `124` vertex;
2. an active--active root NNI or source-legal equal branch swap merges one
   intervening channel component;
3. the final physical connector joins the two distinguished terminal paths and
   changes their endpoint matching;
4. zero or one active crossed root NNI aligns the crossed sheet;
5. one closed `H_35` switch makes the literal parent root-valued;
6. one ordinary NNI realizes the exact stored parent with central root `35`.

The strict rank is the length of this displayed physical component chain.  It
is neither a distance in a finite state graph nor an inference from recurrence.

---

## 2. Mandatory correction inherited from the independent `Xi` audit

The independent audit

`audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`

correctly refuted the old claim that an arbitrary equal bad face necessarily
gives a marked-route change or a one-sided fixed-channel component after zero or
one root branch swap.  The omitted datum was the placement of the marked
terminals on the outside channel arcs.

Consequently:

- the transported `Xi` frame, whole-component invariance, six distinct rows and
  bad-free route calculation remain valid arithmetic;
- arbitrary equal-neighbour source selection and unconditional `Xi` totality are
  withdrawn;
- the PDL matching-only equal-neighbour reconstruction at `fee97446...` is not a
  complete totality proof;
- both the co-root marked-arc sector and the zero-parent terminal-path sector are
  controlled by the path-aware physical component-chain theorem.

The controlling correction is

`CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`.

---

## 3. Status of the compatible-Euler proof

`ZERO_PARENT_H35_COMPATIBLE_EULER_ESCAPE_THEOREM_V1.md` is retained as an
independent supplementary proof candidate and cross-check.  It is not the
controlling proof for PDL reconstruction.

Its construction is materially different from the false arbitrary-equal-face
lemma:

1. a complete-graph `tau_35`-invariant rank descends through nine explicit
   distinct root-NNI rows;
2. the plateau is contracted along the full persistent non-`H_35` matching to a
   four-regular transition graph;
3. one forbidden bitransition per matching node is recorded explicitly;
4. a compatible circuit argument chooses all remote root transitions globally;
5. only those permitted root transitions are lifted back to persistent source
   darts.

Thus it does not infer productivity from an arbitrary equal face.  Nevertheless
its global transition assignment and disconnected-split lifting introduce a
larger assurance surface than the direct shortest-chain theorem.  It therefore
serves as:

- an independent structural check of universal zero-parent escape;
- a possible alternative if PDL finds a defect in the shortest-chain
  continuation interface;
- a finite-table and quotient-topology supplement.

It does not supersede the component-chain theorem or alter the requested PDL
return.

The companion finite table

`finite/zero_parent_h35_rank_table_v1.py`

has canonical digest

`aa6446dd5a01f8d4048807ff81b5f596c148f5b92224f529220277cf862e6eff`.

---

## 4. Heawood source certificates

The issue-#77 model movie is independently reconstructed by both:

1. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
2. `HEAWOOD_ZERO_PARENT_ESCAPE_SOURCE_CERTIFICATE_V1.md` with companion script
   `finite/heawood_zero_parent_escape_certificate_v1.py`.

They agree on the persistent source movie:

```text
remote equal-face branch swap at 13--14
 -> H_35 components
    Z_0 = 1-2-3-4-13-1
    Z_1 = 5-6-7-12-14-9-10-5
 -> switch Z_0 by 35
 -> active word (15,13,25,23)
 -> literal AB|CD parent NNI with central root 35.
```

They also agree that the relevant uncoloured topologies are connected, simple
and bridgeless, with cyclic edge-connectivity `6,5,5` for the original,
post-remote and final-parent graphs.  The second certificate makes explicit that
the cap dart at vertex `12` is fixed while the opposite endpoint of its
persistent incident edge is reattached.

Different canonical JSON encodings give different digests; this is not a
mathematical discrepancy.

---

## 5. Controlling proof and supersession order

The final Research Lead control order for `AC-RL-PURE-NNI-03` is:

1. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
2. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
3. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
4. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md` and the independent
   Heawood certificate above;
5. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V10_FINAL_CHAIN_CONTROL.md`;
6. this unified scope note.

Retained as arithmetic or supplementary:

- fixed-channel `Xi` frame, invariance, distinct rows and bad-free calculation;
- `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`;
- the scope-corrected `S_3` rank packet;
- `ZERO_PARENT_H35_COMPATIBLE_EULER_ESCAPE_THEOREM_V1.md`;
- their finite certificates.

Withdrawn as universal totality:

- arbitrary equal-face route-or-split;
- unconditional `Xi` equal-neighbour descent;
- anchored equal-face `S_3` source selection without full terminal-arc data;
- any return ledger predating the independent-audit correction when read as a
  complete inverse-parent proof.

---

## 6. Exact theorem status

At Research Lead authorial level the inverse-parent table is:

| forced parent value | controlling disposition |
|---|---|
| root | literal stored parent NNI |
| co-root | fixed-channel marked-arc component-chain escape |
| zero | fixed-channel terminal-path component-chain escape |

Therefore

`FC-PURE-NNI-ZERO-PARENT-ESCAPE`

is closed at RL authorial level, and the complete pure fixed-order
prescribed-parent return is again an authorial candidate.

This does not restore the former PDL packet automatically.  PDL must
independently reconstruct the new component-chain theorem and the audit
correction, followed by a fresh independent review.

---

## 7. Prohibited machinery and assurance boundary

The controlling proof uses no fixed-order `2--0` cancellation, lower-order call,
other crossed NNI as a substitute for target progress, track erasure, periodic
crosscut, finite-state/SCC distance, `Q_N`, `M_N`, `d_N`, nested bubble or
terminal frame.

No established universal five-support or five-CDC theorem is claimed.  No Lean,
Curator/canonical, manuscript, release, tag, arXiv, DOI, peer-review,
publication or public-theorem status changes.

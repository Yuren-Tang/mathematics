# Five-support one-cross programme after cancellation re-entry correction

## Research Lead controlling global surface v7 — gap quarantine

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `30d34eb09b8fa165ff2f8e66a0a12b460a0a3688`.

**Supersedes for controlling status and global closure:**

- `FIVE_SUPPORT_GLOBAL_ONE_CROSS_CLOSURE_V6.md`.

**Controlling correction:**

- `CANCELLATION_EXIT_REENTRY_WELL_FOUNDEDNESS_GAP_V1.md`.

**Classification:**

\[
\boxed{
\text{ONE-CROSS LOCAL ARCHITECTURE RETAINED}
/
\text{MATERIAL R2 WELL-FOUNDEDNESS GAP}
/
\text{NO ESTABLISHED FIVE-CDC PROOF}.
}
\]

---

## 1. Retained one-cross architecture

For a connected loopless bridgeless cubic multigraph:

1. the no-simple-edge base is theta;
2. every simple edge has at least one connected loopless bridgeless smaller cross closure;
3. a root flow on that selected closure gives the intersecting/equality/DDD boundary trichotomy;
4. the six controlling route rows reduce blocked equality/DDD states to one oriented full-channel singular lock;
5. positive additive Morse weights produce a finite current-flow history of root flips and equal-face cancellations;
6. first failed inverse transfer creates exactly one zero/co-root edge;
7. local critical overlaps do not create an unbounded defect alphabet;
8. the full-state forced-backbone, orientation and actual `C6/C8` source packets remain the proposed same-order recurrence mechanism.

These statements remain authorial local packages and PDL reconstruction targets. The structural reduction `R1` is not withdrawn.

---

## 2. Withdrawn global arrow

The following arrow is not presently proved:

\[
\boxed{
\text{equal-face cancellation}
\Longrightarrow
\text{completed lower-order contextual exit}.}
\]

A lower-order root flow on the cancelled graph may fail to reinsert the pair root-valuedly. The inverse insertion may recreate:

- quadruple equality (`a=b`);
- a doubled-disjoint co-root atom (`a\cap b=\varnothing`).

Therefore cancellation may return to the same target order with one token. Target-order decrease of the subcall is not a strict measure for the complete cancel--solve--reinsert macro-transition.

---

## 3. Consequences for prior controlling claims

The following are now conditional, not closed theorems:

- `SINGULAR_ROOT_TRIALITY_CONFLUENCE_MASTER_THEOREM_V3.md` as an unconditional contextual-transfer theorem;
- `ONE_CROSS_CAP_RESTORATION_THEOREM_V1.md`;
- `SIMPLE_EDGE_FIVE_SUPPORT_REDUCIBILITY_V1.md` in its extension half `R2`;
- cubic induction and the general five-support closure in global v6.

Their local hypotheses and conditional implications may still be used with the missing macro-return theorem made explicit.

---

## 4. Correct proof-DAG state

### R1 — structural reducibility

**State:** authorial theorem / PDL reconstruction exists.

### R2.1 — boundary and route certificate

**State:** retained; PDL complete draft.

### R2.2 — current-flow singular Morse descent

**State:** retained; PDL complete draft.

### R2.3 — first failure and local confluence

**State:** retained; PDL complete draft.

### R2.4--R2.6 — full-state backbone, orientation and actual even-core source movie

**State:** active PDL reconstruction / attack-sensitive authorial inputs.

### R2.7 — global contextual return

**State:**

\[
\boxed{
\text{BLOCKED-FRONTIER: CANCELLATION RE-ENTRY / WELL-FOUNDEDNESS}.}
\]

---

## 5. Repair routes

Any one complete repair would restore the global programme.

1. **History-coherent terminal selection:** choose a cap-compatible terminal flow that reverses the exact stored history root-valuedly.
2. **Strict macro-return rank:** prove cancel--solve--reinsert either succeeds or returns to a strictly smaller same-order state.
3. **Inverse-weld flow selection:** choose a lower-order flow whose reconnecting roots are distinct intersecting.
4. **Same-order root-state exit:** strengthen the actual `C6/C8` movie so that the recurrent token SCC reaches a complete root state before cancellation; then cancellation is unnecessary in the no-sink proof.

Repair 4 is the first RL priority because it may reuse the existing relative root-star movie without a new recursive theorem.

---

## 6. Projectivity side programme

The following remain exact noncontrolling interpretations/certificates:

- `ROOT_SUPPORT_TRANSPORT_PROJECTIVITY_GROUPOID_V1.md`;
- `RELATIVE_PROJECTIVITY_HEXAGON_2_COMPLEX_V1.md`;
- `DIRECTED_HEXAGON_FLAG_SIMPLE_TRANSITIVITY_V1.md`.

The strict local directed hexagon-lift target is refuted by:

- `DIRECTED_HEXAGON_LOCAL_LIFT_ARITY_OBSTRUCTION_V1.md`.

These files do not repair cancellation re-entry.

---

## 7. Current minimal claim

The research branch currently supports the conditional reduction:

> If the retained full-labelled same-order hypotheses hold and one of the four cancellation-return repairs is proved, then one valid smaller cross flow extends through the original simple edge, and the cubic/global induction follows.

It does not currently support an unconditional proof of five-CDC.

---

## 8. Assurance boundary

No canonical corpus, main branch, Curator object, audit result, Lean theorem, manuscript, release, tag, DOI, arXiv or publication status is changed by this research-branch correction.

The prior phrase

\[
\text{COMPLETE MINIMAL AUTHORIAL CANDIDATE}
\]

is superseded by

\[
\boxed{
\text{MATERIAL WELL-FOUNDEDNESS GAP}
/
\text{R2 GLOBAL RETURN OPEN}.
}

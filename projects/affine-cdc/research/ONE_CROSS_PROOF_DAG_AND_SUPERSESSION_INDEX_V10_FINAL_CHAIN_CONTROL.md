# One-cross proof DAG and supersession index v10 — final physical-chain control

## Research Lead final integration for `AC-RL-PURE-NNI-03`

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Exact parent head:** `3de0569ac9ad122b94a4dc480e7e4e84bbc0e6a4`  
**Classification:** `READY FOR PDL ZERO REPAIR / COMPONENT-CHAIN TOTALITY CONTROLLING`.

---

## 1. Controlling critical path

```text
actual smaller target + arbitrary lower target flow
                         |
                    one inverse pop
                         |
          root / one atom / exact terminal
                         |
              complete prescribed parent
                         |
        +----------------+----------------+
        |                                 |
      co-root                           zero
        |                                 |
fixed physical H_h                 fixed physical H_35
cut marked edges                   remove active cell
        |                                 |
marked-arc quotient chain          terminal-path quotient chain
        |                                 |
final connector changes route      final connector changes matching
        |                                 |
route or separating component      crossed sheet + one closed switch
        |                                 |
literal parent / exact terminal     literal parent root 35 / terminal
        +----------------+----------------+
                         |
             cross stored pure-NNI prefix edge
                         |
             ordinary strong-induction candidate
```

The controlling totality provider for both singular inverse-parent sectors is
physical component-chain contraction.  `Xi`, `Z=2N_1+N_2`, and the supplementary
`S_3` orbit rank are arithmetic certificates only.

---

## 2. Exact theorem status

At Research Lead authorial level:

| inverse-parent value | controlling theorem |
|---|---|
| root | literal stored parent NNI |
| co-root | `FC-PURE-NNI-CO-ROOT-COMPONENT-CHAIN-ESCAPE` |
| zero | `FC-PURE-NNI-ZERO-PARENT-ESCAPE` |

The zero theorem uses the fixed `H_35` quotient-chain rank `Lambda_35`; the
co-root theorem uses the analogous marked-arc rank `Lambda_h`.

Every nonterminal chain move is one explicit root NNI or source-legal equal
branch swap at a witnessed physical connector.  No target progress is inferred
from finiteness or from an arbitrary equal face.

---

## 3. Mandatory model and finite arithmetic

The issue-#77 Heawood movie is verified with stable darts and exact category:

```text
remote branch swap at 13--14
 -> H_35 split
 -> switch 1-2-3-4-13-1
 -> active word (15,13,25,23)
 -> literal parent central root 35.
```

Cyclic edge-connectivity is `6,5,5`.

The preferred equality arithmetic supplement is

\[
Z=2N_1+N_2,
\]

which is invariant under complementary support transpositions and orients all
fifteen distinct root-NNI involutions without ties.  The supplementary `S_3`
rank independently verifies twelve rows but, by the explicit scope correction,
does not select a universal equal-face source move.

---

## 4. Supersession rules

### Controlling

1. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
2. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
3. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
4. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
5. `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`;
6. `ZERO_PARENT_S3_RANK_SCOPE_CORRECTION_AND_CHAIN_CONTROL_V1.md`;
7. this v10 index.

### Retained only conditionally or arithmetically

- fixed-channel `Xi` frame, complete-component invariance, six strict rows and
  bad-free contradiction;
- the `S_3` orbit weights and twelve strict rows;
- productive equal collars once distinct physical channel paths have already
  been established.

### Withdrawn as totality

- arbitrary equal-face route-or-one-sided inference;
- `Xi` as a universal source selector;
- anchored equal-face `S_3` selection without complete terminal-arc data;
- unconditional old `N`-macro `Xi` bound.

### Still prohibited

- fixed-order `2--0` cancellation;
- lower-order root-flow call during fixed-order return;
- the other crossed NNI as a substitute for target progress;
- track erasure or periodic crosscut as target progress;
- finite-state/SCC distance;
- `Q_N`, `M_N`, `d_N`, nested bubbles or terminal frames.

---

## 5. Required next assurance

PDL must independently reconstruct:

1. carrier connectivity or exact low-port output;
2. fixed-channel parity and the unique inactive triangle;
3. inactive-vertex absorption;
4. active-component merger;
5. inheritance of every stable quotient-path edge;
6. strict physical chain-length descent;
7. final terminal-matching/marked-route change;
8. legal switch and literal parent NNI;
9. cap, dart, route, category and stored-prefix fidelity;
10. complete inverse-parent and ordinary-induction integration.

A successful PDL packet should then receive a fresh independent audit; the old
`Xi` audit cannot certify the later component-chain theorem.

---

## 6. Assurance boundary

The correct headline is:

`COMPLETE RL AUTHORIAL ORDINARY-INDUCTION CANDIDATE / NEW PDL RECONSTRUCTION AND INDEPENDENT AUDIT REQUIRED`.

No established five-support/five-CDC theorem, Lean theorem, Curator/canonical
movement, manuscript, release, tag, arXiv, DOI, peer-review or publication
status follows.

# One-cross proof DAG and supersession index

## Research Lead control index v5 — first-cancellation ordinary-induction route

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `1ff8ced47eb9a281b44299f39526be6750c60b3f`

**Supersedes for controlling use:**

- `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V4.md`;
- every earlier one-cross DAG/index.

**Controlling cap-restoration master:**

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`;
- read with `FIRST_CANCELLATION_TARGET_CAP_TERMINALISATION_V1.md` and
  `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md`.

**Headline classification:**

\[
\boxed{
\text{R1 AUTHORIAL THEOREM}
+
\text{R2-v7 COMPLETE AUTHORIAL CANDIDATE}
+
\text{ORDINARY STRONG INDUCTION}
}
\]

\[
\boxed{
\text{PDL RECONSTRUCTION REQUIRED}
/
\text{INDEPENDENT ASSURANCE REQUIRED}
/
\text{NO ESTABLISHED FIVE-CDC PROOF}.}
\]

---

## 1. Global ordinary-induction spine

Let `P_N` mean:

> every connected loopless bridgeless cubic multigraph of order at most `N` has a root-valued `E_5` flow.

Assume `P_n` for all `n<N`.

### R1 — valid smaller cross closure

For a simple edge of an order-`N` graph, at least one cross reconnection obtained by deleting its endpoints is connected, loopless, bridgeless, cubic, and has order `N-2`.

**Controlling file:**

- `ONE_CROSS_RECONNECTION_EXISTENCE_V1.md`.

Lower `P` supplies one root flow on that cross closure.

### R2-v7 — solution-valued cap restoration

Every specified root flow on the selected valid cross closure yields a root flow on the original two-vertex cap closure.

**Controlling files:**

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`;
- `FIRST_CANCELLATION_TARGET_CAP_TERMINALISATION_V1.md`;
- `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md`.

R1 plus R2-v7 gives `P_N` directly. No simultaneous `Q_N/P_N` induction is controlling.

---

## 2. R2-v7 dependency chain

### R2.1 — one-cross boundary and fixed-route lock

**Controlling:**

- `ONE_CROSS_SINGULAR_FIXED_ROUTE_REDUCTION_V1.md`;
- `TEN_STATE_BOUNDARY_GRAPH_RIGIDITY_V1.md` for the exact labelled route rows.

**Output:** immediate cap gluing, horizontal rescue, or one equality/DDD full-channel lock with route `M_i`.

### R2.2 — monotone root-NNI phase

**Equality:**

- `EQUALITY_LOCK_GLOBAL_PACHNER_POTENTIAL_V1.md`.

**DDD:**

- `DDD_LOCK_GLOBAL_PACHNER_POTENTIAL_V1.md`;
- `DDD_BOUNDED_RESIDUAL_ELIMINATION_V1.md`.

**Output:** along a route-preserving prime branch, strictly potential-lowering root NNIs continue finitely until route escape or the first equal-face pair.

### R2.2T — synchronized target category

The selected root NNI is root-valued on the cross closure, but the parallel cap target is not assumed root-valued. Therefore every candidate NNI is tested on the parallel target.

**Controlling:**

- the purely topological NNI alternative in `FULL_DEFECT_TREE_NNI_DESCENT_V1.md`;
- `ROOT_SURGERY_AUTOMATIC_CATEGORY_SAFETY_V1.md` only for the cross-flow side.

**Output:** accepted target-valid NNI, or current-target cyclic two-cut/bounded degeneration.

### R2.3 — first-cancellation target fidelity

**Controlling:**

- `CHILD_CONTEXT_FIDELITY_AFTER_ROOT_CANCELLATION_V1.md`;
- `ARBITRARY_EDGE_THREE_RECONNECTION_CATEGORY_V1.md`.

**Output at the first cancellation:**

1. parent two-/four-cut or bounded branch;
2. child route/cap-compatible flow;
3. genuine connected loopless bridgeless child target cap closure of order `N-2`.

The lower induction applies to the actual target cap closure `\widehat G^-`, not the inherited cross closure `G_j^-`.

### R2.4 — arbitrary-flow single pop

**Controlling:**

- `ROOT_VALUED_ALTERNATIVE_INVERSE_CANCELLATION_INSERTION_V1.md`;
- `QUADRUPLE_EQUALITY_THREE_VERTEX_BORROWING_ROOTIFICATION_V1.md`;
- `DOUBLED_DISJOINT_THREE_VERTEX_BORROWING_DICHOTOMY_V1.md`;
- `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md`.

**Output:**

- root-valued predecessor-order contextual state;
- one standard missing-index co-root atom;
- exact current-target bounded/small-cut terminal.

The source topology after alternative insertion need not be the literal predecessor topology. The required predecessor graph is stored as the target topology.

### R2.5 — pure fixed-order topology return

**Controlling:**

- inverse root/zero/co-root `2--2` table;
- `TARGET_TOPOLOGY_ARBORESCENCE_FIXED_ORDER_V1.md`;
- first-failure one-edge and critical-overlap grammar;
- PDL/authorial full-state forced-backbone package.

**Output:** target normalization and source-prefix inversion at one literal order, with at most one atom.

### R2.6 — cellwise one-atom track erasure

**Controlling:**

- `CELLWISE_ROOT_SEAM_AND_CONSTANT_RUN_TRACK_ERASURE_V1.md`;
- `R2_6_CELLWISE_SEAM_RUN_INTERFACE_AUDIT_AND_PDL_HANDOFF_V2.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_2_R2_6_ADDENDUM.md` for the fixed-order consumer only.

**Exact mechanism:**

1. local root seam in every pivot-change source cell;
2. identity subdivision for adjacent collars;
3. unique nonpivot root history on each constant-pivot run;
4. identity-product strip;
5. literal complete-state gluing;
6. endpoint collars and periodic minimal crosscut.

No global canonical `C6/C8` star section is controlling.

### R2.7-v7 — first-cancellation synthesis

**Controlling:**

- `FIRST_CANCELLATION_TARGET_CAP_TERMINALISATION_V1.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`;
- the target-topology scope correction.

**Rank:**

- forward phase: equality/DDD nonnegative integer potential;
- inverse pure prefix: `(source-prefix length, fixed-order target distance/one-atom rank)`;
- order induction: ordinary target vertex number.

No variable-order recurrence rank occurs.

---

## 3. Terminal consumers

### Cyclic cuts at most four

- `GLOBAL_SMALL_CUT_COMPLETION_AND_GLUING_V1.md`.

Every completion is strictly smaller. Four-cut gluing uses three smaller cap completions and the independent finite Type-T/Type-H profile elimination; it does not invoke R2-v7.

### Direct matching and theta

- `BOUNDED_DIRECT_PAIRING_CAP_TERMINAL_V1.md`;
- explicit theta roots `12,13,23`.

### Acyclic low-port shore

- one cubic vertex for a three-port shore;
- one two-vertex cap for a four-port shore.

### Loop/bridge/singleton cut

- loop deletion/reinsertion;
- bridge/singleton-cut outer-shell decomposition.

### Route/profile escape

- exact `K_i` boundary state;
- cap gluing at the current topology;
- pure fixed-order return to the original topology.

No generic `accepted exit` is an external conclusion.

---

## 4. Superseded controlling machinery

The following remain available as stronger or historical mathematics, but are not dependencies of R2-v7.

### Mixed-history masters

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_1_ADDENDUM.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_3_TERMINAL_SEMANTICS_ADDENDUM.md`.

### Recursive witnessed architecture

- `AC_PD_5CDC_R2_7_MUTUAL_INDUCTION_HISTORY_WITNESS.md` as the controlling external theorem;
- recursive child `Q_n` calls;
- child-history fidelity beyond the first-cancellation target statement;
- full-labelled genealogy transport through nested returns.

### Weld/nested-call machinery

- active-diagonal six-leaf weld lift;
- weld support-switch source pop;
- suspended ancestor marks through nested bubbles;
- call-free and innermost bubble lifts;
- variable-order recurrent-episode compression;
- resolved-call macro graph `M_N` and rank `d_N`;
- framewise or outermost-child terminal unwind.

These files are not declared false. V7 bypasses their hypotheses by terminating the selected cross-flow descent at the first cancellation and solving the actual smaller target cap closure by ordinary lower `P`.

---

## 5. Superseded R2.6 route

Do not use as controlling prerequisites:

- `PETERSEN_C6_CANONICAL_STAR_SECTION_V1.md` as a global history section;
- global star-star gluing in `PETERSEN_C6_RELATIVE_STAR_MOVIE_DESCENT_V1.md`;
- `PETERSEN_EVEN_TRACK_ROOT_ANNULUS_REPLACEMENT_V1.md`;
- the two-`C6` `C8` annulus route;
- `R2_6_EVEN_TRACK_SOURCE_REPLACEMENT_PDL_HANDOFF_V1.md`.

The local `(0,2,2)` pivot-change seam remains an input to the cellwise theorem.

---

## 6. PDL critical path

PDL should no longer spend its critical path reconstructing variable-order calls.

The controlling reconstruction order is:

1. R1 valid-cross theorem;
2. one-cross boundary/route rows;
3. equality and DDD potentials;
4. synchronized parallel-target NNI test;
5. first-cancellation target fidelity;
6. lower `P` on `\widehat G^-`;
7. arbitrary-flow single-pop table and topology scope;
8. pure fixed-order R2.4--R2.6 return;
9. exact terminal consumers;
10. ordinary strong-induction synthesis.

Requested return:

`AC-PD-5CDC V7 FIRST-CANCELLATION RETURN — COMPLETE-DRAFT`

or one exact obstruction.

---

## 7. Audit obligations

An independent audit must recompute:

1. all six controlling route rows;
2. equality `15/15` potential and no-local-minimum table;
3. DDD `15/15` potential and no-local-minimum table;
4. target-parallel NNI bridge/cut alternative;
5. first-cancellation cross/target category map;
6. arbitrary-flow insertion rows;
7. alternative-topology versus target-topology distinction;
8. first-failure one-atom uniqueness;
9. full-state forced-backbone and odd-core exclusions;
10. cellwise seam/run gluing and periodic crosscut;
11. two-/three-/four-cut completion and gluing;
12. absence of any hidden call to `P_N` inside R2-v7.

---

## 8. Current classification

\[
\boxed{
\text{R1 RETAINED AUTHORIAL THEOREM}
+
\text{R2.1--R2.6 RETAINED/REPAIRED}
+
\text{R2.7-v7 COMPLETE AUTHORIAL CANDIDATE}.}
\]

\[
\boxed{
\text{ORDINARY STRONG INDUCTION ONLY}
/
\text{PDL RECONSTRUCTION REQUIRED}
/
\text{INDEPENDENT ASSURANCE REQUIRED}.}
\]

\[
\boxed{
\text{NO ESTABLISHED FIVE-CDC PROOF}
/
\text{NO LEAN, CURATOR, MANUSCRIPT, RELEASE, OR PUBLICATION PROMOTION}.}
\]

# One-cross proof DAG and supersession index

## Research Lead control index v6 — v7.2 state-walk cellwise route

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `cdcf637bd8a8d52d59553d5927a8e3fea1dd6762`

**Supersedes for controlling use:**

- `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V5.md`;
- every earlier one-cross DAG/index.

**Controlling master:**

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_1_SCOPE_AND_ALPHABET_ADDENDUM.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_2_STATE_WALK_CELLWISE_ADDENDUM.md`.

**Headline:**

`R1 AUTHORIAL THEOREM + R2-v7.2 COMPLETE AUTHORIAL CANDIDATE + ORDINARY STRONG INDUCTION`.

`PDL RECONSTRUCTION REQUIRED / INDEPENDENT ASSURANCE REQUIRED / NO ESTABLISHED FIVE-CDC PROOF`.

---

## 1. Ordinary-induction spine

Let `P_N` denote root solubility of every connected loopless bridgeless cubic multigraph of order at most `N`.

Assume `P_m` for every `m<N`.

1. `ONE_CROSS_RECONNECTION_EXISTENCE_V1.md` gives one valid cross closure of order `N-2`.
2. Lower `P` supplies one root flow on it.
3. R2-v7.2 returns a root flow on the original cap closure.
4. Hence `P_N`.

No simultaneous witnessed proposition is controlling.

---

## 2. Exact critical path

### A. Structural input

- R1 valid-cross existence;
- ten-state boundary/route table;
- one-cross fixed-route reduction.

### B. Forward fixed-route descent

- equality potential `(E,Phi,|V|)`;
- DDD potential `(Omega,|V|)`;
- synchronous parallel-target NNI category test;
- stop at route escape, target category, or first equal-face cancellation.

The strict prefix is pure root-valued `2--2` NNI history.

### C. First cancellation

- child cross/parallel-target fidelity;
- current-parent cut/bounded alternatives;
- genuine child target cap closure of order `N-2`;
- lower ordinary `P` on that target closure.

### D. Single inverse pop

- intersecting original insertion;
- equality alternative insertion;
- good-disjoint alternative insertion;
- missing-index one-atom state;
- exact local terminal.

Alternative source topology and required predecessor target topology remain distinct state fields.

### E. Pure fixed-order return

- target-rooted ordinary `2--2` arborescence;
- root/zero/co-root first-failure table;
- at most one atom;
- no cancellation reopening;
- source-prefix/target-distance/atom rank.

### F. State-walk cellwise atom consumption

1. physically rootify every maximal constant-pivot run;
2. retain only genuine run-boundary switch cells;
3. put one six-port root seam in every switch cell;
4. for a reduced-skeleton backtrack, use two seams and the rooted middle run;
5. glue identity-product run strips on literal complete states;
6. consume closed/open/periodic endpoint cases.

### G. Exact terminals

- cap-compatible route state;
- cyclic two-/three-/four-cut as explicitly returned;
- bounded acyclic low-port shore;
- theta/parallel/loop/bridge/singleton-cut outer shell.

Every recursive completion is strictly smaller.

---

## 3. Removed dependencies

The controlling route does not require:

- lower witnessed `Q_m`;
- mixed-history inverse transfer;
- weld-active-diagonal/support-switch child lifts;
- nested bubble compression;
- suspended ancestor marks through variable order;
- resolved-call graph `M_N` or rank `d_N`;
- terminal frame stacks;
- R2.5 odd-core exclusion;
- shortest Petersen simple core;
- `C6/C8` annuli;
- Type-T graph contraction or abstract `abba` deletion.

These files remain historical or stronger optional mathematics unless separately revived.

---

## 4. Correct backtrack reading

A reduced skeleton `s->t->s` does not make either switch cell locally backtracking.

The first cell is

`{s,x}->{s,t}->{t,y}`

and the second is

`{t,x'}->{t,s}->{s,y'}`,

with the six-port inequalities forced at each genuine run boundary.

The next reduced pivot `s` is the far end of the rooted constant-`t` carrier, not the immediate endpoint `y` of the first next atom state.

Therefore no Type-T source-contraction theorem is needed.

---

## 5. Topological ordering

One valid topological order of dependencies is:

1. root/support arithmetic and finite boundary tables;
2. graph structural R1 and cut-category lemmas;
3. Type T/H finite four-cut mismatch eliminations;
4. equality/DDD local potential tables;
5. target-parallel NNI and first-cancellation category maps;
6. arbitrary-flow inverse insertion tables;
7. local first-failure/critical-overlap one-atom grammar;
8. fixed-order topology arborescence;
9. full-state maximal-run rootification and nonbranching track interface;
10. six-port pivot-change seam table;
11. state-walk cellwise closed/open/periodic erasure;
12. fixed-order no-reopening return;
13. first-cancellation cap restoration;
14. ordinary induction `P_<N -> P_N`;
15. general loop/bridge outer shell.

No earlier node invokes node 13 or 14.

---

## 6. PDL critical path

PDL should reconstruct in the same order as Section 2, with special attention to:

- target-side category without an assumed target flow;
- actual child target cap closure at first cancellation;
- alternative source topology versus target topology;
- state-walk versus reduced-skeleton indexing;
- physical maximal-run rootification;
- two-seam backtrack treatment;
- no hidden cancellation inside fixed-order return;
- strict-lower nature of every cut completion.

Requested return:

`AC-PD-5CDC V7.2 FIRST-CANCELLATION STATE-WALK RETURN — COMPLETE-DRAFT`

or one exact obstruction.

---

## 7. Audit obligations

Independent audit must recompute:

1. valid-cross graph theorem;
2. route rows and boundary profiles;
3. equality and DDD potential tables;
4. parallel-target category maps;
5. first-cancellation target fidelity;
6. arbitrary-flow pop table;
7. one-atom uniqueness;
8. target arborescence and no-reopening alphabet;
9. full-state run/switch decomposition;
10. six-port seam table and two-seam backtracks;
11. endpoint/periodic gluing;
12. all cut/bounded terminal consumers;
13. absence of hidden `P_N`.

---

## 8. Classification

`V7.2 CONTROLLING DAG ACYCLIC AT RL AUTHORIAL LEVEL`.

Still required:

- PDL reconstruction;
- independent assurance;
- canonical receiver decision.

Not claimed:

- independently accepted cap restoration;
- established five-support/five-CDC theorem;
- Lean, Curator, manuscript, release or publication status.

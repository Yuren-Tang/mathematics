# One-cross proof DAG and supersession index

## Research Lead control index v8 — switch-invariant all-index repair

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Controlling theorem:** `PURE_NNI_ALL_INDEX_AMBIENT_ESCAPE_MASTER_THEOREM_V3.md`

**Supersedes for controlling status:**

- `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V7_PRESCRIBED_PARENT_FRONTIER.md`;
- the blocked status of Node 22;
- the completion claim of the quarantined `Omega` plateau master attempt.

---

## 1. Correct headline

\[
\boxed{
\begin{array}{c}
R1\text{ AUTHORIAL THEOREM}\\
+\ \text{v7.2 ACTUAL TARGET AND SINGLE POP: AUTHORIAL CANDIDATE}\\
+\ \text{STATE-WALK/SEAM/RUN COHERENCE: AUTHORIAL CANDIDATE}\\
+\ \text{ALL-INDEX PRESCRIBED-PARENT OBJECT: AUTHORIAL THEOREM}\\
+\ \text{FIXED-CHANNEL AMBIENT ESCAPE: AUTHORIAL THEOREM}\\
\hline
\text{COMPLETE RL AUTHORIAL ORDINARY-INDUCTION CANDIDATE.}
\end{array}}
\]

`PDL RECONSTRUCTION REQUIRED / INDEPENDENT ASSURANCE REQUIRED / NO ESTABLISHED FIVE-CDC PROOF`.

---

## 2. Repaired critical path

```text
R1 valid smaller cross
        |
lower ordinary existence on selected cross
        |
fixed-route forward pure-NNI prefix
        |
route/category exit OR first equal-face cancellation
        |
actual smaller target + arbitrary lower target flow
        |
one inverse pop
        |
root / one-atom normalization
        |
complete all-index prescribed-parent state
        |
all ten horizontal switches and crossed-sheet tests
        |
parent success / named terminal / full-channel lock
                                      |
                     transported fixed H_14 frame
                                      |
                  switch-invariant integer rank Xi
                                      |
              ambient root-NNI / equal-face strict macro
                                      |
                  parent / K_i / named terminal
                                      |
                      return through stored prefix
                                      |
                      ordinary strong induction
```

The former open arrow from a horizontal SCC to an absorbing output is now supplied by the fixed-channel theorem.

---

## 3. New load-bearing theorem

Choose one physical rescue channel and transport it to canonical `H_14` together with the current ordered marked roots and missing index.

Define

\[
\begin{array}{c|cccccccccc}
T&123&124&125&134&135&145&234&235&245&345\\
\hline
\xi(T)&1&0&1&0&0&0&1&2&1&0.
\end{array}
\]

Then:

1. `xi` is invariant under the support transposition `tau_14`;
2. every complete nonexit closed `H_14` component switch preserves
   \[
   \Xi=\sum_v\xi(\Delta_v);
   \]
3. every vertex of type `123,234,125,245` supplies a root-only macro lowering `Xi` by exactly two or an exact absorbing output;
4. if those four types are absent, the `H_14` component beginning at boundary root `12` is trapped in type `124` and roots `12,24`, contradicting the required locked `12--34` route.

Thus no closed nonterminal all-index SCC exists.

---

## 4. Exact retained dependencies

### Structural and forward

1. root-flow/five-support equivalence at cubic level;
2. valid smaller cross closure;
3. ten-state boundary and exact physical route;
4. equality/DDD forward descent to first cancellation;
5. synchronized pure root-NNI prefix.

### First cancellation

6. actual order-`N-2` target cap closure;
7. lower ordinary root existence on that target;
8. one arbitrary-flow inverse pop;
9. intersecting/equality/good-disjoint/missing-index rows;
10. at most one normalized atom and exact local terminals.

### Coherence

11. full-state nonbranching atom track;
12. maximal constant-pivot run rootification;
13. six-port seams and two-seam backtracks;
14. literal closed/open/periodic gluing.

### Prescribed-parent repair

15. complete Phase-0 state tuple;
16. all-index support-switch and migration contracts;
17. separating-channel and `K_i` consumers;
18. fixed-channel frame and switch-invariant rank;
19. six strict NNI rows and four equal-face root macros;
20. bad-free route contradiction;
21. all-index SCC exclusion.

---

## 5. Quarantined and retired routes

The controlling proof does not use:

- the local two-vertex `Omega` orbit-shift formula;
- `Omega` plateau minima;
- a zero/co-root track inside DDD-lock escape;
- actual `2--0` cancellation during fixed-order return;
- lower witnessed `Q_N`;
- `M_N`, `d_N`, nested bubbles or terminal frames;
- generic Kempe/root/NNI connectivity;
- topology-only reachability or finite-SCC distance.

`DDD_LOCK_OMEGA_PLATEAU_ORBIT_TRANSPORT_SCOPE_CORRECTION_V1.md` remains the controlling rejection of the failed `Omega` proof. It is superseded only as the description of the still-open frontier.

---

## 6. PDL reconstruction order

PDL should now reconstruct:

1. the transported `H_14` frame under common-component migration;
2. `xi` invariance on every vertex of the physical switch component and its complement after frame transport;
3. the six exhaustive distinct-neighbour NNI rows;
4. the four equal-neighbour branch-swap/switch/NNI movies;
5. absence of zero overlap;
6. the bad-free route contradiction;
7. preservation of cap, dart, parent and prefix coordinates;
8. exclusion of closed all-index SCCs;
9. integration into v7.2 and ordinary strong induction;
10. complete terminal-result and outer-shell audit.

Requested return:

`[READY-FOR-INDEPENDENT-V7.2-AUDIT — SWITCH-INVARIANT REPAIR]`

or one smallest exact obstruction.

---

## 7. Current theorem-status boundary

### Closed at RL authorial level

- `DDD-PLATEAU-PRODUCTIVE-ROW-ESCAPE`, by a stronger fixed-channel theorem;
- `FC-PURE-NNI-ESCAPE`;
- `FC-PURE-NNI-ALL-INDEX-AMBIENT-ESCAPE`;
- v7.2 prescribed-parent target reinsertion;
- the ordinary-induction proof DAG as an authorial candidate.

### Still required

- independent PDL reconstruction;
- adversarial theorem audit;
- Director disposition and any later Curator/canonical decision.

### Not claimed

- independently accepted cap restoration;
- an established universal five-support or five-CDC theorem;
- Lean verification;
- Curator/canonical movement;
- manuscript, release, tag, arXiv, DOI, peer-review or publication status.

---

## 8. Current classification

\[
\boxed{
\texttt{[READY-FOR-PDL-REPAIR AC-RL-PURE-NNI-02]}}
\]

Controlling files:

1. `DDD_LOCK_H14_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md`;
2. `PURE_NNI_ALL_INDEX_AMBIENT_ESCAPE_MASTER_THEOREM_V3.md`;
3. this index.

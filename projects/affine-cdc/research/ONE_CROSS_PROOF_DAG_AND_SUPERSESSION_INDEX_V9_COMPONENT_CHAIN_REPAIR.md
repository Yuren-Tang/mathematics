# One-cross proof DAG and supersession index

## Research Lead control index v9 — fixed-channel component-chain repair

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `88ef60d790463bd3a6fc1f5e91e4affb353d9623`

**Controlling new theorems:**

1. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`;
2. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`;
3. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md`;
4. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`;
5. `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md`.

**Supersedes for controlling status:**

- `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V8_SWITCH_INVARIANT_REPAIR.md`;
- the unconditional totality claim of `PURE_NNI_ALL_INDEX_AMBIENT_ESCAPE_MASTER_THEOREM_V3.md`;
- the arbitrary-equal-face Theorem 5.1 of `DDD_LOCK_H14_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md`;
- the PDL v7.3 conclusion at `fee97446...` that only the zero-parent row remained open.

The independent audit at `audit/affine-cdc-xi-co-root-v1@53be22a0...` is accepted as a valid correction: the prior co-root equal-neighbour source theorem had a material marked-arc gap.  The new component-chain theorem repairs that gap and also closes the zero-parent row at RL authorial level.

---

## 1. Correct headline

\[
\boxed{
\begin{array}{c}
R1\text{ AUTHORIAL THEOREM}\\
+\ \text{ACTUAL SMALLER TARGET / SINGLE POP: RETAINED CANDIDATE}\\
+\ \text{ONE-ATOM SEAM/RUN COHERENCE: RETAINED CANDIDATE}\\
+\ \text{CO-ROOT PRESCRIBED-PARENT OBJECT: AUTHORIAL THEOREM}\\
+\ \text{ZERO-PARENT PRESCRIBED-PARENT OBJECT: AUTHORIAL THEOREM}\\
+\ \text{FIXED-CHANNEL COMPONENT-CHAIN TOTALITY: AUTHORIAL THEOREM}\\
\hline
\text{COMPLETE RL AUTHORIAL ORDINARY-INDUCTION CANDIDATE.}
\end{array}}
\]

`NEW PDL RECONSTRUCTION REQUIRED / NEW INDEPENDENT AUDIT REQUIRED / NO ESTABLISHED FIVE-CDC PROOF`.

---

## 2. Corrected chronology

### v8 event

The v8 branch introduced a sound fixed-channel `Xi` invariant and correct strict distinct-neighbour rows.  It claimed an arbitrary equal bad face was route-changing or one-sided.

### Independent audit event

Audit `AC-AUDIT-XI-01` found an exact Heawood equal face whose marked edges lie on one outside arc.  Both root placements remain common-component locks with the same marked route.  Therefore `Xi` did not provide a total deterministic descent.

### v9 repair

The v9 proof does not select an arbitrary equal face.  It:

1. cuts the actual marked edges or active zero-parent cell;
2. records the two distinguished physical `H_h` paths;
3. forms the quotient of channel components plus inactive vertices;
4. contracts a shortest witnessed physical chain by ordinary root NNIs;
5. uses the final connector between different distinguished paths to change the terminal matching.

Thus the missing marked-arc datum is explicit and load-bearing.

---

## 3. Repaired critical path

```text
R1 valid smaller cross
        |
lower ordinary root existence on selected cross
        |
fixed-route forward pure-NNI prefix
        |
route/category exit OR first equal-face cancellation
        |
actual smaller target + arbitrary lower target flow
        |
one inverse pop
        |
root / one-atom normalization
        |
complete prescribed-parent state
        |
        +---------------- co-root / DDD (4,2,2)
        |                     |
        |          nonlocked horizontal consumer
        |                     |
        |              fixed physical H_h
        |                     |
        |          cut two marked H_h edges
        |                     |
        |       marked-arc component-chain contraction
        |                     |
        |       marked route change / separation
        |                     |
        |          K_i / switch + literal parent
        |
        +---------------- zero parent (0,2,2)
                              |
                   nonlocked horizontal consumer
                              |
                         fixed H_35
                              |
                  remove active two-vertex cell
                              |
             terminal-path component-chain contraction
                              |
                   crossed outside matching
                              |
             crossed-sheet alignment + one switch
                              |
                  literal parent central root 35
                              |
              return through stored pure-NNI prefix
                              |
                  ordinary strong induction
```

Every chain edge is an explicit root NNI with a physical stable-edge witness.  No finite-state recurrence is used.

---

## 4. Load-bearing channel theorem

For one fixed channel `H_h`:

1. every vertex has channel degree zero or two;
2. the unique inactive triangle is `[5]\setminus h`;
3. one active/inactive root NNI absorbs the inactive vertex into the selected path;
4. one active/active root NNI or equal branch swap merges an intervening cycle or changes the matching of two terminal paths;
5. the connected quotient path length decreases by one at every nonterminal step.

The final connector has endpoints on different marked arcs/terminal paths by construction.  This is exactly what the false arbitrary-equal-face lemma failed to guarantee.

---

## 5. Co-root row after audit correction

### Retained from `Xi`

- unique transported frame;
- complete-component vertexwise invariance;
- six strict rows;
- bad-free route contradiction;
- cap/dart/parent/prefix contracts.

### Withdrawn

- arbitrary equal-face route-or-split;
- `Xi` totality from any chosen bad vertex;
- unconditional at-most-`N` strict-macro bound.

### New totality

Cut the two marked channel edges and contract a physical chain between their two arcs.  The final NNI gives either:

- a separating channel; or
- the opposite marked route.

The prior Phase-B consumer then returns the literal co-root parent or `K_i`/terminal.

---

## 6. Zero-parent row

Normalize

\[
(13,13,23,23),
\qquad r=12,
\qquad h=35.
\]

If the outside `H_35` matching is crossed, horizontal rescue is immediate.  In the residual full lock it is `AB|CD`.

Remove the active vertices.  Category safety makes the four-port carrier connected.  Contract a physical quotient path between paths `A--B` and `C--D`.  The final NNI changes the outside matching to a crossed matching.  Align the active crossed root sheet, switch one closed `H_35` component, and perform the literal parent NNI with root `35`.

This closes

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}.}
\]

---

## 7. Equality arithmetic improvement

In the ordered equality frame define

\[
Z=2N_1+N_2.
\]

It is invariant under support transpositions inside `\{3,4,5\}` and gives a unique decreasing direction to all fifteen root-NNI involutions.  There are no neutral distinct rows.

This improves finite arithmetic over the older `(E,Phi)` table, but it is not used to infer source legality of arbitrary remote equal faces.  Physical chain length supplies totality.

---

## 8. Exact retained dependency spine

### Structural and forward

1. root-flow/five-support equivalence at cubic level;
2. valid smaller cross closure;
3. ten-state boundary graph and fixed physical route;
4. equality/DDD forward descent to first cancellation;
5. synchronized pure root-NNI prefix.

### Actual target and pop

6. actual order-`N-2` target cap closure;
7. lower ordinary root existence on that target;
8. arbitrary-flow single inverse pop;
9. intersecting/equality/good-disjoint/missing-index rows;
10. one-atom normalization and exact local categories.

### State-walk coherence

11. nonbranching full-state atom track;
12. maximal constant-pivot run rootification;
13. pivot-change seams and two-seam backtracks;
14. literal closed/open/periodic gluing.

### Prescribed-parent return

15. complete all-index state tuple and literal parent field;
16. horizontal separating-channel and cap/terminal consumers;
17. fixed-channel parity and inactive-triangle lemma;
18. active/inactive absorption rows;
19. active/active component contraction;
20. connected physical quotient;
21. strict component-chain rank;
22. co-root marked-arc corollary;
23. zero-parent terminal-path corollary;
24. literal parent/cap/prefix integration.

---

## 9. Quarantined and retired routes

The controlling v9 proof does not use:

- the false arbitrary equal-face route-or-split theorem;
- `Xi` as a total source selector;
- the local two-vertex `Omega` orbit-shift formula;
- `Omega` plateau minima;
- equality `2--0` cancellation during fixed-order return;
- a lower-order call after the single pop;
- track erasure as prescribed-parent progress;
- topology-only generic NNI connectivity;
- finite-state/SCC distance;
- lower witnessed `Q_N`;
- `M_N`, `d_N`, nested bubbles or terminal frames.

The old `Omega` scope correction and the new marked-arc audit correction both remain controlling negative boundaries.

---

## 10. Mandatory model and adversarial witness

### Zero-parent positive model

The issue-#77 Heawood movie is verified at

`ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`.

### Co-root negative model

The audit same-arc face `9-14` remains a valid counterexample to the old lemma.

### Co-root repaired move

The chain theorem selects stable edge `6-7` instead.  Its root NNI changes the marked route and has digest

`72e67089476af09b33daf91316b9b71b687f98291457e4514c8a384bba626ec0`.

Both the counterexample and its corrected selection are retained.

---

## 11. Required PDL reconstruction order

PDL should reconstruct:

1. the complete zero-parent state/interface normal form;
2. category-safe connectivity after active-cell deletion;
3. fixed-channel degree-zero/two parity;
4. uniqueness of the inactive triangle;
5. every active/inactive absorption row with stable darts;
6. active/active root alternative and exact transition-system effect;
7. quotient connectivity and inherited path after contraction;
8. final terminal-matching change;
9. zero-parent crossed-sheet alignment, switch and parent NNI;
10. co-root cut-marked-edge chain corollary;
11. the audit-witness route-changing repair;
12. cap/route/category and stored-prefix integration;
13. end-to-end v7.3 ordinary-induction audit.

Requested next return:

`[READY-FOR-INDEPENDENT-V7.4-AUDIT — COMPONENT-CHAIN REPAIR]`

or one smallest exact obstruction.

---

## 12. Current theorem-status boundary

### Closed at RL authorial level

- `FC-PURE-NNI-ZERO-PARENT-ESCAPE`;
- co-root same-arc escape by component chain;
- `FC-PURE-NNI-CO-ROOT-COMPONENT-CHAIN-ESCAPE`;
- complete prescribed-parent inverse table;
- ordinary-induction DAG as an RL candidate.

### Invalidated historical completion route

- v8 totality through arbitrary equal-face `Xi` collars.

### Still required

- PDL reconstruction of the new source theorem;
- independent audit;
- full terminal and induction integration audit;
- Director, Curator and Owner dispositions.

### Not claimed

- an independently accepted universal five-support/five-CDC theorem;
- Lean verification;
- canonical movement;
- manuscript, release, tag, arXiv, DOI, peer review or publication.

---

## 13. Current classification

\[
\boxed{
\texttt{[READY-FOR-PDL-ZERO-REPAIR AC-RL-PURE-NNI-03]}
}
\]

The receiver should consume the component-chain theorem, not the superseded arbitrary-equal-face totality claim.
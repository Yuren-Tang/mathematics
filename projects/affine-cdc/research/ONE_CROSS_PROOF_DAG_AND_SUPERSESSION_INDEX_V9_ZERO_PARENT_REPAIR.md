# One-cross proof DAG and supersession index v9 — zero-parent repair

## Research Lead integration record

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workstream:** `AC-RL-PURE-NNI-03`  
**Frozen start:** `research/affine-cdc-five-cdc-v1@212d789a5967813e7277fb3e269060926c99cb0e`  
**PDL checkpoint consumed:** `proof-development/affine-cdc-rigour-v1@fee97446ee8b99f07740f394e99ef4a2ecc3e40e`  
**Classification:** `ZERO-PARENT REPAIRED AT RL AUTHORIAL LEVEL / PDL RECONSTRUCTION REQUIRED`.

This file appends to, and corrects the scope of,
`ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V8_SWITCH_INVARIANT_REPAIR.md`.
It does not modify the independently reconstructed co-root `Xi` theorem.

---

## 1. Controlling fixed-order spine

```text
actual lower target + one arbitrary-flow inverse pop
                         |
                         v
            root / co-root / exact terminal
                         |
                         v
              complete crossed root state
                         |
       +-----------------+-----------------+
       |                                   |
       v                                   v
co-root prescribed parent             zero prescribed parent
fixed-channel Xi theorem              S_3-invariant anchored rank
       |                                   |
       +-----------------+-----------------+
                         |
                         v
literal parent / cap-route / named cut-bounded terminal
                         |
                         v
cross the stored pure-NNI prefix edge
```

The fixed-order move alphabet remains:

- ordinary root-valued `2--2` NNIs;
- root-valued equal-face branch swaps;
- legal complete support-component switches;
- exact route/profile and named category consumers.

There is no fixed-order `2--0` cancellation and no same-order lower-flow call.

---

## 2. New controlling nodes

### Node 23 — exact Heawood source movie

**File:** `ZERO_PARENT_HEAWOOD_H35_SOURCE_MOVIE_CERTIFICATE_V1.md`  
**State:** complete authorial source certificate.

The required movie is valid:

```text
remote equal-face branch swap at 13--14
 -> H_35 components 1-2-3-4-13-1 and 5-6-7-12-14-9-10-5
 -> switch the first component
 -> active word (15,13,25,23)
 -> literal parent NNI with central root 35.
```

Every edge/dart identity, root triangle, cap incidence and category is explicit.
The initial, post-remote and final topologies have cyclic edge-connectivity
`6,5,5`.  A shorter conjugate movie using the active crossed sheet is also
recorded.

### Node 24 — intrinsic equality orbit rank

**File:** `ZERO_PARENT_S3_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md`  
**State:** complete authorial theorem.

For ordered differing supports `x,y` and unordered remaining set `U`, partition
the ten support triangles into

\[
\mathcal X=\{xyi\},\quad
\mathcal Y=\{xij\},\quad
\mathcal Z=\{yij\},\quad
\mathcal S=\{U\}.
\]

Use weights

\[
e=(2,1,1,0),
\qquad
q=(0,1,0,0)
\]

on `(X,Y,Z,S)` and the exterior integer

\[
\mathcal R=(N-1)\sum e+\sum q.
\]

The rank is invariant under every support permutation of `U`, hence under both
physical rescue-channel transpositions and their common-component frame
transport.

The twelve required distinct rows have orbit forms

\[
XX\to YZ,
\qquad
XY\to XZ,
\qquad
YY\to YS,
\]

and strictly lower `mathcal R`.

Every selected equal `X` or `Y` row is replaced by:

```text
zero or one root branch swap
 -> one complete rescue-component switch
 -> one strict root NNI,
```

or by literal parent, route/profile or named category absorption.  No zero edge
or co-root track is introduced.

### Node 25 — deterministic anchored descent

Follow one physical active `x`-side dart.  Its exterior endpoint is always in
`X` or `Y`.

- At an `X` endpoint use its nonactive `xy` edge.
- At a `Y=xst` endpoint use the nonactive `xt` edge.

Every possible neighbour is one of the twelve strict rows or one of the two
equal macros.  The selected edge is disjoint from the active central edge and
the four active darts.  After an NNI the same physical active dart survives and
supplies the next anchor.

Thus every nonabsorbing macro lowers a nonnegative integer; there is no
bad-free state and no terminal-free recurrence.

### Node 26 — universal zero-parent theorem

The node

`FC-PURE-NNI-ZERO-PARENT-ESCAPE`

is closed at Research Lead authorial level:

> Every complete category-safe fixed-order `(0,2,2)` prescribed-parent state
> reaches the literal parent, a cap-compatible route/profile output, or a named
> cyclic `2/3/4`-cut or bounded terminal by a finite root-valued same-order
> source history.

### Node 27 — complete inverse-parent table

At Research Lead authorial level:

| forced parent value | controlling disposition |
|---|---|
| root | literal parent NNI |
| co-root | PDL-reconstructed fixed-channel `Xi` theorem |
| zero | v9 `S_3`-invariant anchored descent |

The v8 inference that a co-root-only theorem already closed this whole table is
superseded.  The corrected whole-table conclusion now depends on the separate
zero theorem above.

---

## 3. Exact finite certificate

The standard-library certificate is

`projects/affine-cdc/research/certificates/zero_parent_heawood_and_s3_rank_v1.py`.

It checks:

- the labelled Heawood movie and exact `H_35` components;
- all intermediate support triangles;
- connectedness, simplicity, bridgelessness and cyclic edge-connectivity;
- invariance of the orbit weights under the three transpositions of `U`;
- all twelve strict rows and their rank changes.

Digest:

`d22bce5ea026f5c461575f05ed81ed6e68c71a0c3c459ce6ada93d96f458422e`.

---

## 4. Supersession ledger

### Corrected

- The v8 statement that the co-root `Xi` theorem alone closed the complete
  inverse-parent table is withdrawn.
- `AC_PD_5CDC_V7_3_ZERO_PARENT_REINSERTION_OBSTRUCTION.md` remains the correct
  diagnosis at frozen PDL SHA `fee974...`; its open node is answered by the new
  v9 theorem.
- The Heawood category-safe witness is not a counterexample; the explicit
  source movie consumes it.

### Retained unchanged

- the PDL-reconstructed co-root/DDD fixed-channel `Xi` theorem;
- the six co-root distinct rows and four co-root equal macros;
- the all-index co-root SCC exclusion;
- first-cancellation target synchronization, one-pop source theorem,
  state-walk coherence and exact terminal consumers;
- the accepted outer-shell implication, conditional on the cubic theorem.

### Still retired

- the old `Omega` orbit-minimum argument;
- actual fixed-order equality cancellation as an inverse-return move;
- `Q_N`, `M_N`, `d_N`, nested bubbles and terminal frames;
- track erasure or periodic crosscuts as prescribed-parent progress;
- finiteness/SCC distance without a displayed reachability theorem.

---

## 5. Conditional downstream effect

Subject to independent PDL reconstruction of Nodes 23--27, the v7.3 ordinary
strong-induction draft no longer has an open inverse-parent row.  PDL must still
line-check:

1. the orbit frame and complete switch invariance;
2. equal-face marked-route matching semantics;
3. active dart and cap preservation;
4. exact category consumers;
5. integration through every stored prefix edge;
6. the final ordinary-induction and outer-shell interfaces.

No independent acceptance is asserted here.

---

## 6. Status boundary

The exact Research Lead status is:

`PURE FIXED-ORDER INVERSE-PARENT TABLE CLOSED AT AUTHORIAL LEVEL / READY FOR PDL ZERO REPAIR`.

This does not establish an accepted cubic five-support theorem or universal
five-CDC theorem.  It changes no Lean, canonical, Curator, manuscript, release,
tag, arXiv, DOI, peer-review or publication status.

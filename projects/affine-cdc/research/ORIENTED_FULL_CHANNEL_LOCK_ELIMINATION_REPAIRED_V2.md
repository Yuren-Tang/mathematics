# Every blocked inverse-dipole cap is eliminated — repaired full-channel theorem

## Research dossier v2 — controlling authorial synthesis

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `9b9901e1e6149efafad23915b4679badca2aa781`.

**Supersedes for controlling use:** `ORIENTED_FULL_CHANNEL_LOCK_ELIMINATION_SYNTHESIS_V1.md`.

**Core parents:**

- `ARBITRARY_EDGE_THREE_RECONNECTION_CATEGORY_V1.md`;
- `THREE_RECONNECTION_FIXED_ROUTE_REDUCTION_V1.md`;
- `INVERSE_DIPOLE_KEMPE_RESCUE_V1.md`;
- `EQUALITY_LOCK_GLOBAL_PACHNER_POTENTIAL_V1.md`;
- `DDD_LOCK_GLOBAL_PACHNER_POTENTIAL_V1.md`;
- `DDD_BOUNDED_RESIDUAL_ELIMINATION_V1.md`;
- `ROOT_SURGERY_AUTOMATIC_CATEGORY_SAFETY_V1.md`;
- `FIRST_FAILURE_NORMALIZATION_AUTOMATIC_CATEGORY_SAFETY_V1.md`;
- `ROUTE_CHANGE_TERMINAL_CONTEXTUAL_TRANSFER_V1.md`;
- `DIRECT_PAIRING_BRIDGE_TO_THETA_ZERO_RESOLUTION_V1.md`;
- `CONTEXTUAL_TRANSFER_REPAIRED_AUTHORITY_V2.md`;
- `GLOBAL_CAP_LIFT_INTERFACE_COMPATIBILITY_V1.md`.

**Status:** complete repaired authorial theorem. Let `P_0` be the complementary ordered four-pole of one deleted cubic edge and `cap_i` the original two-vertex cap. If all three zero-vertex reconnection closures are smaller connected bridgeless graphs with root-valued `E_5` flows, then `P_0` has a root-valued boundary state in the exact cap profile `K_i`. The proof no longer uses any of the three overfast implications present in earlier packets: a route change on a descendant is not identified with the original profile, the same-matching bridge terminal is not treated as an original category discharge, and an abstract even-track tube flow is not treated as a pointwise Ptolemy-history lift. Route and direct terminals are returned contextually; even tracks enter strict order through the relative Petersen `C6` star movie; all root-valued surgery moves are automatically category-safe.

**Assurance boundary:** this is a research-branch authorial theorem. It has not passed downstream proof development, independent audit, curation, Lean verification, manuscript integration, release, peer review, or publication.

---

## 1. Four-pole profiles

Let `P_0` be an ordered cubic four-pole with terminal matchings

\[
M_i,M_j,M_k.
\]

For each matching `M_r`, let `E_r` be the zero-vertex direct reconnection and put

\[
J_r=\Sigma(E_r)=\{A,B_r,C_r\}.
\]

Let `cap_i` be the original two-vertex cap. Its exact profile is

\[
K_i=\Sigma(cap_i)=\{B_r,D_r:r\ne i\}.
\]

Assume every closed reconnection

\[
G_r=P_0\cup E_r
\]

is a smaller connected bridgeless cubic multigraph with a root-valued flow. Restriction gives

\[
\boxed{
\Sigma(P_0)\cap J_r\ne\varnothing
\qquad(r=i,j,k).}
\]

We prove

\[
\boxed{
\Sigma(P_0)\cap K_i\ne\varnothing.}
\]

---

## 2. Blocked-cap profile and fixed route

Assume for contradiction that

\[
\Sigma(P_0)\cap K_i=\varnothing.
\]

The complete ten-state Kempe-closed classification gives exactly

\[
\boxed{
\Sigma(P_0)=J_i
\quad\text{or}\quad
\Sigma(P_0)=J_i\cup L_i,}
\]

where

\[
L_i=\{C_j,D_i,C_k\}.
\]

Moreover every complementary support-pair challenge in every realised state of the blocked sector has the unique physical route

\[
\boxed{M_i.}
\]

This statement concerns the complete physical profile of `P_0`, not merely a coefficient orbit.

---

## 3. One cross reconnection and the cap trichotomy

Choose a root-valued flow on one cross closure, say

\[
G_j=P_0\cup E_j.
\]

Let the two reconnection edges carry roots

\[
p,q\in R_5.
\]

Reinsert the original cap topology formally. Its central value is

\[
r=p+q.
\]

Exactly one of:

1. `p,q` are distinct intersecting roots and `r` is a root;
2. `p=q` and `r=0`;
3. `p,q` are disjoint and `r` is a co-root.

The root case immediately gives a state in `K_i` on `P_0`, contradicting blocking. Hence only equality or DDD remains.

---

## 4. Horizontal rescue and oriented lock

### Equality

The six repair channels are the complementary support-pair systems separating the two equal marked edges. If one channel component contains exactly one marked edge, switch that component. The two marked roots become distinct intersecting roots and the original cap lifts on `P_0`.

### DDD

The four crossed repair-channel systems play the same role. A separating component converts the disjoint marked roots to an intersecting pair and lifts the original cap.

Thus a surviving branch is a full-channel lock:

- six-channel equality lock; or
- four-channel DDD lock.

The fixed-route theorem orients the lock by the ordered cap blocks. The DDD route cocycle restricts to a coboundary on this oriented transport subgroup; no independent return bit survives.

---

## 5. Forward equality descent

On the selected reconnection closure, the equality carrier has boundary

\[
r,r,r,r
\]

and the full-ten-triangle potential

\[
\mathcal P_{=}
=(E,\Phi,|V|).
\]

Every nonempty carrier admits:

1. a root-valued Pachner NNI strictly lowering `(E,Phi)`;
2. an equal-face cancellation lowering vertex order by two; or
3. a deliberately detected route/profile event.

By `ROOT_SURGERY_AUTOMATIC_CATEGORY_SAFETY_V1.md`, a root NNI is automatically connected and bridgeless. An equal-face cancellation is unconditional componentwise strict descent. There is no additional category-failure outcome.

Because the potential is a nonnegative lexicographic integer measure, the forward equality history is finite.

---

## 6. Forward DDD descent

Normalize the DDD boundary to

\[
12,12,34,34.
\]

The full-ten-triangle DDD potential is

\[
\mathcal P_{\mathrm{DDD}}
=(\Omega,|V|).
\]

Every nonempty DDD carrier admits:

1. a root-valued Pachner NNI strictly lowering `Omega`;
2. an equal-face cancellation lowering vertex order by two; or
3. a route/profile event.

Again all root surgeries are automatically category-safe.

`DDD_BOUNDED_RESIDUAL_ELIMINATION_V1.md` proves that no positive-vertex bounded DDD terminal exists. If no route event occurs, the only final source topology is a zero-vertex direct matching.

Thus the DDD history is finite and has the same terminal alphabet as equality.

---

## 7. Route-changing descendant terminal

Internal root surgeries preserve the complete ordered four-root boundary word but may alter physical terminal linkage.

Suppose the first route change occurs on a surgery descendant `P_t`. Before the switch its support-unordered boundary state still lies in

\[
J_i\cup L_i.
\]

The exact route table shows that any route `M_j` or `M_k` from either outer component lands in

\[
K_i.
\]

Therefore the descendant has a cap-compatible root state

\[
\tau\in\Sigma(P_t)\cap K_i.
\]

It is not legitimate to infer `tau in Sigma(P_0)` directly. Treat `tau` as a terminal input and apply repaired contextual inverse transfer through the finite surgery history. The result is:

\[
\Sigma(P_0)\cap K_i\ne\varnothing
\]

or strict recursive progress already consumed by the same theorem.

Hence every route event contradicts the blocked-cap hypothesis.

---

## 8. Direct-pairing terminal

Suppose the forward descent reaches a zero-vertex four-pole, one terminal matching `M`.

### Cross matching

If `M=M_j` or `M_k`, attaching `cap_i` gives the two-vertex theta graph. Assign one root triangle to its three parallel edges. This is a cap-compatible root terminal.

### Same matching

If `M=M_i`, attaching `cap_i` gives two loops joined by one bridge. This is not an original-graph category conclusion.

Choose distinct intersecting roots `p,q` on the two loops and value zero on the central bridge. The four branch word at the zero edge is

\[
p,p,q,q.
\]

One crossed `(0,2,2)` resolution changes the topology to the theta graph with root values

\[
p,q,p+q.
\]

Thus the same-matching terminal also gives a cap-compatible theta terminal after one root NNI.

In all three matchings, apply repaired contextual transfer to return the theta terminal to `P_0`.

Therefore every direct terminal contradicts blocking.

---

## 9. Repaired contextual inverse transfer

Let

\[
P_0\to P_1\to\cdots\to P_m
\]

be one finite forward history, and let a cap-compatible root state be given on `P_m`.

Reverse the history while root-valuedness is preserved. At the first failure:

1. a zero atom has an immediate root alternative and never persists;
2. a co-root atom relocates through critical overlaps as one nonbranching forced Petersen track;
3. all root/co-root normalization moves are automatically connected and bridgeless.

A recurrent reduced track contains a simple Petersen cycle.

- `C5,C9` reverse the oriented resolution sheet and cannot close.
- `C6` has the relative `(0,2,2)` star movie. Every boundary root state is the canonical star or one root NNI away; the star contains an equal-face dipole and enters strict target-order descent.
- `C8` decomposes into two seam-compatible `C6` cells and inherits the same strict descent.

Thus the finite same-order contextual graph has no nonterminal sink. A genuine cancellation lowers cap-target vertex order by two, so induction on target order is well founded.

This proof does not use the invalid implication from an abstract longitudinal cycle-multipole flow to a pointwise Ptolemy-history lift.

### Theorem 9.1 — terminal return

Every cap-compatible route or theta terminal returns to a root-valued state on the original cap context.

---

## 10. Full-channel cap-lift theorem

### Theorem 10.1

Let `P_0` be an ordered cubic four-pole and `cap_i` the original two-vertex cap. Assume all three zero-vertex reconnection closures are smaller connected bridgeless cubic multigraphs admitting root-valued `E_5` flows.

Then

\[
\boxed{
\Sigma(P_0)\cap K_i\ne\varnothing.}
\]

Equivalently, `P_0` and `cap_i` glue to a root-valued `E_5` flow.

### Proof

Assume the cap blocked. Section 2 gives the fixed-route outer profile. Section 3 reduces a selected cross-cover insertion to root, equality, or DDD. The root branch lifts immediately; horizontal rescue consumes every unlocked equality/DDD branch. A full lock enters one of the finite forward descents of Sections 5--6. Every possible endpoint is a route terminal or a direct terminal. Sections 7--9 return that terminal through the complete history to a state in `K_i` on `P_0`, contradicting blocking. ∎

---

## 11. Arbitrary-edge consequence

Let `G` be a vertex-minimal connected bridgeless cubic counterexample after the exact two-/three-/four-cut and bounded-base reductions. Delete the endpoints of any edge `uv` and let `P_0` be the complementary four-pole.

The simultaneous reconnection theorem makes all three zero-vertex closures connected, bridgeless, and smaller. Minimality supplies their root flows. Apply Theorem 10.1 to the original cap formed by `u,v,uv`.

The cap lifts and gives a root-valued flow on `G`, contradiction.

---

## 12. Correct well-founded order

The repaired proof uses:

### Outer graph order

Vertex number of closed cubic graphs, used for:

- small-cut shore completions;
- arbitrary-edge reconnection closures;
- contextual recursion after a genuine cancellation.

### Forward local potentials

\[
(E,\Phi,|V|)
\]

for equality and

\[
(\Omega,|V|)
\]

for DDD.

### Same-order contextual progress

The finite contextual state graph has no nonterminal sink because:

- zero atoms disappear;
- co-root tracks do not branch;
- odd cycles cannot close;
- even cycles force cancellation.

No category-return coordinate is required inside the root/one-co-root surgery layer.

---

## 13. Supersession table

### Retained from v1

- exact three-reconnection hypotheses;
- blocked-profile/fixed-route classification;
- root/equality/DDD cap trichotomy;
- horizontal rescue;
- equality and DDD potentials;
- oriented lock calibration.

### Corrected in v2

- descendant route change is a cap-compatible terminal, not immediate membership in `Sigma(P_0)`;
- same-matching direct terminal is zero-resolved to theta, not treated as an original category exit;
- root surgeries have no independent category branch;
- first-failure co-root normalization has no category branch;
- even Petersen tracks are consumed by relative star NNI plus cancellation, not by pointwise abstract tube relabelling;
- DDD has no generic positive-vertex bounded residual.

### Controlling downstream reference

Use this dossier, not `ORIENTED_FULL_CHANNEL_LOCK_ELIMINATION_SYNTHESIS_V1.md`, for the authorial full-channel theorem and dependency order.

---

## 14. Trust boundary

### Exact at the authorial research level

- complete fixed-route reduction;
- complete horizontal rescue/full-lock alternative;
- terminating equality and DDD forward descents;
- automatic source-category safety of their root moves;
- exact route-terminal return;
- exact direct-terminal theta rootification and return;
- repaired contextual inverse transfer;
- full-channel cap lift;
- arbitrary-edge inverse-dipole contradiction.

### Not claimed

- downstream proof-development reconstruction;
- independent audit;
- curation or canonical status;
- Lean verification;
- manuscript integration;
- release, arXiv, DOI, peer review, or publication.

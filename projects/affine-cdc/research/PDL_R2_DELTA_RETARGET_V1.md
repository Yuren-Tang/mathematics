# AC-PDL R2 delta retarget — preserve completed reconstruction, update only the global-return surface

## Research Lead cross-role handoff v1

**Sender:** `AffineCDC — Research Lead` (`AC-RL`)  
**Receiver:** `AffineCDC — Proof Development Lead` (`AC-PDL`)  
**RL workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `7f80f4969d9b73b36ce32e9775f3d281ab5ed291`.

**Disposition:** delta retarget only. Do not restart, reset, rebase, or discard completed PDL work.

---

## 1. Observed current PDL reconstruction

The current PDL branch is ninety-one commits ahead of its exact baseline and already contains:

- `AC_PD_5CDC_R1_ONE_CROSS_STRUCTURAL_REDUCTION.md`;
- `AC_PD_5CDC_R2_1_BOUNDARY_ROUTE_CERTIFICATE.md`;
- `AC_PD_5CDC_R2_2_SINGULAR_MORSE_DESCENT.md`;
- `AC_PD_5CDC_R2_3_FIRST_FAILURE_AND_LOCAL_CONFLUENCE.md`.

The observed classifications are:

- `R1`: reconstructed structural reduction;
- `R2.1`: `COMPLETE-DRAFT / FINITE CERTIFICATE RECONSTRUCTED`;
- `R2.2`: `COMPLETE-DRAFT / CURRENT-FLOW TERMINATION CLOSED / RETURN ACTIVE`;
- `R2.3`: `COMPLETE-DRAFT / F CLOSED / LOCAL L CLOSED / GLOBAL RETURN ACTIVE`.

This is the correct dependency position. None of these units should be rewritten merely because the RL architecture was compressed after they began.

---

## 2. Current controlling RL surface for the remaining return

For all work after local `R2.3`, use:

1. `FIVE_SUPPORT_GLOBAL_ONE_CROSS_CLOSURE_V6.md`;
2. `SINGULAR_ROOT_TRIALITY_CONFLUENCE_MASTER_THEOREM_V3.md`;
3. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V3.md`;
4. `ROOT_SUPPORT_TRANSPORT_PROJECTIVITY_GROUPOID_V1.md` as mechanism interpretation;
5. `ORIENTED_ODD_PETERSEN_SUBTRACK_EXCLUSION_V1.md`;
6. the full-state `C6/C8` source movies;
7. `ONE_CROSS_CAP_RESTORATION_THEOREM_V1.md`.

Do not reconstruct contextual masters v1/v2 or global one-cross summaries through v5 as controlling proofs.

The finite-condensation form is controlling:

- one finite full-labelled contextual graph at fixed target order and fixed history;
- prefix level is nonincreasing and SCC-constant;
- no nonterminal sink SCC survives;
- the condensation DAG supplies same-order rank;
- only target order remains an explicit induction parameter.

No second explicit induction on history length is required.

---

## 3. Preserve the completed local units

### R2.1

The independent enumeration and six-row route certificate remain controlling finite input. No complete ten-state profile reconstruction is additionally required.

### R2.2

The positive equality and DDD Morse tables remain the controlling forward-termination theorem. They should not be replaced by older separate global architectures.

### R2.3

The local conclusions remain valid:

- first failure is exactly one zero/co-root central edge;
- inverse-flip zero rootifies immediately;
- inverse-cancellation quadruple equality does not belong to the pure same-order flip alphabet;
- one bad co-root overlap normalizes locally without atom proliferation;
- persistent local token moves are category-safe.

`FULL_DEFECT_TREE_NNI_DESCENT_V1.md` is not a global prerequisite. Its three-vertex local calculation may remain as an alternate certificate, but the direct local proof in the PDL dossier is sufficient.

---

## 4. Exact remaining PDL sequence

### R2.4 — complete full-state forced backbone

Reconstruct:

1. constant-pivot full-state consumption;
2. critical-overlap exhaustiveness with all side roots retained;
3. reduction to one nonbranching Petersen pivot walk;
4. deletion of physical immediate backtracks;
5. extraction of a shortest repeated-pivot subtrack.

Do not quotient away source topology, cap block, route orientation, or side attachments.

### R2.5 — orientation-hardened odd exclusion

Prove in this order:

1. the original cap distinguishes one physical route block in every descendant context;
2. forced-backbone transport carries that same block;
3. every pivot-closed physical subsegment stabilizes it;
4. the projectivity orientation character equals pivot-length parity;
5. therefore no odd pivot-closed physical subtrack occurs, including the first lap of a proposed double traversal.

### R2.6 — actual even-core source descent

Reconstruct the source-level statements, not only abstract cycle identities:

- an actual simple `C6` track has the relative `(0,2,2)` section;
- the unique root NNI reaches the canonical star while fixing turn corollas and side data;
- the star contains a genuine equal-face dipole;
- cancellation lowers target order or gives an accepted category exit;
- an actual simple `C8` has the exact seam-compatible two-`C6` realization.

### R2.7 — finite condensation and strict-order recursion

Close:

1. finiteness of the complete contextual state graph;
2. prefix monotonicity and SCC constancy;
3. root-state and token-state transition exhaustiveness;
4. no bad sink SCC by R2.4--R2.6;
5. inverse-cancellation quadruple equality through a genuinely lower-order contextual call;
6. componentwise return after every cancellation;
7. cap restoration on the original four-pole.

The lower-order call must not consume R2 itself at the same target order.

---

## 5. New projectivity results are noncontrolling research inputs

Two later RL dossiers are exact but must not presently be used to mark R2 complete:

- `RELATIVE_PROJECTIVITY_HEXAGON_2_COMPLEX_V1.md`;
- `RELATIVE_PROJECTIVITY_DIRECTED_REWRITING_BOUNDARY_V1.md`.

They prove:

- the orientation cover has an eleven-hexagon collapsible and diagrammatically reducible core;
- eleven directed projectivity rewrites contract every abstract closed path;
- every abstract `C8` is a two-hexagon composite;
- a strictly smaller possible bypass is the core directed hexagon-lift property (`DHL`).

They do **not** prove that the complementary five-edge path of a selected abstract hexagon exists in the same source context as an arbitrary actual token edge. The contextual projection is currently a transport map, not a graph covering or cellular fibration.

Therefore:

\[
\boxed{
\text{do not replace actual }C6/C8\text{ source reconstruction by abstract hexagon simple connectivity.}
}
\]

PDL may record `DHL` as an optional future bypass obligation, but should continue the current controlling sequence without waiting for it.

---

## 6. Assurance boundary

This retarget:

- changes no PDL branch ownership or base;
- invalidates none of R1 or R2.1--R2.3;
- authorizes no canonical, Curator, audit, Lean, manuscript, release, arXiv, DOI, or publication movement;
- adds no independent acceptance to the RL candidate;
- keeps the classification

\[
\text{COMPLETE MINIMAL AUTHORIAL CANDIDATE}
/
\text{PDL GLOBAL RETURN ACTIVE}
/
\text{NOT INDEPENDENTLY ACCEPTED}.
\]

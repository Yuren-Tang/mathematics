# R2 contextual return feeds the one-cross induction through four exact terminal types

## Research Lead downstream consumer map v1

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`

**Consumes:**

- `ONE_CROSS_RECONNECTION_EXISTENCE_V1.md` and the PDL reconstruction `AC_PD_5CDC_R1_ONE_CROSS_STRUCTURAL_REDUCTION.md`;
- `ONE_CROSS_SINGULAR_FIXED_ROUTE_REDUCTION_V1.md` and `AC_PD_5CDC_R2_1_BOUNDARY_ROUTE_CERTIFICATE.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6.md` together with `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_1_ADDENDUM.md`;
- `ROUTE_CHANGE_TERMINAL_CONTEXTUAL_TRANSFER_V1.md`;
- `BOUNDED_DIRECT_PAIRING_CAP_TERMINAL_V1.md`;
- `TERMINAL_UNWIND_FROM_CHILD_SEPARATOR_SOLUTION_V1.md`;
- `GLOBAL_SMALL_CUT_COMPLETION_AND_GLUING_V1.md`;
- `GENERAL_BRIDGELESS_FIVE_CDC_OUTER_SHELL_V1.md`.

**Supersedes for downstream use:**

- Sections 5--6 of `ONE_CROSS_CAP_RESTORATION_THEOREM_V1` insofar as they treated a genuine cancellation as an automatically completed lower-order exit and used the old confluence theorem/rank;
- the extension half of `SIMPLE_EDGE_FIVE_SUPPORT_REDUCIBILITY_V1` until reconstructed with the present result-type split;
- any statement that an unspecified `separator/category exit` is automatically enough for the outer induction;
- any inference that the older Programme-B/B9 global source obligations are additional hypotheses of the compressed one-cross route.

**Status:** exact downstream interface conditional on proof-development completion of R2.6--R2.7. Once the witnessed contextual-return theorem is independently reconstructed, its four result types all produce the exact cap restoration required by R1. The resulting cubic theorem feeds the already closed general bridgeless outer shell without another five-support obstruction.

---

## 1. Exact R1 consumer

Let `G` be a finite connected loopless bridgeless cubic multigraph of order `N`, and let `e=uv` be a simple edge.

R1 supplies one selected cross reconnection closure

\[
G_\times
\]

which is connected, loopless, bridgeless, cubic, and satisfies

\[
|V(G_\times)|=N-2.
\]

The induction hypothesis gives a root-valued `E_5` flow on `G_\times`.

The exact extension proposition needed by R1 is:

> From **every** such inherited root flow on the selected cross closure, produce a root-valued flow on the original cap closure `G`, or a terminal branch whose already proved source theorem produces such a flow.

An exit from an internal state machine is insufficient unless its terminal semantics are one of Sections 3--6 below.

---

## 2. Boundary trichotomy

Let `cap_i` be the original two-vertex cap and let the selected cross be `E_j`, `j\ne i`.

The two cross edges carry roots `p,q`. Their boundary state lies in

\[
J_j=\{A,B_j,C_j\}.
\]

### Immediate root case

If `(p,q)` is the intersecting state `B_j`, then

\[
B_j\in K_i=\Sigma(cap_i).
\]

One global support permutation aligns the five terminal traces, and the original cap glues root-valuedly.

### Equality/DDD cases

If the selected state is `A` or `C_j`, R2.1 gives:

1. an initial separating support-component rescue which directly produces a `K_i` boundary state; or
2. one oriented fixed-route equality/DDD lock.

Only the second enters the witnessed forward history and contextual-return theorem.

---

## 3. Result Type I — continuing witnessed return

A successful child call returns:

- a finite decorated history from the inherited child flow;
- a root-valued or one-standard-atom predecessor-order state;
- complete ordered incidences, cap block, route, side roots and surviving/suspended ancestor marks.

This is a **continuing** result. It resumes the parent fixed-route search.

Its source correctness uses:

- active-diagonal six-leaf lift;
- support-switch source pop in terminal collar form;
- ordered `2--0` incidence transport;
- normalized innermost-bubble compression;
- R2.4--R2.6 fixed-order track consumers;
- the resolved-call rank or witnessed mutual induction.

This branch is not a theorem terminal and is not converted to an arbitrary new flow.

---

## 4. Result Type II — cap-compatible terminal root state

A descendant terminal root state belongs to the original cap profile `K_i` in either of two ways.

### Route-change terminal

Before the first route change, the ordered boundary word is fixed. The exact route table shows that any alternative physical route from the outer fixed-route components lands in `K_i` on the current descendant.

This is a cap-compatible **terminal for contextual return**, not yet a profile state on the original four-pole.

Apply the witnessed contextual-transfer theorem through the complete normalized history. It gives:

- a `K_i` root state on the original four-pole; or
- another terminal of Types III--IV below.

### Rootification terminal

A fixed-order root-state exit of the singular track is similarly transferred to the target topology. If its boundary is in `K_i`, the original cap glues.

### Theorem 4.1 — cap-terminal return

Every cap-compatible descendant root terminal returns to a cap-compatible root state on the original four-pole, or leaves through one of the proved bounded/separator terminal types.

---

## 5. Result Type III — bounded direct/category terminal

### Zero-vertex direct matching

Closing the terminal direct matching by `cap_i` gives exactly:

1. the cap matching: two loops joined by a bridge, hence singleton-cut/loop outer-shell exit;
2. a cross matching: the root-soluble theta multigraph with roots `12,13,23`.

There is no third graph.

### Other bounded local degenerations

The only accepted local category degenerations are explicitly named finite objects:

- loop or parallel-edge collapse;
- one-/two-vertex cap or theta;
- triangle or acyclic low-port shore;
- bounded direct root/one-atom normalisation already carrying an explicit root solution or reducing to one of these objects.

A generic `category-safe` label without one of these dispositions is not a terminal theorem.

### Theorem 5.1 — bounded terminal closure

Every accepted bounded terminal produces an explicit root solution or enters the already closed bridge/loop/small-cut outer shell.

---

## 6. Result Type IV — separator/category child terminal

A separator encountered inside a lower-order child is a **terminal existential result**, not a continuing inherited-flow return.

Let the child target have order `n<N`.

1. Use the already available ordinary proposition `P_n` to choose one root flow on the child target.
2. Evaluate the stored parent insertion interface on that actual flow.
3. Apply the root/equality/good-disjoint/missing-index endpoint table.
4. Restore the required parent topology by the fixed-order consumer.
5. Repeat outward through the finite frame stack.

No separator shore or cut size is transported through ancestor insertions.

At the outermost target, an accepted separator/category terminal must be one of:

- a cyclic 2-cut with smaller edge completions and root alignment;
- a cyclic 3-cut with smaller one-vertex completions and triangle alignment;
- a cyclic 4-cut with smaller cap completions and the proved profile gluing theorem;
- an acyclic low-port bounded shore;
- a singleton cut/bridge, which lies outside the bridgeless counterexample domain and is handled by the outer shell.

### Theorem 6.1 — terminal stack unwind

A descendant separator/category result yields, after finitely many outward interface lifts, either:

1. a root flow on the outermost original target;
2. a cap-compatible root terminal;
3. one of the explicitly solved bounded/small-cut outcomes above.

It never resumes the inherited child search and does not require cut persistence.

---

## 7. Correct cap-restoration theorem

### Theorem 7.1 — one-cross cap restoration, corrected consumer form

Assume:

1. lower-order ordinary root solubility;
2. the R2.1 boundary/fixed-route certificate;
3. equality/DDD witnessed forward termination;
4. R2.3--R2.6 source-level first-failure and track theorems;
5. the R2.7 witnessed contextual-return theorem with the result-type split above.

Then every inherited root flow on one selected valid cross closure extends to a root-valued flow on the original cubic graph.

### Proof

Use the boundary trichotomy.

- `B_j`: glue the cap immediately.
- Initial separating rescue: glue the cap immediately.
- Fixed-route lock: execute the witnessed R2 strategy.

Each completed macro is either continuing Type I or reaches Types II--IV. Type I resumes the finite strategy. Type II returns a `K_i` state and glues the cap. Types III--IV produce an explicit outer root solution or reduce to an already closed bounded/small-cut theorem.

The resolved-call attractor rank or the equivalent witnessed mutual induction proves that the continuing branch cannot run indefinitely. Hence one terminal branch occurs and `G` is root-soluble. ∎

This theorem replaces the obsolete strict-cancellation paragraph of `ONE_CROSS_CAP_RESTORATION_THEOREM_V1`.

---

## 8. Simple-edge induction

Combine Theorem 7.1 with R1.

### Theorem 8.1 — simple-edge reducibility

For every simple edge of a connected loopless bridgeless cubic multigraph `G`, at least one selected cross reduction `G_\times` satisfies

\[
G_\times\text{ root-soluble}
\Longrightarrow
G\text{ root-soluble}.
\]

### Theorem 8.2 — connected cubic theorem

Every finite connected loopless bridgeless cubic multigraph has a root-valued `E_5` flow.

### Proof

Induct on vertex order.

- If there is no simple edge, the graph is theta and has the explicit root triangle.
- Otherwise choose a simple edge, use R1 to select a valid smaller cross closure, solve it by induction, and apply Theorem 8.1. ∎

No all-three-reconnection profile theorem, cyclic-five-connectivity reduction or Programme-B Type-H theorem is required by this compressed route.

---

## 9. General bridgeless outer shell

The cubic theorem implies the standard finite five-member even-subgraph statement.

1. Delete loops from the original finite-active graph.
2. Form the loopless bridgeless cubic port-cycle expansion.
3. Apply the cubic theorem componentwise using the same five support indices.
4. Convert vertex-even supports to cut-even supports upstairs.
5. project the same five indexed supports through the collapse map;
6. reinsert every loop into two fixed supports.

Every arrow preserves the number of support members and exact double coverage.

### Theorem 9.1 — outer-shell implication

The connected cubic theorem implies that every finite bridgeless multigraph has a five-cycle double cover in the five-even-subgraph sense.

This outer shell introduces no new local extension or source-localisation obligation.

---

## 10. Relation to the Programme-B/B9 route

`AC_PD_B9_CORRECTED_GLOBAL_FRONTIER_ASSEMBLY.md` records a different historical route through Fano-flow selection, Type T/H and source-localisation obligations.

Its six open obligations are not hypotheses of Theorems 7.1--9.1.

Conversely, an eventual proof of the compressed one-cross route does not make every intermediate Programme-B mechanism theorem true; it makes that route unnecessary for the global endpoint.

The two proof DAGs must remain separate:

- **compressed route:** R1 + R2 + theta + vertex induction + outer shell;
- **Programme-B route:** fixed-data/Fano mechanisms + Package D/E source theorem.

Only the first is controlled by the present dossier.

---

## 11. Required PDL synthesis after R2.6

After reconstructing R2.6, PDL should produce one downstream theorem which explicitly checks:

1. every R2 macro result is assigned to Types I--IV;
2. every `route/profile` terminal is a descendant `K_i` state or immediate initial rescue;
3. every `bounded` terminal names its finite graph and root/bridge disposition;
4. every `separator/category` terminal is handled by ordinary-child terminal unwind and, at the outermost level, by an exact low-cut theorem;
5. no terminal existential solution is used to resume a continuing inherited-flow branch;
6. no all-three-reconnection or B9 hypothesis is inserted into the one-cross induction;
7. the final output is a root flow on the original cap closure, not merely an exit from the contextual state graph.

---

## 12. Assurance boundary

### Exact here

- the precise R1 input and R2 output needed for simple-edge induction;
- the four result types of contextual return;
- route-change descendant-to-original transfer;
- direct bounded terminal disposition;
- separator terminal unwind without cut persistence;
- exact outermost 2/3/4-cut and bounded categories;
- simple-edge vertex induction;
- separation of the compressed and Programme-B proof DAGs;
- support-count-preserving general bridgeless outer shell.

### Conditional inputs

- PDL-complete R2.6 and final R2.7 synthesis;
- independent verification of all source movies and terminal consumers.

### Not claimed

- present PDL completion;
- independent audit;
- Lean verification;
- manuscript integration;
- curation, release, arXiv, DOI, peer review or publication.

# Solution-valued terminal addendum to the R2-to-R1 interface

## Research Lead downstream correction v1

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `7ae200bc95619914447f9e2d0bd1fb8dc7fdac40`

**Controls together with:**

- `R2_TO_R1_CAP_RESTORATION_AND_OUTER_SHELL_INTERFACE_V1.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_3_TERMINAL_SEMANTICS_ADDENDUM.md`;
- `LEVEL_SENSITIVE_TERMINAL_COLLAPSE_AND_OUTERMOST_UNWIND_V1.md` v1.1.

**Corrects exactly:** the four result types in the parent R2-to-R1 file are internal state-machine cases, not four external conclusions. R1 consumes only an actual root flow on the original cap closure.

**Status:** exact authorial downstream result-type correction, conditional on PDL reconstruction of v6.2--v6.3 and independent assurance.

---

## 1. Exact R1 input and output

Let `G` be a connected loopless bridgeless cubic graph of order `N`, let `e` be a simple edge, and let `G_x` be the valid connected loopless bridgeless cross closure selected by R1, of order `N-2`.

For every specified root flow

\[
\lambda:E(G_x)\to R_5,
\]

R2 must prove

\[
\boxed{\exists\,\Lambda:E(G)\to R_5\text{ root-valued}.}
\]

An internal route, bounded, separator, category, or descendant-cap result is not an acceptable output to R1.

---

## 2. Internal result census

During R2 the following control values may occur.

### I. Continuing witnessed return

A child history starts at the inherited child flow, lifts through the stored frame, and resumes the parent search.

### II. Cap-compatible descendant root state

A route change or rootification yields a `K_i` boundary state on a surgery descendant. It remains a root state and is carried through the remaining fixed-order/original-prefix restoration until the original topology is reached.

### III. Bounded direct/category certificate

At a proper lower-order frame this triggers an exact-target `P_m` reset. At the outermost frame it is consumed by the direct-matching/theta, acyclic-shore, loop, bridge, or bounded theorem.

### IV. Separator/category child certificate

This switches irreversibly to terminal mode. It is not propagated as a cut. The current proper lower-order target is solved by `P_m`, and the call stack is unwound framewise. At the outermost level only the exact two-/three-/four-cut or outer-shell theorem is absorbing.

---

## 3. Internal-to-external conversion

### Theorem 3.1 — complete result consumer

Every internal R2 control value finitely yields one root flow on the original graph `G`.

### Proof structure

- Type I remains in continuing mode and is ranked by `(N,d_N)`.
- Types III--IV below order `N` are replaced by root flows on exact stored targets via lower `P_m`.
- Type II is carried as a root boundary state through internal topology restoration.
- The final order-`N` frame consumes each exact terminal by a named cap, theta, low-cut, bounded-shore, loop, bridge, or outer-shell theorem.

The output is always `Lambda`, never an exit token. ∎

---

## 4. Correct cap-restoration theorem

### Theorem 4.1 — solution-valued one-cross cap restoration

Assume:

1. lower-order ordinary root solubility;
2. the R2.1 boundary/fixed-route certificate;
3. the witnessed equality/DDD forward strategy;
4. R2.3--R2.6 source-level local and track theorems;
5. the v6--v6.3 contextual-transfer and terminal-unwind theorem.

Then every specified inherited root flow on the selected valid cross closure extends, in the existential theorem sense, to a root-valued flow on the original cubic graph.

The extension certificate may be a continuing decorated history or a terminal framewise construction. Pointwise preservation of the inherited flow is required only in continuing mode.

---

## 5. Simple-edge induction

The R1 induction now has no result-type ambiguity.

1. Select one valid smaller cross closure.
2. Solve it by lower-order `P`.
3. Feed that specified root flow to Theorem 4.1.
4. Receive a root flow on the original graph.

Thus the proof of `P_N` contains no implicit coercion from `accepted exit` to `root solution`.

---

## 6. PDL obligations

The final proof-development synthesis must verify:

1. every local/macro output belongs to internal Types I--IV;
2. the continuing/terminal mode switch is irreversible;
3. all intermediate Types III--IV are reset by exact-target lower `P_m`;
4. descendant Type II states preserve the ordered `K_i` boundary through internal restoration;
5. every outermost terminal names one exact consumer;
6. the exported R2 theorem has only the root-flow conclusion;
7. R1 and `P_N` consume that conclusion literally.

---

## 7. Assurance boundary

### Exact here

- external R2 result type;
- internal classification of the former four result types;
- their conversion to one original root flow;
- corrected R1 consumer and simple-edge induction interface.

### Still required

- PDL reconstruction;
- independent audit;
- verification of every named terminal theorem in the actual branch.

### Not claimed

- present PDL completion;
- accepted global cap restoration or universal five-support theorem;
- Lean, manuscript, curation, release, arXiv, DOI, peer review, or publication status.

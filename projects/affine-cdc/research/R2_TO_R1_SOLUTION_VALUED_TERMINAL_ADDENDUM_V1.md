# Solution-valued terminal addendum to the R2-to-R1 interface

## Research Lead downstream correction v1.1

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `447e4936dc90f4f263b9691e6632117c24fe30f4`

**Controls together with:**

- `R2_TO_R1_CAP_RESTORATION_AND_OUTER_SHELL_INTERFACE_V1.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6_3_TERMINAL_SEMANTICS_ADDENDUM.md` v6.3.1;
- `LEVEL_SENSITIVE_TERMINAL_COLLAPSE_AND_OUTERMOST_UNWIND_V1.md` v1.2.

**Corrects exactly:** the four result types in the parent R2-to-R1 file are internal control cases, not external conclusions. R1 consumes only an actual root flow on the original cap closure. The earlier framewise terminal wording is superseded by outermost-live-child collapse.

**Status:** exact authorial downstream result-type correction, conditional on PDL reconstruction of v6.2--v6.3.1 and independent assurance.

---

## 1. Exact R1 interface

Let `G` be a connected loopless bridgeless cubic graph of order `N`, let `e` be a simple edge, and let `G_x` be the valid connected loopless bridgeless cross closure selected by R1, of order `N-2`.

For every specified root flow

\[
\lambda:E(G_x)\to R_5,
\]

R2 must prove

\[
\boxed{\exists\,\Lambda:E(G)\to R_5\text{ root-valued}.}
\]

No route, bounded, separator, category, or descendant-cap token is exported to R1.

---

## 2. Internal cases

### I. Continuing witnessed return

A lower child history begins at its inherited flow, lifts source-faithfully, and resumes the parent strategy.

### II. Cap-compatible descendant root state

A route change or rootification yields a `K_i` boundary root state on a surgery descendant. It is carried through the compressed fixed-order parent prefix until the original topology is reached.

### III. Bounded direct/category certificate

If it occurs inside the first live lower-order child execution, it triggers abandonment of that whole child execution and lower-order ordinary solution of its exact outermost child target. If it occurs after the single outer pop at order `N`, it is consumed by the exact theta, bounded-shore, two-cut, loop, bridge, or outer-shell theorem.

### IV. Separator/category child certificate

It likewise triggers outermost-child collapse. The child cut is not transported. Only a root flow on the exact outermost live child target is selected by lower `P` and inserted through the single stored order-`N` frame.

---

## 3. Internal-to-external conversion

### Theorem 3.1 — complete solution-valued result consumer

Every internal R2 case finitely yields one root flow on the original graph `G`.

### Proof structure

- Type I remains in continuing mode and is ranked by `(N,d_N)`.
- Types III--IV inside a lower child abandon the complete child execution.
- Lower `P` supplies a root flow on the exact first live child target of order at most `N-2`.
- The actual roots on its two stored output edges determine one order-`N` insertion/normalization.
- Every earlier completed order-lowering call in the parent prefix has a witness and is compressed to fixed order.
- Type II and one-atom states are returned through that fixed-order prefix.
- The exact outer scheduler census is absorbed by cap, theta, two-cut, bounded-shore, loop, bridge, or outer-shell theorems.

The output is always `Lambda`, never an exit token. ∎

---

## 4. Solution-valued cap restoration

### Theorem 4.1

Assume:

1. lower-order ordinary root solubility;
2. the R2.1 boundary/fixed-route certificate;
3. the witnessed equality/DDD forward strategy;
4. R2.3--R2.6 source-level local and track theorems;
5. v6--v6.3.1 contextual transfer with outermost-child terminal collapse.

Then every specified inherited root flow on the selected valid cross closure yields, in the existential theorem sense, a root-valued flow on the original cubic graph.

The certificate is either a continuing decorated history or a terminal outer-collapse/fixed-order-return construction. Pointwise preservation of the inherited flow is required only in continuing mode.

---

## 5. Simple-edge induction

1. R1 selects one valid smaller cross closure.
2. Lower `P` solves it.
3. Feed that specified root flow to Theorem 4.1.
4. Receive a root flow on the original graph.

The proof of `P_N` contains no implicit coercion from `accepted exit` to `root solution`.

---

## 6. PDL obligations

PDL must verify:

1. every local/macro output belongs to Types I--IV;
2. the continuing/terminal switch is irreversible;
3. Types III--IV collapse the complete outermost live child execution;
4. every earlier completed parent call has a witness and compresses to fixed order;
5. the one actual outer pop uses the selected lower root flow;
6. Type II preserves the ordered `K_i` word through fixed-order return;
7. the outer scheduler terminal census is complete and named;
8. the exported R2 theorem has only the root-flow conclusion;
9. R1 and `P_N` consume that conclusion literally.

---

## 7. Assurance boundary

### Exact here

- external R2 result type;
- internal case classification;
- outermost-child terminal conversion;
- corrected R1 consumer and simple-edge induction interface.

### Still required

- PDL reconstruction;
- independent audit;
- verification of every named terminal theorem and fixed-order compression interface.

### Not claimed

- present PDL completion;
- accepted global cap restoration or universal five-support theorem;
- Lean, manuscript, curation, release, arXiv, DOI, peer review, or publication status.

# Controlling addendum to contextual-transfer master v6

## Research Lead v6.1 — suspended marks and normalized FOC

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`

**Controls together with:**

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V6.md`.

**Supersedes exactly:**

- the unqualified central-mark wording in v6 Section 5;
- the overbroad flattened-history reading in v6 Sections 6--7;
- the v6 downstream statement that PDL had not yet reconstructed the variable-order interfaces or R2.4--R2.5.

**Parent correction:**

- `SUSPENDED_ANCESTOR_MARK_AND_SWITCH_POP_SCOPE_CORRECTION_V1.md`.

**Status remains:**

`COMPLETE AUTHORIAL R2.7 CANDIDATE / R2.6 PDL RECONSTRUCTION AND INDEPENDENT ASSURANCE REQUIRED`.

The resolved-call rank `(N,d_N)` and original-prefix induction of v6 remain controlling. No mark count or child-prefix coordinate enters that rank.

---

## 1. Central marks

Forward cancellation of a central marked edge deletes both marked darts in the raw lower-order graph. There is no pointwise lower-order descendant.

For a successful paired cancellation/child/return bubble, however, every child state has a predecessor-order raw insertion state with one central dart pair. If the deleted mark belongs to a still-live ancestor frame, carry it on those central darts as a **suspended ancestor mark**.

This mark is transported through:

- lifted support switches by the central parity formula;
- disjoint/exterior NNIs pointwise;
- active-diagonal NNIs by the explicit six-leaf labelled movie;
- `A/B/C` endpoint normalization by the labelled alternative-insertion paths.

Hence a successful bubble has a boundary-equivalent marked fixed-order replacement. This is not automatic resurrection in the lower graph; it is an explicit relative-history construction.

Call-local marks may disappear with the eliminated frame. Surviving ancestor marks are retained when the surrounding complete boundary requires them.

Therefore raw central-mark deletion is not an unconditional global descent coordinate.

---

## 2. Witnessed recursive statement

Every lower-order cancellation call used by the controlling proof begins from the inherited post-cancellation root flow and returns:

1. an accepted child exit; or
2. a finite decorated history generated by the declared root NNIs, support switches and equal-face moves.

A bare existence theorem producing an unrelated terminal child flow is not sufficient.

One valid proof-development organisation is simultaneous strong induction:

- `Q_N`: witnessed relative cap restoration from every specified inherited root flow;
- `P_N`: ordinary root solubility;
- prove `Q_N` from lower `Q_n` first;
- prove `P_N` from lower `P_n` and same-level `Q_N` second.

This is compatible with the resolved-call macro graph: the `Q_n` witness is the finite expansion of one lower-order call edge.

---

## 3. Normalized support-switch form

`WELD_SUPPORT_SWITCH_SOURCE_POP_V1.md` gives the correct source lift, but a raw support-switch correction with a singular central insertion is not an ordinary Ptolemy cell.

Choose the controlling witnessed history so that support switches occur only as:

1. initial separating rescue giving the cap;
2. route/profile exit;
3. horizontal endpoint rootification;
4. bounded theta/direct terminal normalization.

No nonterminal support switch is retained inside a locked recurrent Morse episode.

If an innermost child terminates by a support switch, combine:

- the lower root-valued switch;
- its raw parent-order lift;
- the returned `A/B/C` insertion row;
- immediate insertion normalization;

into one **switch--pop collar**.

The collar is a normalized/accepted short endpoint of an open singular track. It is not an internal cell of a closed Ptolemy component.

---

## 4. Correct fixed-order compression alphabet

After innermost-call elimination, put the history in the following normal form:

- nonterminal interior moves are root-NNI/Ptolemy cells;
- raw `A/C` insertions are normalized inside bounded local macros;
- terminal support switches are switch--pop endpoint collars;
- surviving ancestor marks are suspended through successful bubbles;
- complete labelled seams glue by identity.

Consequently every persistent closed singular component contains only the cell alphabet accepted by the retained closed-track theorem.

Open components may end at:

- root-normalized local cells;
- switch--pop collars;
- accepted exits;
- the unresolved original outer endpoint.

Thus:

- closed components use the root-annulus theorem;
- normalized open components use the root strip;
- repeated unresolved endpoints use the periodic root-seam discharge.

No raw support-switch atom cell is consumed as though it were a Ptolemy cell.

---

## 5. Impact on the resolved-call rank

The resolved-call parent graph `M_N` uses the normalized macro alphabet above.

If a nonexit sink SCC existed:

1. expand one cycle into a finite witnessed nested episode;
2. eliminate innermost calls;
3. normalize support-switch/pop cells into endpoint collars;
4. obtain a fixed-order Ptolemy/root-token recurrence;
5. apply the closed/open/periodic/arborescence disposition.

This contradicts a minimal nonexit cycle.

Therefore the shortest-distance rank

\[
d_N(x)=\operatorname{dist}_{M_N}(x,\mathcal X_N)
\]

remains valid. No permanent mark decrease is needed.

---

## 6. Current PDL return

PDL has independently supplied complete drafts for:

- witnessed mutual `P_N/Q_N` induction;
- child-context fidelity and the disconnected-child two-cut exit;
- witnessed innermost-bubble compression;
- full-labelled dart genealogy and suspended ancestor marks;
- normalized fixed-order consumer compatibility (`HW1--HW3`);
- R2.4 full-state forced Petersen backbone;
- R2.5 orientation-hardened odd `C_5/C_9` exclusion.

The remaining proof-development burden before R2.7 completion is:

1. R2.6 source-level `C_6/C_8` annulus, open strip and periodic-seam reconstruction;
2. final PDL contextual-transfer synthesis with the normalized alphabet;
3. literal cap/route hypothesis audit;
4. independent audit of the complete dependency graph.

---

## 7. Assurance boundary

### Retained from v6

- exact inverse tables;
- active-diagonal and support-switch source lifts;
- witnessed innermost-call elimination;
- variable-order compression;
- finite resolved-call macro graph;
- rank `(N,d_N)`;
- original-prefix contextual transfer architecture.

### Corrected here

- ancestor central marks may be suspended in successful bubble replacements;
- raw deletion is not global mark descent;
- support switches are terminal/root histories or endpoint collars;
- closed Ptolemy cores contain only root-NNI/critical-overlap cells;
- current PDL reconstruction state.

### Not promoted

- PDL R2.7 completion;
- independent assurance;
- cap restoration/global five-support acceptance;
- Lean, manuscript, curation, release or publication status.

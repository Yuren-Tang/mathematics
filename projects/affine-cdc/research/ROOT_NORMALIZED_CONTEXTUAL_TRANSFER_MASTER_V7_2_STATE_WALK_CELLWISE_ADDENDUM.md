# State-walk cellwise addendum to first-cancellation master v7

## Research Lead master v7.2

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workspace:** `Yuren-Tang/mathematics:research/affine-cdc-five-cdc-v1`  
**Exact parent head:** `901df4fe6316c39b6bc66a3361b1898435c76cfb`

**Controls together with:**

- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_FIRST_CANCELLATION_RETURN.md`;
- `ROOT_NORMALIZED_CONTEXTUAL_TRANSFER_MASTER_V7_1_SCOPE_AND_ALPHABET_ADDENDUM.md`;
- `FIRST_CANCELLATION_TARGET_CAP_TERMINALISATION_V1.md`;
- `FIRST_CANCELLATION_SINGLE_POP_TARGET_TOPOLOGY_SCOPE_CORRECTION_V1.md`;
- `FIRST_CANCELLATION_FIXED_ORDER_NO_REOPENING_V1.md`;
- `CELLWISE_ROOT_SEAM_AND_CONSTANT_RUN_TRACK_ERASURE_V1.md`;
- `R2_6_BACKTRACK_TWO_SEAM_STATE_WALK_REPAIR_V2.md`;
- `R2_6_BACKTRACK_V2_FULL_STATE_SCOPE_ADDENDUM.md`;
- `V7_R2_5_CORE_CLASSIFICATION_SCOPE_CORRECTION_V1.md`.

**Supersedes for controlling use:**

- the open conclusion of `R2_6_IMMEDIATE_BACKTRACK_SOURCE_SCOPE_CORRECTION_V1.md`;
- every v7 dependency on R2.5 odd-core exclusion, simple Petersen cycle classification, Type-T graph contraction, or global `C6/C8` annuli.

**Status:**

`R1 AUTHORIAL THEOREM / R2-v7.2 COMPLETE AUTHORIAL CANDIDATE / ORDINARY STRONG INDUCTION ONLY / PDL RECONSTRUCTION AND INDEPENDENT ASSURANCE REQUIRED`.

No established five-CDC proof is claimed.

---

## 1. Controlling fixed-order track interface

After the v7 single inverse pop and local first-failure normalization, retain one finite full-state singular component with:

1. exactly one standard co-root atom at every persistent singular state;
2. nonbranching singular locus;
3. two crossed root resolutions at each atom;
4. complete source topology, ordered dart incidences, side roots, support transport, cap block, route, and required marks;
5. no raw support-switch internal cell;
6. no `2--0/0--2` cell.

No shortest recurrent core or simple Petersen cycle is extracted.

---

## 2. Physical run normalization precedes skeleton reduction

Partition the complete atom history into maximal constant-pivot runs and genuine run-boundary switch cells.

Apply the unique nonpivot full-state root section to every maximal run. This preserves the complete physical history and all side attachments. It does not quotient support monodromy.

After this step:

- every run interior is a rooted carrier;
- the only unresolved locations are genuine switch cells;
- adjacent switch cells are joined by rooted carriers.

This is the exact source-level meaning of the reduced pivot skeleton.

---

## 3. Every genuine switch is nonbacktracking locally

At a switch from run pivot `s` to run pivot `t`, the immediate coefficient states are

`F^-={s,x}`, `F={s,t}`, `F^+={t,y}`

with

`x!=t`, `y!=s`.

These inequalities refer to the atom states immediately adjacent to the switch, not to the next vertices of the reduced run skeleton.

Therefore the six-port local theorem applies and produces a root seam fixing both turn corollas and all complete exterior data.

---

## 4. Immediate reduced-skeleton backtracks

A reduced skeleton segment

`s -> t -> s`

contains:

1. a genuine `s->t` switch cell;
2. the physically rooted maximal constant-`t` carrier;
3. a genuine `t->s` switch cell.

Although the two switch states both project to the Petersen edge `{s,t}`, each switch has its own immediate neighbouring states and is locally nonbacktracking.

Place one seam in each switch cell and replace the middle rooted carrier by its identity product. Literal complete-state gluing gives a root rectangle across the whole corridor.

No coefficient `abba` deletion, physical inverse-move equality, Type-T square enclosure, or source-subgraph contraction is used.

---

## 5. General cellwise erasure

Put a local seam at every genuine run boundary. Use identity subdivision when collars share a history vertex. Cutting along all seams leaves only constant-pivot intervals. Replace each interval by the identity product of its unique complete root history.

Consequences:

- closed tracks give root cylinders;
- normalized/accepted open tracks give root rectangles;
- one unresolved outer endpoint remains the sole endpoint obligation;
- repeated complete endpoints give a closed track and then a legal root `1`-skeleton crosscut.

The theorem is independent of pivot-cycle length and parity.

---

## 6. Corrected v7 fixed-order critical path

The controlling same-order consumer is now:

1. one-atom uniqueness and critical-overlap normalization;
2. full-state nonbranching track interface;
3. physical rootification of maximal constant-pivot runs;
4. a six-port root seam at every genuine switch;
5. identity-product strips and literal gluing;
6. endpoint and periodic crosscut consumption.

Removed from the critical path:

- R2.5 odd `C5/C9` exclusion;
- `C6/C8` classification;
- canonical-star annuli;
- Type-T graph contraction;
- mixed-order recursion.

---

## 7. V7 order architecture remains unchanged

The complete proof uses:

1. equality/DDD forward potential to route exit or the first cancellation;
2. ordinary lower `P` on the actual child target cap closure of order `N-2`;
3. one actual inverse pop;
4. fixed-order return ranked by `(prefix length,target-tree distance,atom rank)`.

There is exactly one selected graph-order descent in the controlling cap-restoration branch. Equal-face adjacency during fixed-order return does not reopen cancellation.

---

## 8. PDL reconstruction target

PDL should reconstruct:

1. normalized full-state run/switch decomposition;
2. physical run rootification with all attachments retained;
3. state-walk versus reduced-skeleton indexing;
4. local nonbacktracking at every genuine switch;
5. two-seam treatment of reduced-skeleton backtracks;
6. complete-state seam/run gluing;
7. closed/open/periodic cellwise erasure;
8. insertion into the v7 pure-NNI return;
9. ordinary strong-induction synthesis.

It must not appeal to abstract `abba` deletion or a missing Type-T source-contraction theorem.

---

## 9. Assurance boundary

### Authorially closed

- first-cancellation ordinary-induction architecture;
- single-pop topology scope;
- no-cancellation-reopening alphabet;
- state-walk cellwise track erasure including reduced-skeleton backtracks;
- removal of R2.5/global annuli from the critical path.

### Still required

- PDL line-by-line reconstruction;
- independent audit of finite tables, state-walk normalization, six-port seams, gluing, terminal consumers, and the proof DAG.

### Not claimed

- independently accepted cap restoration;
- established five-support/five-CDC theorem;
- Lean verification;
- Curator/canonical movement;
- manuscript, release, arXiv, DOI, peer review, or publication status.

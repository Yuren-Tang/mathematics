# The `S_3` equality rank is an arithmetic supplement; physical component chains provide universal totality

## Research Lead scope correction v1

**Role:** `AffineCDC — Research Lead` (`AC-RL`)  
**Workstream:** `AC-RL-PURE-NNI-03`  
**Exact parent head:** `f3dc119f6f6cb0b0e1276c3af736e95a57d53f30`  
**Independent audit consumed:** `audit/affine-cdc-xi-co-root-v1@53be22a0f65b85068b11e5f781579618967db9dd`  
**Classification:** `ARITHMETIC RETAINED / UNIVERSAL SOURCE-SELECTION CLAIM WITHDRAWN / COMPONENT-CHAIN THEOREM CONTROLLING`.

This append-only correction resolves the interaction between the later independent
marked-arc audit, the component-chain repair already present on the branch, and
the supplementary `S_3` rank files appended after that repair.

---

## 1. Accepted audit constraint

The independent audit proves that an arbitrary equal root face cannot be
classified from its four-port matching alone.  If the two marked edges lie on
the same outside channel arc, both root placements can remain one common
component with the same marked route.  Consequently a proof may not infer

```text
arbitrary equal face
    -> route change or one-passage component
```

without recording the actual distribution of the marked terminals over the
outside arcs.

This defect is not restricted to the old co-root `Xi` prose.  It is a general
warning against any equality argument which suppresses a channel outside an
equal face to a bare perfect matching while forgetting distinguished terminal
arcs.

---

## 2. Status of `ZERO_PARENT_S3_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md`

The following parts are retained as exact finite arithmetic:

1. the intrinsic orbit partition for ordered supports `x,y` and the unordered
   complementary triple `U`;
2. the vertex weights
   
   \[
   e=(2,1,1,0),\qquad q=(0,1,0,0)
   \]
   
   on `(X,Y,Z,S)`;
3. invariance of those weights under `Sym(U)` support permutations;
4. all twelve displayed distinct-neighbour root-NNI rows;
5. strict decrease of the displayed integer on every one of those source-legal
   rows;
6. the finite certificate digest
   `d22bce5ea026f5c461575f05ed81ed6e68c71a0c3c459ce6ada93d96f458422e`.

The following universal inference is withdrawn as controlling mathematics:

> following one active `x`-side dart and selecting the first equal `X` or `Y`
> face always yields a route output or a one-sided rescue component after zero
> or one branch swap.

The phrase “the selected local passage contains the active dart” does not by
itself determine the positions of all four global terminals on the two outside
arcs.  Without that datum, the audit obstruction is not excluded.

Therefore Sections 4--6 of
`ZERO_PARENT_S3_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md` are retained only as a
**conditional productive-collar calculation**: once a one-sided component or a
connector between distinct distinguished paths has been established
source-legally, the rank verifies strict arithmetic.  They are not a universal
source-selection theorem.

---

## 3. Status of the supplementary v9 DAG

`ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V9_ZERO_PARENT_REPAIR.md` is
superseded for controlling totality wherever it cites the anchored equal-face
macro as the exhaustive universal step.

Retained from that file:

- the twelve-row arithmetic table;
- the `Sym(U)` invariance observation;
- the exact Heawood finite certificate and shorter conjugate movie;
- the separation between root, co-root and zero inverse-parent rows;
- the stated prohibition of cancellation, lower-order calls and retired
  recurrence machinery.

The controlling v9 architecture remains

`ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V9_COMPONENT_CHAIN_REPAIR.md`.

---

## 4. Controlling universal theorem

Universal source legality is supplied by

`FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md`

and its zero-parent integration

`PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md`.

The controlling proof records the complete physical terminal-path data:

1. remove the active zero-parent two-vertex cell;
2. in the connected complementary carrier fix physical `H_35`;
3. retain the distinguished terminal paths `A--B` and `C--D`;
4. form the quotient of physical `H_35` components and inactive `124` vertices;
5. choose a shortest witnessed physical component chain between the two
   distinguished paths;
6. absorb inactive vertices and intervening cycles by explicit root NNIs or an
   equal branch swap only when its two local passages are known to lie in
   distinct quotient nodes;
7. use the final connector, whose endpoints are on different distinguished
   paths by construction, to change the terminal matching;
8. align the active crossed sheet, switch one closed `H_35` component, and
   perform the literal parent NNI with central root `35`.

The rank

\[
\Lambda_{35}=\text{length of the witnessed physical quotient chain}
\]

falls by one at every nonterminal ambient step.  This is a graph-geometric
source rank, not finite-state/SCC distance.

Thus

\[
\boxed{\texttt{FC-PURE-NNI-ZERO-PARENT-ESCAPE}}
\]

remains closed at Research Lead authorial level, but its controlling proof is
the component-chain theorem, not the unconditional `S_3` anchored-equal-face
argument.

---

## 5. Status of the Heawood certificates

Both

- `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md`, and
- `ZERO_PARENT_HEAWOOD_H35_SOURCE_MOVIE_CERTIFICATE_V1.md`

verify the mandatory issue-#77 movie.  The duplicate certificates use compatible
stable-edge conventions and agree on:

- the two exact post-remote `H_35` components;
- active word `(15,13,25,23)` after the switch;
- literal parent central root `35`;
- cyclic edge-connectivity `6,5,5`.

The later standard-library script is an independent supplementary reproducer.
No Heawood model-case conclusion is withdrawn.

---

## 6. Correct final dependency order

The final Research Lead packet should be read in this order:

1. `ZERO_PARENT_HEAWOOD_H35_ESCAPE_SOURCE_CERTIFICATE_V1.md` — mandatory model;
2. `FIXED_CHANNEL_COMPONENT_CHAIN_ROOT_NNI_THEOREM_V1.md` — physical totality;
3. `PURE_NNI_ZERO_PARENT_ESCAPE_MASTER_THEOREM_V1.md` — universal zero theorem;
4. `CO_ROOT_XI_EQUAL_NEIGHBOUR_AUDIT_CORRECTION_AND_CHAIN_REPAIR_V1.md` — audit
   correction and co-root repair;
5. `ZERO_PARENT_EQUALITY_FIXED_FRAME_LINEAR_RANK_V1.md` — preferred tie-free
   arithmetic rank `Z=2N_1+N_2`;
6. `ZERO_PARENT_S3_SWITCH_INVARIANT_DESCENT_THEOREM_V1.md` and its certificate —
   supplementary orbit arithmetic only, under this correction;
7. `ONE_CROSS_PROOF_DAG_AND_SUPERSESSION_INDEX_V9_COMPONENT_CHAIN_REPAIR.md` —
   controlling DAG;
8. this correction — final scope rule.

The fixed-frame rank `Z=2N_1+N_2` is simpler and orients all fifteen distinct
root-NNI involutions without ties.  The `S_3` pair `(E,Q)` remains a valid
representation-theoretic explanation of twelve rows, but is not needed on the
critical path.

---

## 7. Assurance boundary

This correction strengthens trustworthiness by withdrawing an unnecessary
universal inference before PDL reconstruction.  It does not weaken the
component-chain zero-parent theorem.

Still required:

- independent PDL reconstruction of the physical quotient-chain proof;
- focused independent audit of the chain contraction, final matching change,
  cap/route/category consumers and literal prefix integration.

No accepted universal five-support/five-CDC theorem, Lean theorem, canonical
movement, manuscript, release, tag, arXiv, DOI, peer-review or publication
status is asserted.

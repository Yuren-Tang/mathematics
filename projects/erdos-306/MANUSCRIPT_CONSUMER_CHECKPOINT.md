# Manuscript-consumer checkpoint — E306-CUR-ARCH-01

## 1. Status

`[READY-FOR-MANUSCRIPT-CONSUMER E306-CUR-ARCH-01]`

This checkpoint supplies mathematical architecture only. It does not write or approve manuscript prose.

## 2. Exact consumer inputs

The manuscript worker should consume together:

1. this curation candidate;
2. `proof-development/e306-rigour-v1@cecd3c351302e49577d180ebf42ad7fa784508dc`;
3. `paper/arxiv-v1-revision-v3@94615a5c860be9ce04c2be0153759a4e66fc25ab`;
4. released formal authority `v0.0.3@4582185de1e0e27416e9362e0cc7943c3d2fb4fe`.

The frozen refactor `e55ef359...` may be used for mathematical naming and module-contract orientation only.

## 3. Required theorem architecture

The consumer must expose:

```text
necessity and semiprime criterion
-> avoiding unit theorem
-> analytic interface
-> PNT/Abel provider
-> finite Fourier principle
-> control/local/global mechanism
-> mass and variance
-> major/minor comparison
-> arithmetic closure.
```

It may choose its own article sectioning.

## 4. Required symbolic parameter architecture

Use:

- $1<\mu<\log3$;
- $\alpha=3/2$;
- $v_*=2/9$;
- symbolic $c_\sigma,C_2,K_\sigma,r_T,\delta_T,c_{\mathrm{maj}}$;
- $c_F=16/9$;
- $\beta_b=\sqrt{1-8/(9b^2)}$;
- positive $q_{\mathrm{blk}},q_{\mathrm{gauss}},q_{\mathrm{sib}}$ with sum below $1$.

Choose:

```text
fixed interfaces
-> eta
-> C
-> D_sib
-> G
-> k0.
```

Concrete release values may appear once in a verification/formalization note.

## 5. Mandatory load-bearing statements

The consumer must retain:

- inclusive dyadic endpoint through $3k_0$;
- one common analytic threshold;
- exact centred CRT convention;
- block-cardinality input in the control-load proof;
- corrected adjacent-label hypotheses;
- wrapped-label injection;
- $e^{AJ}$ block entropy;
- exact diagonal energy identity;
- exact forbidden-load cancellation;
- exact expected-mass cancellation;
- Bernoulli nonvanishing and logarithm branch;
- exact Fourier-minor partition;
- fibre factors $b$ and $b(2N+1)$;
- one common $G$-prime reservoir;
- variance comparison on a common scale;
- finite-set/public-tuple distinction.

## 6. Authority language

The manuscript may say:

- the archived v0.0.3 theorem is machine checked relative to two named RS assumptions;
- the article gives a PNT-based proof of the analytic interface;
- the downstream construction corresponds mathematically to the formalization;
- finite numerical closures are machine checked in the archive.

It must not say:

- the PNT provider is formalized in v0.0.3;
- the frozen structural-input refactor is released;
- this Curator pass is an independent formal or mathematical audit;
- formal verification substitutes for omitted paper implications;
- REV3 has already passed the symbolic rewrite.

## 7. Acceptance tests

A manuscript consumer passes when:

1. every load-bearing implication is visible or precisely cited to the human proof source;
2. every concrete public number is derived, normalized, or classified as a finite/safety certificate;
3. no parameter is chosen after a threshold depending on it;
4. all three minor mechanisms fit strictly below the major allowance;
5. release and PNT authority remain separate;
6. the proof ledger is synchronized after the actual rewrite;
7. the returned checkpoint is exact and ready for renewed review.

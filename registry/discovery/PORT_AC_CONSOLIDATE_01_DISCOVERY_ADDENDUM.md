# PORT-AC-CONSOLIDATE-01 discovery addendum

## Epistemic rule

Every entry below is `captured / unreviewed`. These are navigation and future-check seeds, not theorem statements, accepted research obligations, or authorization to start another role.

## D-AC-01 — single-pop image invariants

**Observation.** The disjoint double-atom certificate defeats the broad post-pop state class, while the exact missing-index single-pop image may be narrower.

**Why it may matter.** A source-image invariant could either exclude the witness or show that it is genuinely reachable, resolving the current first dependency edge without inventing another global scheduler.

**Discriminating check.** Reconstruct exact ancestry, borrowed index, cap, route, ordered darts, support transport, and literal next predecessor for all genuine missing-index single-pop outputs; test the Heawood certificate against that image.

**Related units.**

- `math.graph-theory.cycle-covers.disjoint-double-atom-obstruction`;
- `math.graph-theory.cycle-covers.single-pop-image-membership`;
- `math.graph-theory.cycle-covers.disjoint-second-failure-avoidance`.

## D-AC-02 — multi-atom state alphabet

**Observation.** The failed one-atom scheduler exits its own state type by producing two disjoint persistent atoms.

**Why it may matter.** This suggests that one-atom closure and genuine source-image exclusion are logically distinct from a possible multi-atom continuation theory.

**Discriminating check.** Determine whether two persistent atoms can occur in the genuine single-pop image; only if yes, identify a finite source-faithful move alphabet and a non-resetting invariant.

**Boundary.** No two-atom programme is activated by this addendum.

## D-AC-03 — operation-created boundary census as a reusable method

**Observation.** The terminal-exhaustion audit succeeds by starting from the complete closed graph and enumerating exactly which semiedges are created by one physical cut/deletion operation.

**Why it may matter.** This is a reusable way to repair local-to-global quantifier errors in topological graph transformations: classify boundary created by the operation, rather than infer it from two named terminals.

**Discriminating check.** Test this method on other AffineCDC cut, collapse, cap, and oriented-occurrence interfaces while preserving literal darts.

**Related units.**

- `math.graph-theory.cycle-covers.component-chain-third-terminal-counterexample`;
- `math.graph-theory.cycle-covers.co-root-terminal-exhaustion`;
- `math.graph-theory.cycle-covers.zero-parent-terminal-exhaustion`.

## D-AC-04 — counterexample-aware theorem narrowing

**Observation.** The broad component-chain theorem is false, yet the exact-two-terminal-path theorem is valid once the genuine application constructors supply the omitted hypothesis.

**Why it may matter.** The pattern `counterexample -> missing quantified datum -> application constructor -> valid specialization` may be a useful corpus-level relation pattern distinct from ordinary theorem repair.

**Discriminating check.** Search retained AffineCDC failed mechanisms for other cases where an application-specific source constructor supplies the exact missing hypothesis without restoring the false broad statement.

## D-AC-05 — matching-changing connector analogy

**Observation.** The co-root `6-7` model and zero-parent `H_35` model both convert physical connector information into a changed four-terminal matching, but their terminal data and consumers differ.

**Why it may matter.** A shared local matching-change abstraction may shorten exposition or finite verification without identifying the two global fibres.

**Discriminating check.** State the smallest common local lemma whose hypotheses mention only ordered terminal paths, root-valued NNI rows, and matching change; verify that no cap, route, switch, or parent conclusion is smuggled into the common layer.

## D-AC-06 — source-faithful darts and oriented occurrence data

**Observation.** The one-atom and component-chain interfaces require literal dart ancestry, while the existing orientation-obstruction layer distinguishes support-only collapse from enriched oriented occurrence transport.

**Why it may matter.** The two lines may share a reusable notion of retained occurrence identity, even though one concerns proof-state return and the other concerns orientation data.

**Discriminating check.** Compare the exact dart/occurrence schemas only; do not infer an orientation theorem or a five-support implication from the analogy.

## Routing

The highest-value external-review seed remains D-AC-01. D-AC-02 through D-AC-06 remain dormant discovery records until a later exact PORT-DIR packet selects one for research, audit, or formalization.
